# 스키마 계약 — 데이터 표 · 이벤트

> **이 문서가 정본이다.** GDD는 "무엇을 만드는가", 이 문서는 "어떤 모양으로 만드는가"를 정한다.
>
> 🔴 **새 표 · 새 열 · 새 열거값 · 새 이벤트 · 새 시스템을 만들려면 이 문서를 먼저 고친다.**
> 이 문서 변경은 **공지 후 단독 작업**이다 (여러 명이 동시에 고치면 계약 자체가 깨진다). 공지는 **GitHub Issue 또는 Draft PR**로 한다.

| 항목 | 값 |
|---|---|
| 기준 기획 | `VillageDefense-M1-GDD.md` (2026-08-29 정합성 정정본) |
| 최종 갱신 | 2026-08-29 |
| 대상 월드 | `VillageDefense` |
| 협업 방식 | LocalWorkspace + git |

관련: [`협업-규칙.md`](협업-규칙.md) · [`VillageDefense-M1-GDD.md`](VillageDefense-M1-GDD.md) · [`VillageDefense-Roadmap.md`](VillageDefense-Roadmap.md)

---

## 0. 공통 규약

### 0-1. CSV 열 규약

1. `RootDesk/MyDesk/{Name}.csv` + **`{Name}.userdataset` 쌍 필수.** csv만 있으면 런타임에서 안 읽힌다
2. **첫 열이 기본 키.** 복합키는 키 열들을 **앞에 연속 배치**하고, 두 값을 합친 문자열 열을 따로 만들지 않는다
3. 🔴 **`#Note` 는 항상 마지막 열.** 새 열은 반드시 **`#Note` 앞에** 넣는다
4. 🔴 **열 중간 삽입 금지. 뒤에 추가만.** 중간에 넣으면 기존 행의 값이 전부 한 칸씩 밀린다
5. **빈 칸 = 기본값.** 로더는 모든 열에 `tonumber(...) or 기본값` / `or ""` 폴백을 넣는다. 이게 "그 열을 모르는 쪽에서도 안 죽는" 확장 계약의 실체다
6. 열 이름 **PascalCase**, 약어도 첫 글자만 — **`Hp` `Mp` `Ap` `Id`**
7. Boolean 은 소문자 `true` / `false`
8. 단위 — 좌표 **world unit**(1 unit = 100px), 시간 **초**, 확률·배율 **소수**
9. 🔴 **헤더 변경은 공지 후 단독 작업.** `.csv` 는 git `merge=union` 이라 헤더가 두 줄 되면 조용히 깨진다. 공지는 Issue 또는 Draft PR(협업-규칙 §3-4) — Draft PR 이면 **본문 맨 위에 "○○ 헤더 변경 포함"** 을 적는다

> ✅ CSV는 MSW가 **로컬 직접 편집을 공식 허용한 유일한 데이터 형식**이다. 데이터를 CSV로 밀어 넣을수록 협업이 안전해진다.

### 0-2. 열거형

전부 **대문자 SNAKE_CASE**. **이 문서가 유일한 정본**이며 GDD 본문에 사본을 두지 않는다.
⚠️ **엔진 enum 은 ExecSpace를 못 넘는다**(`LEA-3036`, 빌드에 안 잡힘) → 경계를 넘는 값은 **문자열 키**로 보내고 받는 쪽에서 되돌린다.

| 이름 | 값 |
|---|---|
| `VillageId` | `ELLINIA` · `HENESYS` · `KERNING` · `PERION` · `NAUTILUS` · `LITH` |
| `MatchPhase` ⚠️하이픈 | `PHASE0-1`(개척) · `PHASE0-2`(전직 유예) · `PHASE1`(성장) · `PHASE2`(견제) · `PHASE3`(최종) — **2026-09-08 추가기획2 로 5단 개편.** 구 4값 `PHASE1_PIONEER`·`PHASE2_VILLAGE`·`PHASE25_PRESSURE`·`PHASE3_FINALE` 폐기 |
| `NpcRole` (마을 10) | `WORKSHOP_CRAFT` · `WORKSHOP_ENHANCE` · `WORKSHOP_REPAIR` · `SHOP_POTION` · `LIFE_STORAGE` · `PRODUCTION_RECRUIT` · `PRODUCTION_TRAIN` · `RECORD_COLLECTION` · `RECORD_STATS` · `DEFENSE_TOWER` |
| `NpcRole` (공용 2 + 2) | `COMMON_MATCH_GUIDE` · `COMMON_DISPATCH_OFFICER` · `COMMON_DIMENSION_GATE` · **`COMMON_JOB_CHANGE`**(리스항구 전직 교관 · 클릭 → B 스킬 창 · 2026-09-22 `a/plan-0914` B1). 리스항구 공용 물약 상인은 새 역할이 아니라 `SHOP_POTION` 의 PUBLIC 행(`VD_SHOP_POTION_NOVICE`) |
| `LaneStage` | `1`=1차포탑 · `2`=2차포탑 · `3`=억제기 · `4`=코어 |
| `TowerStage` | `TOWER1` · `TOWER2` · `SUPPRESSOR` · `CORE` |
| `TraitKey` (마을별 시설 특성 · A-2-17) | `SUPPRESSOR_ATTACK` · `MAX_TARGETS` · `TOWER_SINGLE_MUL` · `AURA_SPEED` · `AURA_ATK` · `AURA_REGEN` · `REFLECT` — 2026-09-13 WO-021 S1. **`TOWER_SPLASH`**(노틸러스 포탑 · 주 표적 ×2 + 반경 안 ×1) — 2026-09-22 확정(`a/plan-0914` B3b) |
| `DropSourceKind` (A-2-19 `DropTable`) | `GROUND`(사냥터 1/2/3 공통 · `SourceId` = `1`/`2`/`3`) · `MONSTER`(도감 ID) · `ELITE`(엘리트 ID) — 2026-09-22 |
| `DifficultyRuleKey` (A-2-7a `DifficultyRule`) | 배율·수치: `MINION_P3_BOOST` · `ZOMBIE_MUL` · `BALROG_HP_MUL` · `BALROG_ATK_MUL` · `BOSS_HP_MUL` · `BOSS_ATK_MUL` · `MATCH_EXP_MUL` · `ACCOUNT_EXP_MUL` · `ELITE_PER_KILLS` · `ELITE_COUNT` · `REGION_DROP_MUL` · `BALROG_TICKET` · `BOSS_SOULSTONE` · `UNLOCK_ACCOUNT_LV` / 선택창 문구: `TITLE` · `INTRO` · `NEG` · `POS` · `REWARD` · `HARD` · `TIP` — 2026-09-22 · append-only |
| `GuideStepKind` (A-2-22 `GuideStep`) | `GOAL`(목표 바) · `TOAST` · `CHECK`(체크리스트) — 2026-09-22 |
| `RecruitTier` (A-2-20 `MonsterRecruit`) | `1` · `2` · `3` = 사냥터 1/2/3 몹 — integer · 2026-09-22 |
| `AchievementCondType` (A-2-23 `AchievementConfig`) | 계정 누적: `MATCH_SETTLED`(순위 정산 받은 판) · `RANK_FIRST`(1위 · `CondParam` = ★, `0` = 무관) · `BALROG_HIT`(발록이 쓰러진 매치에서 발록에게 피해) · `MONSTER_KILL`(일반 사냥 몹 처치 · 막타) · `ELITE_KILL`(자이언트 처치 · 막타) · `BOSS_KILL`(지역 보스 처치 판정 = 최다 피해) · `RECRUIT`(수비대 모집 개체) · `DISPATCH_UNITS`(파병 보낸 개체) / 도감에서 도출: `DEX_FOUND`(발견한 일반 종 수 · `Target` `0` = 전 종) · `DEX_BOSS`(발견한 보스 수 · `0` = 전부) / 매치 1판 안: `ENHANCE_ALL_MAX`(장비 칸 전부 최대 강화) — 2026-09-26 · append-only |
| `CollectionKind` (A-2-25 `CollectionReward`) | `MONSTER`(그 종 처치 수) · `ELITE`(그 종의 자이언트 처치 수 — 같은 도감 칸) · `BOSS`(보스 처치 수) — 2026-09-26 |
| `GrowthActionType` (A-2-26 `GrowthPoint`) | `FACILITY_LEVEL`(시설 하나가 새 레벨에 처음 도달) · `TRAINING_LEVEL`(병과 훈련 한 단계 완료) · `CRAFT_FIRST`(제작 레시피 첫 완성) · `RECRUIT_FIRST`(수비 몬스터 종 첫 모집) — 2026-09-26 |
| `MatchOutcome` (저장 레코드 `MatchHistory` · A-4) | `BALROG`(발록 처치로 끝남) · `TIMEOUT`(시간 종료) · `ALL_OUT`(전원 탈락) · `LEFT`(끝나기 전에 나감 · 순위 없음) — 2026-09-26 |
| `GemId` | `DIAMOND`(공격력) · `SAPPHIRE`(마력) · `AMETHYST`(이동속도) · `AQUAMARINE`(점프력) · `GARNET`(명중률) · `OPAL`(회피율) · `TOPAZ`(HP) · `EMERALD`(MP) · `STR_CRYSTAL` · `DEX_CRYSTAL` · `LUK_CRYSTAL` · `INT_CRYSTAL` |
| `StatId` | `STR` · `DEX` · `INT` · `LUK` · `ATTACK` · `MAGIC` · `DEFENSE` · `SPEED` · `JUMP` · `ACCURACY` · `AVOID` · `MAX_HP` · `MAX_MP` |
| `EquipSlot` | `WEAPON` · `HAT` · `TOP` · `BOTTOM` · `SHOES` · `GLOVES` |
| `ItemType` | `EQUIP` · `CONSUME` · `MATERIAL` · `GEM` |
| `SectorId` | `WORKSHOP` · `LIFE` · `RECORD` · `DEFENSE` |
| `MonsterClass` | `MELEE` · `RANGED` · `TANK` |
| `RouteKind` | `VILLAGE` · `HUB` · `LANE` · `REAR` · `BOSS` |
| `OwnershipMode` | `OWNER_ONLY` · `PUBLIC` |
| `SourceType` (획득) | `MONSTER_KILL` · `ELITE_KILL` · `MINION_KILL` · `BOSS_KILL` · `BOSS_FIRST_CLAIM` · `ACHIEVEMENT` · **`MATCH_RANK`**(순위별 심장·계정 경험치 · 2026-09-22) · **`ACCOUNT_LEVELUP`**(계정 레벨업 심장 · 2026-09-22) · **`COLLECTION`**(도감 단계 계정 경험치 · 2026-09-26) |
| `SinkType` (소모) | `CRAFT` · `ENHANCE` · `REPAIR` · `SHOP_BUY` · `MONSTER_RECRUIT` · `MONSTER_TRAIN` · `MONSTER_HEAL` · `TOWER_UPGRADE` · `TOWER_REPAIR` · `TOWER_REBUILD` · `BALROG_CHALLENGE` · `DISPATCH` · **`BOSS_ENTRY`**(지역 보스 입장 · 지역 재화 25) · **`DEATH_PENALTY`**(2P+ 사망 · 메소 5,000) · **`GEM_EXCHANGE`**(보석 3:1) — 뒤 3개 2026-09-22 |
| `MoveType` *(소문자 유지)* | `hop` · `walk` · `fly` · `stay` |
| `AiType` *(소문자 유지)* | `wander` · `chase` · `static` |
| `JobId` | `NOVICE` · `WARRIOR` · `MAGICIAN` · `ARCHER` · `THIEF` · `PIRATE` — ✅ **확정**(2026-09-09 A 승인). `ItemInfo.ReqJob` 은 현재 숫자 `0` 자리표시자라 지킬 관례 없음 — 문자열 ID 로 바꿀 때는 헤더 계열 변경(협업-규칙 §2-1)이라 **사전 공지 후 A 가 채운다**. 스냅샷의 `BEGINNER` / `MAGE` 는 B 가 치환 |
| `JobTier` | `0`(초보자) · `1`(Lv10 1차) · `2`(Lv20 2차) · `3`(Lv30 3차) — **integer**. 🔴 값 의미 고정(append-only · 이후 이름 변경·삭제 금지): `0`=`NOVICE` · `1`=1차 · `2`=2차 · `3`=3차. A 의 `VillageOwnership` 마을 소유 조건(GDD §4.1 "전직 여부")이 `JobChangedEvent.Tier` 로 판정한다 |
| `SkillBehavior` | `MELEE_ARC` · `PROJECTILE` · `ORIGIN` · `BLINK` · `BUFF_SELF` · `BUFF_ALLY` · `AOE` · `TAUNT` · `PASSIVE` — 🟡 스냅샷 상태: 실행기(`SkillExecutors`)에 `MELEE_ARC` · `TAUNT` 핸들러가 아직 없고, `AOE` · `BUFF_ALLY` 는 핸들러만 있고 행이 없다 |
| `EffectUnit` | `ATK_PCT` · `HP_PCT` · `FLAT` · `RATIO` · `SEC` · `STACK_PCT` |
| `BuffTag` | `HYPER_BODY` · `MAGIC_GUARD` · `DARK_SIGHT` · `SHADOW_PARTNER` · `ENERGY_SHIELD` · `INVULNERABLE` · `REFLECT_HP_PCT` · `COST_REDUCE_UPGRADE` · `COST_REDUCE_RECRUIT` · `DROP_RATE_DREAM_SHARD` · `STACK_GAIN` · `GUARANTEED_CRIT` · `ENERGY_CHARGE` — B 내부용(`SkillExecutors` 가 해석). 빈칸 = 없음 |
| `QuestState` ⚠️ M2 | 예약만 |

> `MoveType`/`AiType` 만 소문자인 것은 **기존 78행이 이미 그 값**이기 때문이다. 대문자로 바꾸면 전 행을 고쳐야 하므로 예외로 둔다.
>
> ⚠️ **`MatchPhase` 만 하이픈을 쓴다** (사용자 확정 2026-09-08). SNAKE_CASE 원칙의 유일한 예외다 — `PHASE05` 가 "0.5페이즈"로 안 읽혀서 `PHASE0-1` / `PHASE0-2` 로 정했다. **다른 열거형에 하이픈을 퍼뜨리지 않는다.** 이 값들은 CSV 셀·문자열 비교로만 쓰이므로 식별자 규칙에 걸리지 않는다.

### 0-3. 이름 규약

| 대상 | 규칙 | 예 |
|---|---|---|
| 맵 파일 · 맵 루트 엔티티 | **`{지역}_{종류}_{이름}`**, **영어 필수** | `Henesys_Village_MinimiMain` |
| `.model` EntryKey | `model://` + **전부 소문자** | `model://chasemonster` |
| 스크립트 | PascalCase, **기능 폴더 아래 필수** (`MyDesk/` 루트 배치 금지) | `Lane/LaneController.mlua` |
| `@Logic` 접근자 | `_` + **정확한 스크립트명**, 축약 금지 | `_EventBus` · `_MinionFlowService` |
| UIGroup 파일 | `{도메인}Group.ui` | `VillageWorkshopGroup.ui` |
| 이벤트 | `{명사}{과거분사}Event` | `TowerDestroyedEvent` |
| 인스턴스 룸 키 (매치 · 2026-09-23 B11) | `match_{MatchId}_{세대}` — `MatchId` = 로비 매치 ID(`m1`…) · 세대 = 로비가 룸을 만들 때마다 1씩 오르는 번호(같은 매치를 다시 출발시켜도 새 키). 둘 다 월드 인스턴스가 뜰 때 1부터 — **키는 월드 인스턴스 안에서만 유일하면 된다**(룸과 공유 메모리가 월드 인스턴스 단위 · `RoomService.GetSharedMemory` 설명). **같은 이름의 공유 메모리가 인계 레코드**(A-4 `MatchRoomHandoff`) | `match_m1_1` |

🔴 **`MapName` 값은 맵 루트 엔티티 이름과 글자 단위로 일치해야 한다.** 하나라도 틀리면 그 스포너는 **에러 없이 조용히 안 돈다** (같은 원인으로 이미 32건이 죽은 이력).

🔴 **매치 맵 = 인스턴스 맵** (2026-09-23 · WO-024 · B11). 매치는 매치마다 인스턴스 룸 하나(위 룸 키) 안에서 돌고, 정적 룸에는 **로비 2개**(`Orbis_Lobby_VictoriaStation` · `Orbis_Lobby_VictoriaShip`)만 있다. 인스턴스 맵은 맵 루트 `MapComponent.IsInstanceMap = true` **와** 맵 파일 헤더 `"Usage": 1` 이 **둘 다** 있어야 한다 — 헤더가 0 이면 `CreateInstanceRoom` 이 `LEA-3002` 로 실패한다(실측). 룸에 넣는 맵 목록 = `PortalRoutes` 의 `FromMap`·`ToMap` 전부(로비 2개 제외) → 새 맵을 매치에 넣으려면 인스턴스 맵으로 만들고 이 표에 노선을 둔다. `@Logic`·Service 는 룸마다 따로 뜨고, 룸 사이 이동은 `RoomService` 로만 한다(포탈 불가).

🔴 **엔트리·폴더 이름 제약** (LocalWorkspace가 OS 파일 규칙을 탄다): 금지 문자 `< > : " / \ | ? *` · 예약어 `CON` `PRN` `AUX` `NUL` `COM1~9` `LPT1~9` · 이름 시작/끝의 마침표·공백 · **전체 경로 255자 초과 금지** → clone 위치를 짧게.

### 0-4. ID 대역

| 대상 | 대역 |
|---|---|
| 밍키타 출신 몬스터/NPC | `1001` ~ `1999` |
| 안농 출신 몬스터/NPC | 메이플 원본 ID 그대로 (6~7자리) |
| 기능 NPC | `VD_` 접두 문자열 — `VD_WORKSHOP_CRAFT` |
| **신규 도메인** (아이템·스탯·보석·섹터·포탑·레시피·병과·직업·엘리트 재료) | 🔴 **숫자 ID를 쓰지 않고 대문자 열거값 문자열.** 대역 관리 자체가 불필요해진다 |
| 숫자 ID가 불가피한 곳 | **사람별 하위 대역**을 배정 |

⚠️ `.csv` 는 `merge=union` 이라 **ID가 중복돼도 git이 안 잡아준다.** 두 줄 다 남고 조용히 깨진다 → §정합성 검사 3번이 이걸 막는다.

### 0-5. 상태 객체 필드 규약

- **도메인 접두사 필수** — `account_*` / `match_*` / `village_*`
- 🔴 **매치 재화와 계정 재화를 같은 필드·같은 저장 키에 합치지 않는다**
- `AccountProfile` 은 **`SchemaVersion` 필수**, M1 최초값 `1`. `2` = WO-022(무료 스택). **`3` = 2026-09-22 `a/plan-0914` B8·B10** — `account_level`(기본 `1`) · `account_exp`(기본 `0`) · `account_guideOff`(기본 `false`) 추가. **`4` = 2026-09-26 `a/m1-finish`(기록·칭호 등록서)** — `account_titles`(보유 칭호 `TitleId` 쉼표 목록 · 기본 `""`) · `account_title`(장착 `TitleId` · 기본 `""` = 없음) 추가. 구버전 JSON 은 키가 없으므로 **역호환 기본값 분기 필수**(계정 영구 재화가 걸린 표라 잘못 읽으면 심장이 사라지거나 공짜로 생긴다 — WO-022 교훈)
- 영구 저장 진입점은 **`PlayerDBManager` 하나뿐**
- 🔴 **저장은 read-modify-write.** 통째로 덮어쓰면 다른 도메인 필드가 지워진다
- ⚠️ **선언 없는 `self.<name>` 대입은 런타임 에러**(`cannot set <name>, no such field`) → `property` 선언 또는 `self._T.<name>`

상태 객체 (GDD §4.10): `AccountProfile`(영구) · `MatchLobbyState` · `MatchSession` · `MatchPlayerState` · `VillageMatchState` · `MinionFlowState`

`MatchPlayerState`의 플레이어 성장 원장은 `match_stats` · `match_inventory` · `match_equipped` · `match_ap` · `match_sp`다. 장비·인벤토리 목록이 ExecSpace 경계를 넘을 때는 `any` table이 아니라 문자열로 직렬화한다.

---

## 1. 🔴 신규 시스템 등록 절차

**문제**: 이 계약서는 **이미 아는 것**만 다룬다. 아무도 계획하지 않은 새 시스템(예: 플레이어 스킬 시전)은 협약 밖에서 태어나 각자 방식으로 만들어지고 **합칠 때 터진다.** 협약을 아무리 잘 맺어도 "새로 만드는 것"에 대한 협약이 없으면 소용없다.

**해결**: 새 시스템은 **구현 전에 등록을 먼저 머지**한다. 이벤트 3종 세트(§B-2)의 확장판이며 같은 논리다.

### 등록서 8항목

| # | 항목 | 이게 없으면 merge에서 무슨 일이 나나 |
|---|---|---|
| 1 | 시스템 이름 + **소유 폴더** | 폴더가 갈려야 `.mlua` 충돌이 원천 차단된다 |
| 2 | 새 CSV 표 — 파일명 · 헤더 · 기본 키 (없으면 "없음") | 이름·열이 미리 갈려야 `merge=union` 이 안전하다 |
| 3 | 새 이벤트 — **발행할 것 / 구독할 것** | 미리 선언돼야 상대가 구독할 수 있다. 나중에 하면 **발행자 파일을 고쳐야 한다** |
| 4 | 새 열거값 | §0-2 정본에 합류. 같은 개념을 다른 값으로 쓰는 걸 막는다 |
| 5 | ID 대역 / 문자열 접두사 | union 머지는 **ID 중복을 안 잡아준다** |
| 6 | 저장 필드 — 어느 상태 객체에 무슨 필드 + 접두사 | 통짜 덮어쓰기로 남의 필드가 지워지는 사고를 막는다 |
| 7 | 새 `.model` / `.ui` 파일명 | 새 파일이면 안전, 기존 파일이면 소유권 협의 |
| 8 | 🔴 **건드리는 기존 파일 전부** | **여기가 유일한 협의 지점이다** |

### 절차

1. 만들 사람이 이 문서에 **등록 항목을 추가**해 PR을 올린다 — **코드 없이 문서만**
2. 상대는 **8번**과 **2·5번**만 본다
3. 머지된 뒤에 구현을 시작한다

> **8번이 비어 있으면 = "새 파일만 만든다" = 사실상 통과.** 8번에 뭐가 있으면 거기만 이야기하면 된다.
> self-merge 가능 여부는 8번 중 **남의 폴더 파일**만 본다(협업-규칙 §3-3). 자기 폴더 안 기존 파일은 8번에 적되 상대 승인 대상이 아니다.

### 등록 대상 판정

아래 중 하나라도 해당하면 등록한다 — **새 CSV 표** / **새 이벤트** / **새 열거값** / **상태 객체 필드 추가** / **남의 폴더 파일 수정**.
해당 없으면(내 폴더 안에서 새 `.mlua` 만 추가) 그냥 만든다.

- **기존 표에 열 추가는 등록 대상이 아니다.** §0-1 9(헤더 변경 = 공지 후 단독 작업)를 따르고, 이 계약서의 **헤더 정의**와 `Docs/tools/check-integrity.cjs` CANONICAL 은 **구현 PR 안에서 같이** 고친다. 등록 PR 을 따로 먼저 내지 않는다
- `Docs/` 의 자기 시스템 몫(자기 표 헤더 정의 · 자기 CANONICAL 항목 · 자기 CHANGELOG 조각)은 **"남의 폴더"로 치지 않는다**(협업-규칙 §2-2)

### 등록된 시스템

| 시스템 | 소유 | 상태 |
|---|---|---|
| 스킬 시전 · 스킬 UI | B (`Skill/`) | ✅ **등록 완료** (#37 · 2026-09-09) — 경계면 `StatUIController.mlua`(`OnClickSkill`) 은 A 승인 필요. `SummonManager` 쪽(`SpendMp` · `GrantMp` · MP 회복 · maxMp 다리)은 **B 가 `b/summon-mp-ledger`(PR #41) 에서 네 항목 전부**(A 의 #40 comment 5588167790 번복 · A 승인 필요) |
| 전직 | B (`Job/`) | ✅ **등록 완료** (#37) — 🔴 **10레벨 전직이 마을 획득의 전제**라 사용자 골격의 크리티컬 패스에 있다. `JobChangedEvent` → 코어 연결 자격, `JobId` 열거값 확정(§0-2)이 최우선 협의 항목 |
| 플레이어 스탯 · 장비 · 강화 · 기능 NPC · UI | A (`Stat/` `Item/` `Npc/`) | 🟡 **등록 문서 PR 진행 중** — 경계면 `PlayerAttack.mlua` · `StatusHUD.ui` · `LevelTable.csv`은 B 리뷰 필요 |
| 매치 시계 5단 · 드랍 소유권 · 물약 쿨타임 (추가기획2 · WO-014) | A (`Match/` `Farm/` `Item/`) | 🟡 **등록 + 구현 PR #35 (Draft)** — 경계면 없음(8번 비어 있음 → §3-3 self-merge 대상) |
| 파병 | A (`Dispatch/`) | 🟡 **등록 문서 PR 진행 중** (2026-09-09 · `a/dispatch-real-move`) — 8번이 전부 A 소유라 B 협의 항목 없음 |
| **계정 영구 저장 (DataStorage · WO-018 S1)** | A (`Progression/`) | 🟡 **등록 + 구현 PR #46 (Draft)** (2026-09-09 · `a/player-db`) — 8번은 **이 계약서 + `Match/` · `Lane/` 각 1줄**, 전부 A 소유. **B 협의 항목 없음** |
| **침범 안정장치 + 마을별 시설 특성 (WO-016 · WO-021 S1)** | A (`Lane/` `Faction/`) | 🟡 **등록 + 구현 PR #53 (Draft)** (2026-09-13 · `a/lane-guard-facility-traits`) — 8번 전부 A 소유. **B 협의 항목 없음** |
| **9/14 기획 반영 — 웨이브·난이도 3단계·전투 공식·엘리트·수비대 모집·계정 레벨·사망·★1 안내 (WO-027/028/029)** | A (`Match/` `Lane/` `Stat/` `Monster/` `Boss/` `Item/` `Farm/` `Progression/` `Guide/`) | 🟡 **등록 PR = 통합 브랜치 `a/plan-0914` 의 B0 (`a/p0914-0-contract` · Draft PR #67 아래 stacked)** (2026-09-22 · #40 공지 5776547020) — 8번 전부 A 소유. **B 협의 항목 없음.** B 에게는 B4 뒤 `SkillDatabase.GetAttackPower` 한 줄 교체 부탁만(PR 코멘트) |
| **기록 · 칭호 — 매치 집계 · 공개 통계 · 계정 도감 · 업적 · 칭호 · 플레이 기록 + 파병 등급 경고 (M1 마무리 · WO-031)** | A (`Match/` `Progression/` `Dispatch/` `Farm/` `Boss/` `Lane/` `Item/` `Npc/`) | 🟡 **등록 PR = 통합 브랜치 `a/m1-finish`(#103) 의 묶음 0 (`a/m1-0-contract` · Draft PR #104 아래 stacked)** (2026-09-26 · #40 공지 5844015147) — 8번 전부 A 소유. **B 협의 항목 없음** |

#### 등록서 — 기록 · 칭호 (M1 마무리 · 2026-09-26 · `a/m1-finish` · WO-031)

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | 매치 집계(`MonsterKillCount` · `BossKillCount` · `VillageGrowth`) + 공개 통계 `PublicPlayerSummary` = `Match/`(신규 `MatchTallyService`) · 계정 도감 · 업적 · 칭호 · 플레이 기록 = `Progression/`(신규 `CollectionService` · `AchievementService` · `TitleService` · `MatchHistoryService` · 저장 도메인 `AccountRecordData` · 로비 계정 창 `AccountRecordUIController`) · 파병 등급 경고 · 레인 HUD 배지 = `Dispatch/` + `Match/MatchClockUIController` — **전부 A** |
| 2 | 신규 CSV | **`AchievementConfig`**(A-2-23 · `AchievementId` · A-3 예약 해소) · **`TitleInfo`**(A-2-24 · `TitleId`) · **`CollectionReward`**(A-2-25 · `Kind`+`Tier`) · **`GrowthPoint`**(A-2-26 · `ActionType`) — 각각 쓰는 묶음(2 `GrowthPoint` · 4 `CollectionReward` · 5 `AchievementConfig` `TitleInfo`)의 구현 PR 에서 파일 + `.userdataset` 생성. **기존 표 헤더 변경 없음** |
| 3 | 이벤트 | **신규 없음 — 직접 호출.** B-3 `Progression/` 예약 4종(`AchievementProgressedEvent` · `AchievementUnlockedEvent` · `CollectionUpdatedEvent` · `VillageGrowthPointAddedEvent`)은 **예약 유지**(구독자가 A 자신뿐 · 파병 등록서와 같은 판단). 보상은 `RewardGrantedEvent` 대신 `AccountLevelService.AddExp` 직접 호출 — 중복 지급은 **달성 기록**(업적 `d` · 도감 단계 `tm`/`te`/`tb` · A-4)이 막는다 |
| 4 | 열거값 | §0-2 에 **`AchievementCondType`** · **`CollectionKind`** · **`GrowthActionType`** · **`MatchOutcome`** 신설 · `SourceType += COLLECTION` |
| 5 | ID | `AchievementId` = `ACH_*` · `TitleId` = `TTL_*` 대문자 문자열(숫자 ID 없음). 도감 키 = **`MonsterInfo.Id`**(일반 종 · 자이언트는 `EliteMonsterInfo` 의 베이스 몹 칸에 합산) · 보스 = **`BossInfo.BossId`**. 도감 목록은 표에서 도출 = `MapMonsters` 에 나오는 종 + `BossInfo` 보스(별도 목록 표 없음) |
| 6 | 저장 필드 | **`AccountProfile`** += `account_titles` · `account_title`, `SchemaVersion` 3 → **4**(§0-5 · 역호환 기본값). **새 저장 키 2** — `AccountCollection`(도감) · `AccountAchievement`(업적 카운터·달성) + 예약 키 **`MatchHistory`** 구현(최근 20판). 레코드 모양은 A-4. 세 키 모두 `AccountProfile` 과 분리(자주 바뀜 · 계속 자람 → 바이트 분리 · §0-5 통짜 덮어쓰기 방지) · 저장은 `PlayerDBManager` 단일 창구(도메인 `AccountRecordData` 를 한 줄씩 추가). 매치 원장(서버 메모리 · 저장 안 함): 유저별 `MonsterKillCount` · `BossKillCount` · `VillageGrowth` + 이미 받은 성장 액션 키 · 매치 1판 조건(`ENHANCE_ALL_MAX`) |
| 7 | 신규 `.model` / UI | 새 UI 그룹 **`AccountRecordGroup`**(로비 계정 창 · 탭 도감/업적/칭호/기록 · GroupOrder 5). 기존 그룹에 **추가만**(사용자 배치 보존): `LobbyGroup`("내 기록" 버튼) · `MatchClockGroup`(파병 배지) · `VillageRecordGroup`(생존자 목록 + 선택 상세) · `MatchResultGroup`(처치 수 · 칭호). 새 `.model` 없음 — 칭호 이름표는 플레이어의 엔진 `NameTagComponent.Name` 한 줄("칭호 닉네임") |
| 8 | 🔴 건드리는 기존 파일 | `Dispatch/DispatchService` · `Match/MatchClockUIController` · `Match/MatchSessionLogic` · `Match/MatchResultUIController` · `Match/RankRewardService` · `Match/LobbyUIController` · `Match/MatchLobbyGateway`(로비 계정 창 조회) · `Farm/FarmReward` · `Boss/BossRewardService` · `Boss/BalrogRoomService`(발록 피해 기록) · `Lane/LaneStateService`(성장·모집·파병 훅 · 통계 뷰) · `Item/CraftService` · `Item/EnhanceService` · `Npc/VillageRecordUIController` · `Progression/PlayerDBManager`(도메인 한 줄씩) · `Progression/AccountData` · `Progression/AccountProfile` · `Progression/AccountStorageLogic` · 위 UI 그룹 4개 · 이 계약서 · 정합성 검사 · M1 GDD — **전부 A 소유. B 협의 항목 없음.** `Skill/` `Job/` `PlayerAttack` `PlayerHit` 은 건드리지 않는다 |

> 값(도감 단계 · 업적 16개 · 칭호 · 성장 점수)의 정본은 허브 `WorkOrders/WO-031-M1-마무리-기록-파병-통합.md` §4~§6 — 전부 CSV 값이라 바꿔도 코드는 안 바뀐다.

#### 등록서 — 9/14 기획 반영 (2026-09-22 · `a/plan-0914` · WO-027 · WO-028 · WO-029)

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | 웨이브 표·억제기 폭발·시설 HP 재조정 = `Lane/` · 난이도 3단계 + 단일 적용점 `DifficultyService` + 선택창 + 지역 보스 입장권 = `Match/` `Boss/` · 전투 공식(명중 레벨차 · 치명 100% · 방어 감산 · STR→방어 · 일반몹 공격) = `Stat/` + 각 공격자 파일 · 엘리트 자이언트 = `Monster/`(신규 폴더 · `Catalog/EliteCatalog` 유지) · 수비대 모집 재료 경로 = `Lane/` · 순위 보상·계정 레벨 = `Progression/` `Match/` · 사망/리스폰 = `Match/`(신규 `PlayerRespawnService`) · ★1 안내 = `Guide/`(신규 폴더) · 인스턴스 룸 동시 매치 = `Match/`(B11 · WO-024 · 룸 키·인계 레코드 = §0-3 · A-4) — **전부 A** |
| 2 | 신규 CSV | **`MinionWave`**(A-2-6a · `Profile`+`WaveKey`) · **`DifficultyRule`**(A-2-7a · `Difficulty`+`Key`) · **`DropTable`**(A-2-19 · `SourceKind`+`SourceId`+`ItemId`) · **`MonsterRecruit`**(A-2-20 · `MonsterId`) · **`RankReward`**(A-2-21 · `Rank`+`Difficulty`) · **`GuideStep`**(A-2-22 · `StepKey`) — 각각 만드는 묶음(B3/B6/B2/B5/B8/B10)의 구현 PR 에서 파일 + `.userdataset` 생성. **기존 표 열 추가**(§0-1 9 · 구현 PR 안에서 헤더+CANONICAL): `MonsterInfo += Attack`(B2) · `ConsumeInfo += HealHpPct,HealMpPct`(B1) · `BossReward += SoulstoneStar1/3/5`(B6) · `EliteMonsterInfo += ScaleMul,CoinDrop,DreamDrop,SoulstoneDrop,Tier` · `EliteSpawnTable += KillsPerElite`(B7) |
| 3 | 이벤트 | **신규 없음.** 안내·난이도 문구·사망 팝업은 서버→클라 문자열 RPC. 보상은 기존 `RewardGrantedEvent`(단일 통로). 사망 감지는 A 쪽 피해 적용점(`ApplyPlayerDefense` 뒤 HP≤0) — `PlayerHit`(B 수정 중) 손대지 않음 |
| 4 | 열거값 | §0-2 에 **`DropSourceKind`** · **`DifficultyRuleKey`** · **`GuideStepKind`** · **`RecruitTier`** 신설 · `TraitKey.TOWER_SPLASH` 확정 · `SourceType += MATCH_RANK, ACCOUNT_LEVELUP` · `SinkType += BOSS_ENTRY, DEATH_PENALTY, GEM_EXCHANGE` |
| 5 | ID | 신규 숫자 ID 없음. `EliteId` = `E<BaseMonsterId>` 문자열 · 아이템 `WEAPON_CLUB` `ARMOR_HP_LV10` `POTION_ORANGE` `POTION_WHITE` `ELIXIR` `POWER_ELIXIR` `GEM_RANDOM` |
| 6 | 저장 필드 | **`AccountProfile`** += `account_level`(1) · `account_exp`(0) · `account_guideOff`(false) · `SchemaVersion` 2 → **3**(§0-5 · 역호환 기본값 필수). 매치 원장: `MatchPlayerState` 에 사망 페널티·엘리트 킬 카운터(맵별 · 서버 메모리) · 안내 단계(서버 메모리) — 저장 안 함 | ✅ B9(2026-09-23 · PR #78): 사망·리스폰 = `Match/PlayerRespawnService`(참가자 `IsDead` 폴링 · 1P 이전 리스항구 무료 · 2P+ 내 마을(`P_To_<Lane1>` 포탈 위치) + 팝업 `RevivePopupGroup`/`PlayerRespawnUIController` 선택 5,000메소 `SummonManager.SpendMeso` / 경험치 `DeductExpInLevel`(구간 필요치 ×0.5 · 구간 안 경험치까지만 · 레벨 유지) · 선택 시간 = `RespawnDuration` 15초(고르면 `ProcessRevive`) · 발록 방 사망은 기존) |
| 7 | 신규 `.model` / UI | UI 그룹 **`GuideGroup`**(목표 바) · 부활 팝업(기존 `PopupGroup` 확장 검토) · 로비 그룹 난이도 선택창 재배치(S1 뼈대 · 엠블럼 자리 · 사용자 배치 보존) · 모집 창 재료 표시. 새 `.model` 없음(엘리트는 베이스 몹 모델 + `Scale`) |
| 8 | 🔴 건드리는 기존 파일 | `Farm/FarmReward` · `PortalNetwork` · `Item/InventoryService` · `Item/ItemCatalog` · `Item/EnhanceService` · `Summon/SummonManager` · `Lane/MinionFlowService` · `Lane/LaneFacility` · `Lane/LaneStateService` · `Lane/LaneFacilityService` · `Faction/FactionAttack` · `Faction/FactionAuraController` · `Match/MatchSessionLogic` · `Match/MatchLobbyGateway` · `Match/LobbyUIController` · `Match/MatchResult*` · `Stat/DamageFormula` · `Stat/StatService` · `Stat/StatUIController` · `MonsterAttack` · `Boss/BossSkillRunner` · `Boss/BossProjectileAttack` · `Boss/BossSpawner` · `Boss/BalrogRoomService` · `Catalog/EliteCatalog` · `Progression/AccountProfile` · `Progression/AccountStorageLogic` · `Progression/BalrogHeartService` · `WorldMap/WorldMapController` · CSV 값 변경(`LevelTable` `MapMonsters` `MinionPhaseConfig` `TowerConfig` `VillageFacilityTrait` `EnhanceTable` `ItemInfo` `ShopItem` `FunctionalNpcCatalog` `MapNpcs` `BossInfo` `DifficultyConfig`) · 이 계약서 · 정합성 검사 · `Docs/추가기획3/구현항목-결정.md`(신규) · B11: `Progression/PlayerDBManager`(룸 이동 전 동기 저장) · 로비 2개를 뺀 맵 41개(인스턴스 맵 표시·헤더) — **전부 A 소유. B 협의 항목 없음.** `Skill/` `Job/` `PlayerAttack` `PlayerHit` 은 건드리지 않는다 |

> 값의 정본은 허브 `WorkOrders/WO-027-밸런스-초안표.md` §9-0 · 저장소 안 요약은 `Docs/추가기획3/구현항목-결정.md`. 묶음 순서·검증은 `WO-029`.

#### 등록서 — 침범 안정장치 + 마을별 시설 특성 (2026-09-13 · WO-016 · WO-021 S1)

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | 순차 무적 · 알림 정책 · 수비대 이속 2배 · 대플레이어 계수 = `Lane/` · 마을별 특성(억제기 공격 · 대상 수 · 포탑 단일 5배 · 이속 오라 · 반사) = `Lane/` + `Faction/`, 전부 A |
| 2 | 신규 CSV | **`VillageFacilityTrait`** — 헤더·기본 키는 **A-2-17** 이 정본 (복합키 `VillageId`+`Stage`+`TraitKey`) · **`FacilityAttackFx`**(2026-09-14 추가 · WO-025 · **A-2-18** · 복합키 `VillageId`+`Stage`+`Level`) |
| 3 | 이벤트 | **신규 없음.** 알림은 `_UIToast` 토스트뿐 |
| 4 | 열거값 | **`TraitKey`** 신설 (§0-2) — `SUPPRESSOR_ATTACK` · `MAX_TARGETS` · `TOWER_SINGLE_MUL` · `AURA_SPEED` · `AURA_ATK` · `AURA_REGEN` · `REFLECT`. S2 후보 `TOWER_SPLASH` 는 예약만. `FacilityAttackFx.Mode` = `SHOT` · `CAST_HIT` |
| 5 | ID | 신규 숫자 ID 없음. 전부 대문자 열거값 문자열 |
| 6 | 상태 | 저장 필드 없음. 원장(`VillageMatchState`)에 알림 시각 `alertAt`(서버 메모리 · 매치 초기화 시 비움) |
| 7 | 신규 `.model` / `.ui` | **`Models/Effects/LaneShot.model`**(`model://laneshot` · 순수 스프라이트 발사체 · 2026-09-14). 억제기의 전투·오라 부품은 스폰 직후 `AddComponent` (기존 `LaneSuppressor.model` 무수정) |
| 8 | 🔴 건드리는 기존 파일 | `Lane/LaneStateService.mlua` · `Lane/LaneFacility.mlua` · `Lane/LaneFacilityService.mlua` · `Lane/DefenderService.mlua` · `Faction/FactionAttack.mlua` · `Faction/FactionAuraController.mlua` · `Faction/TurretAI.mlua`(연출 훅 3줄 · 2026-09-14) · `Stat/StatService.mlua`(오라 이속 배수 메서드 1개) · 이 계약서 · 정합성 검사 · M1 GDD · `추가기획2/미정-값.md` — **전부 A 소유. B 협의 항목 없음.** 신규 `Lane/LaneAttackFx.mlua` · `Lane/LaneShot.mlua` |

> 잠정값(대플레이어 10 · 알림 5초/주인만 · 안내 3초 · 수비대 이속 ×2 · 반사 상한 무제한 · 억제기 사거리/쿨 = 포탑 차용 · 오라 범위 = 컨트롤러 기본)은 전부 `property` 또는 CSV 칸이다. `추가기획2/미정-값.md` E·F 절이 목록.

#### 등록서 — 계정 영구 저장 (DataStorage)

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | **`Progression/`** — A. 이 프로젝트 최초의 `_DataStorageService` 사용처다 |
| 2 | 신규 CSV | **없음** |
| 3 | 새 이벤트 | **발행 없음.** 구독: `UserEnterEvent`(로드) · `UserLeaveEvent`(마지막 저장) — 둘 다 엔진 이벤트. 🔴 `UserLeaveEvent` 는 이 프로젝트에서 **처음 구독**한다 |
| 4 | 새 열거값 | **없음** |
| 5 | ID 대역·접두사 | 저장 필드는 §0-5 규약대로 **`account_*`** 접두사 |
| 6 | 저장 필드 | 상태 객체 **`AccountProfile`**(영구) — `SchemaVersion`(최초 `1`) · `account_level` · `account_balrogHeart` · `account_unlocks`.<br>**저장 레이아웃**: `GetUserDataStorage(ProfileCode)` 안에서 키 = 필드명 (per-user container).<br>**키 2개**: `AccountProfile`(작고 자주 바뀜) · `MatchHistory`(계속 자람 — **키만 예약**, 구현은 후속).<br>🔴 키를 나눈 이유가 §0-5 의 "통짜 덮어쓰기 금지"다 — **도메인이 키를 공유하지 않으면 남의 필드를 지울 수가 없다.**<br>🔴 매치 자원(`meso` `coin` `exp` `level` 장비 인벤 시설 스킬)은 **하나도 저장하지 않는다** (GDD §3) |
| 7 | 새 모델/UI 파일 | **없음.** `PlayerDBManager` 를 `@Logic` 으로 두어 `Global/DefaultPlayer.model` 부착을 피했다 |
| 8 | 🔴 **건드리는 기존 파일** | `Docs/스키마-계약.md`(이 등록서) · `Match/MatchSessionLogic.mlua`(`Expire` 끝에 flush **1줄**) · `Lane/LaneStateService.mlua`(`Eliminate` 끝에 flush **1줄**) — **전부 A 소유. B 협의 항목 없음** |

**구성** — `PlayerDBManager`(`@Logic`, 저장 단일 창구) · `AccountData`(`@Logic`, 원장 + 5메서드 계약) · `AccountProfile`(`@Struct`, 직렬화 미러) · `AccountStorageLogic`(`@Logic`, 키 상수).

> 🔴 **`_DataStorageService` 직접 호출은 `PlayerDBManager` 안에서만.** 저장 경로가 갈리면 스키마가 조용히 어긋나고, Batch 로 모으는 이점이 사라져 Credit 이 선형으로 는다.
>
> **비용**: 접속 시 `BatchGet` 1회 · **300초 주기** `BatchSetAsync` · 이탈 시 `BatchSetAndWait`. 안 바뀐 유저는 요청 자체를 보내지 않는다. 5인 30분 매치 기준 약 **40 credit** (충전은 분당 `100+동접×10`).
>
> ⚠️ 이 프로젝트 **최초의 `@Struct`** 다. Maker `Reimport All` 로 로드 확인이 필요하다.

#### 등록서 — 플레이어 스탯 · 장비 · 강화 · 기능 NPC · UI

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | `Stat/`(스탯·데미지) · `Item/`(인벤토리·장비·강화) · `Npc/`(기능 NPC), 전부 A |
| 2 | 신규 CSV | `ItemInfo` · `EnhanceTable` · `GemInfo` · `GemDropTable` · `VillageNpcSector` · `FunctionalNpcCatalog` · `EnhanceSlotBonus`(2026-09-05 추가 · ⑫) — 헤더·기본 키는 A-2-8 이하가 정본 |
| 3 | 이벤트 | 발행 `StatRecalculatedEvent` · `EquipChangedEvent` · `ApAllocatedEvent` · `SpSpentEvent`; 구독 `MatchLevelChangedEvent` · `JobChangedEvent` · `ItemEnhancedEvent` · `GemSpentEvent` · `NpcOpenedEvent` |
| 4 | 열거값 | `StatId` 13종 · `EquipSlot` **7종**(2026-09-08 `SUBWEAPON` 추가) · `ItemType` 4종 · `SectorId` 4종 |
| 5 | ID | `ItemId` · `StatId` · `SectorId`는 대문자 열거값 문자열. `ItemInstanceId`는 서버가 발급하는 불투명 문자열 |
| 6 | 상태 | `MatchPlayerState`에 `match_stats` · `match_inventory` · `match_equipped` · `match_ap` · `match_sp` |
| 7 | 신규 UI | `CharacterGroup` · `VillageWorkshopGroup` · `VillageLifeGroup` · `VillageRecordGroup` · `VillageDefenseGroup` · `CommonNpcGroup` |
| 8 | 기존 파일 | `PlayerAttack.mlua`(B 승인) · `StatusHUD.ui` · `ToastGroup.ui` · `LevelTable.csv` · 이 계약서 · 협업 규칙 · M1 GDD · 정합성 검사 |

#### 등록서 — 매치 시계 5단 · 드랍 소유권 · 물약 쿨타임 (2026-09-08 · WO-014 · 추가기획2)

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | 매치 시계·만료 = `Match/` · 드랍 소유권 = `Farm/` · 물약 쿨 = `Item/`. 전부 A |
| 2 | 신규 CSV | **`MatchConfig`** — 헤더·기본 키는 A-2-5a 가 정본 (기본 키 `Key`+`Profile`). 그 외는 기존 표 열 추가뿐: `ConsumeInfo` +3열 |
| 3 | 이벤트 | 신규 없음. 기존 `MatchPhaseChangedEvent` 의 `Phase` payload 에 **`"EXPIRED"`** 값이 하나 늘어난다(append-only) |
| 4 | 열거값 | `MatchPhase` **5값으로 교체** (§0-2) — 구 4값은 이 저장소에서 전부 사라진다. **하이픈 예외 1건** |
| 5 | ID | 신규 숫자 ID 없음. `MatchConfig.Key` 는 대문자 열거값 문자열(`DEFAULT`) |
| 6 | 상태 | 유저 econ 에 **`autoPickup`**(boolean · 기본 `true` · 세션 메모리) 1개. `InventoryService` 에 `potionReadyAt`(서버 메모리) |
| 7 | 신규 `.model` / UI | 신규 없음. `CharacterGroup` 스탯 탭에 **토글 버튼 노드 1개 추가**(기존 파일 · A 소유) |
| 8 | 🔴 건드리는 기존 파일 | `MinionPhaseConfig.csv` · `MinionComposition.csv` · `ConsumeInfo.csv` · `Match/MatchSessionLogic.mlua` · `Match/MatchClockUIController.mlua` · `Lane/LaneFacility.mlua` · `Farm/FarmReward.mlua` · `Farm/MesoCoin.mlua` · `Summon/SummonManager.mlua` · `Stat/StatUIController.mlua` · `Item/ItemCatalog.mlua` · `Item/InventoryService.mlua` · `CharacterGroup` UIGroup · 이 계약서 · M1 GDD · 정합성 검사 — **전부 A 소유. B 경계면 없음** |

> ⚠️ `MatchPhase` 열거값 교체는 **`Skill/` `Job/` 이 이 값을 구독하고 있다면** 영향이 있다. 2026-09-08 기준 B 폴더에서 페이즈 문자열 사용처는 없다(전체 검색 확인). 앞으로 구독한다면 새 5값을 쓴다.
#### 등록서 — 파병 (2026-09-09)

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | 파병 — **`Dispatch/`** (A) |
| 2 | 신규 CSV | **`DispatchRule`** — 헤더·기본 키는 **A-2-16** 이 정본 |
| 3 | 이벤트 | **신규 없음.** B-3 `Dispatch/` 3행(`DispatchSubmittedEvent` · `DispatchCancelledEvent` · `DispatchJoinedEvent`)은 **예약 유지** — 구독자가 A 자신뿐이라 이번 구현은 직접 호출로 한다. 통계·B 쪽 구독이 생기면 그때 §6-3 3종 세트로 정의한다 |
| 4 | 열거값 | 신규 없음 (`SinkType.DISPATCH` 는 §0-2 에 이미 있다) |
| 5 | ID | `OrderId` = **서버가 발급하는 불투명 문자열** (`ItemInstanceId` 와 같은 성격) |
| 6 | 상태 | 신규 저장 필드 없음. 대기열은 `DispatchService` 서버 메모리 — GDD §4.10 `MinionFlowState` 의 "접수된 파병"에 해당 |
| 7 | 신규 UI | 없음. 기존 `CommonNpcGroup.ui` 의 `dispatch` 창에 대기열·취소 노드만 추가 |
| 8 | 기존 파일 | `Lane/LaneStateService.mlua` · `Lane/MinionFlowService.mlua` · `Lane/DefenderService.mlua` · `Match/MatchSessionLogic.mlua` · `Npc/CommonNpcUIController.mlua` · `ui/CommonNpcGroup.ui` · 이 계약서 · 정합성 검사 — **전부 A 소유. B 협의 항목 없음** |

> NPC 스폰을 실제로 켜기 전, 기존 `NpcInfo.ModelId=staticnpc`가 Maker에서 유효한 네이티브 모델인지 확인한다. 유효하지 않으면 스포너 PR에서 검증된 공용 NPC 모델을 추가 등록한다. 모델 누락을 경고로 숨긴 채 스포너를 연결하지 않는다.

#### 등록서 — 스킬 시전 · 스킬 UI · 전직 (B · 2026-09-09 · `b/skill-register`)

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | 스킬 시전·스킬 트리·핫바·스킬 UI → `Skill/` · 전직 → `Job/`, 전부 B. 스크립트: `Skill/SkillCaster` `SkillExecutors` `SkillAttack`(Component) `SkillProjectile`(Component) `SkillDatabase` `SkillHotbar` `PlayerSkillState` `SkillWindowLogic` · `Job/JobDatabase` |
| 2 | 신규 CSV | **`SkillInfo`**(키 `SkillId`) · **`JobInfo`**(키 `JobId` — A 의 `StatService.LoadJobs` 가 읽는 주·부스탯·AP 비율) · **`JobTier`**(키 `JobId`+`Tier` — 전직 레벨·표시명·기본 공격력). 헤더·열 정의는 **A-2-16** 이 정본. 전부 `RootDesk/MyDesk/` 루트, `.userdataset` 쌍 |
| 3 | 이벤트 | 발행 `JobChangedEvent(UserId, PrevJobId, NewJobId, Tier)` · `SkillUsedEvent(UserId, SkillId, MapName)`; 구독 `SpSpentEvent`(A). 클라 내부용 `SkillStateChangedEvent(What)` 은 B 의 UI 갱신 신호이며 **타 도메인 구독 대상이 아니다**. 명세는 B-3 `Skill/` `Job/` |
| 4 | 열거값 | `JobId` 6종(§0-2 확정 제안) · `JobTier` 0~3 · `SkillBehavior` 9종(행 7 + 실행기 전용 2) · `EffectUnit` 6종 · `BuffTag` 13종 |
| 5 | ID | `SkillId` = 대문자 문자열 `SK_{직업 머리글자 W/M/A/T/P}{차수}{번호}` (예 `SK_W11` 파워 스트라이크) · `JobId` = 열거값 · `WeaponMotion.MotionId` 행은 `MOTION_{스킬}_{무기}` |
| 6 | 상태 | **없음.** M1 은 스킬 레벨·쿨다운·직업을 서버 메모리(`userId` 키)에만 둔다. 영구 저장은 M2 에서 `match_skills` / `account_job` 접두사로 등록 예정 |
| 7 | 신규 `.model` / `.ui` | `RootDesk/MyDesk/Models/Skills/SkillProjectile.model`(`model://skillprojectile`) · `ui/SkillWindow.ui`(`GroupOrder 5`) |
| 8 | 🔴 기존 파일 | `Summon/SummonManager.mlua` — `SpendMp(userId, amount) → boolean` · `GrantMp` · MP 회복 틱 · `StatRecalculatedEvent` maxMp 다리 네 항목을 **B 가 `b/summon-mp-ledger`(PR #41) 에서 한 PR 로**(A 가 #40 comment 5588167790 에서 "A 가 직접"을 번복 · A 승인 필요), `Skill/` 은 `_SummonManager:SpendMp` 호출만 · `Stat/StatUIController.mlua` — `OnClickSkill()` 스텁 1줄을 `_SkillWindowLogic:Toggle()` 로(A 가 예약해 둔 훅) · `WeaponMotion.csv` — 스킬×무기 행 추가만 · `PlayerHit.mlua` — `IsHitTarget` 피격 무적 시간에 `_SkillBuffs:GetHitImmuneMul` 배율 · `OnHit` 오버라이드로 불굴의 진(SK_W31 · HP 최대치 고정 · 경직·넉백 없음)과 전 직업 궁 시전 중 무적일 때 기본 피격 처리를 건너뜀 · `OwnerUserId()` 헬퍼 추가. 시그니처·`@ExecSpace` 는 부모 그대로 둔다(바꾸면 LEA-3014 SignatureMismatch) — **A 승인(#40 comment 5676245836)** · 이 계약서 · `Docs/tools/check-integrity.cjs`(CANONICAL·PK) |

> ✅ **A 답변(2026-09-09)**: ① `JobId` 6종 확정(§0-2) ② `JobChangedEvent.Tier` append 승인 — integer · 값 고정(§0-2 `JobTier`) ③ **MP 회복은 `SummonManager` 에 넣는다** — 3초마다 `max(1, floor(maxMp × 0.03))` 회복(`maxMp` clamp · `mp < maxMp` 일 때만 돌고 그때만 Push) + `GrantMp(userId, amount)`(`GrantSp` 와 같은 꼴 · 포션·보상·리모컨용) + `SpendMp`(이벤트 없음). 같은 파일 같은 구간이라 한 번에. ④ **maxMp 원장은 `SummonManager.econ.maxMp` 가 유일한 진상** — `StatService final.maxmp`(장비·버프분)는 `StatRecalculatedEvent` 구독으로 econ 에 흘려보내는 다리를 놓는다(`StatService.mlua` 수정 없음). 결과 `econ.maxMp = BaseMaxMp + Σ LevelTable.AddMaxMp + 장비·버프 maxmp`. B 는 `GetEcon(userId).maxMp` / `SpendMp` 만 본다. **담당 번복(A · #40 comment 5588167790)**: ③④ 네 항목 전부 B 가 `b/summon-mp-ledger`(PR #41) 에서 구현, A 승인 필요.
> 🔴 **`JobInfo` 에 `JobId="NOVICE"` 행 필수** — `StatService.mlua:99` 가 신규 유저를 `job="NOVICE"` 로 만든다. 행이 없으면 `GetJob` 이 하드코딩 폴백(초보자 · 주 STR · 부 DEX · AP 6:4)을 돌려줘 안 터지는 대신 표와 **조용히 갈라진다.**

---

# A부. 데이터 표

## A-1. 현존 표 8종

### A-1-1. 🔴 정본화 대상 2종 — 두 월드가 갈라져 있다

병합 전 상태: 밍키타 `MonsterInfo` 9열(`…ModelId,MoveSpeed,JumpForce`) vs 안농 10열(`…Level,MaxHP,Exp,…,MoveType,AiType`). `MaxHP` 가 4번째에 삽입돼 뒤 열이 전부 밀렸고 `#Note` 위치도 다르다. `MonsterCatalog.mlua` 도 읽는 열이 서로 다르다.

**정본 (합집합. 안농 열 순서 + 밍키타 2열을 뒤에):**

```
MonsterInfo.csv
Id,Name,Level,MaxHp,Exp,CoinMin,CoinMax,ModelId,MoveType,AiType,MoveSpeed,JumpForce,IconRUID
```

✅ **2026-09-22 등록(`a/plan-0914` B2 `a/p0914-2-balance-data` · 헤더+CANONICAL 동시 변경 · #40 comment 5776547020 공지):** 맨 뒤에 **`Attack`** 추가 → `…,JumpForce,IconRUID,Attack`. 일반몹 한 방(방어 감산 전) = **동레벨 플레이어 HP 의 15%** = `round(0.15 × (1000 + 50 × (Level − 1)))`(Lv1 150 · Lv10 217 · Lv17 270 · Lv24 322 · 79행 전부 계산값). 지금은 일반몹 피해 계산 경로 자체가 없다(`MonsterAttack` 은 엔진 기본값) — **B4 가 `MonsterAttack.CalcDamage` 를 만들며 이 열을 읽는다**(그 전까지는 데이터만). 🔴 **`CoinMin`/`CoinMax` 의 뜻이 바뀌었다: 동전 개수 → 메소 총액**(Lv≤2 3 · Lv3 5 · Lv4~6 7 · Lv7~9 12 · Lv10~16 **60** · Lv17~23 **180** · Lv24+ **450**). `FarmReward.DropCoins` 가 총액을 최대 3개 동전(`MesoCoin.Value`)에 나눠 떨군다. `Exp` 는 평타 간격 실측 뒤 같은 브랜치의 다음 커밋에서.

> 📝 **2026-09-06 `Level` 값 기준 확정**: 원작 레벨이 아니라 **추가기획1 §3 "지역별 사냥터 3단계"의 게임 Lv**(초급 10 · 중급 17 · 상급 24 · 리스항구 1/4/7). 여러 티어에 나오는 몬스터는 낮은 티어 기준. 스폰 관리(`MapMonsters`) 22종 19행 재조정 · `MaxHp/Exp` 는 원본 유지(m1-balance). 도감(`VillageRecordGroup`)은 `MapMonsters` 에 있는 종만 보여준다.

| 열 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `Id` | string(정수 문자열) | — | **기본 키.** §0-4 |
| `Name` / `Level` | string / integer | `""` / `0` | 표시용 |
| `MaxHp` | integer | `1` | 밍키타 출신 행은 빈칸 → 기본값 |
| `Exp` · `CoinMin` · `CoinMax` | integer | `0` | 처치 보상 |
| `ModelId` | string | `""` | 스폰할 `.model` id |
| `MoveType` · `AiType` | enum(소문자) | `walk` / `wander` | §0-2 |
| `MoveSpeed` · `JumpForce` | number | `-1` | `-1` = 모델 기본값. 안농 출신 행은 빈칸 |
| `Attack` (✅ B2) | integer | `0` | 한 방 피해(감산 전 · 동레벨 플레이어 HP 15%). `0`/빈칸 = 안 때림(B4 연결 전엔 어차피 안 읽힘) |

```
MapMonsters.csv
MapName,MonsterId,X,Y,SpawnerId,SpawnId,SpawnCount,RespawnSeconds,Enabled,LevelOverride,#Note
```

`MapName`+`SpawnId` 복합키(정합성 검사 C3 기준). `X`/`Y` world unit. `SpawnerId` = `<MapName>_SP001`, `SpawnId` = `<SpawnerId>_M<MonsterId>`. `SpawnCount` 만큼 독립 슬롯, 각 슬롯이 `RespawnSeconds` 뒤 한 마리 재생성.

| 열 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `LevelOverride` | integer | 빈칸 | 🔴 **이 자리에서만 쓰는 게임 레벨.** 빈칸이면 `MonsterInfo.Level` 을 그대로 쓴다 (2026-09-08 신설) |

> **왜 필요한가** — 같은 몹이 두 티어에 걸치기 때문이다. 리본 돼지는 리스항구 상급(Lv7)·노틸러스 중급(Lv17)·상급(Lv24)에,
> 파란 달팽이는 리스항구 초급(Lv1)·중급(Lv4)에 나온다. 도감은 종당 한 줄이라 레벨을 하나만 가질 수 있다.
> `MonsterCatalog` 는 이 값이 있으면 그 자리의 레벨로 보고, **`MaxHp` 도 레벨 기준값 비율로 환산**한다
> (`hp × base(LevelOverride) ÷ base(Level)`). 기준값 표는 `MonsterCatalog.LevelBaseHp` 가 정본이다.

> 🔴 정본화 시 **`MonsterCatalog.mlua` 도 한 벌로 통일**하고 신규 열 전부에 §0-1(5) 폴백을 적용한다.

### A-1-2. 나머지 6종 (그대로 유지)

```
NpcInfo.csv        Id,Name,ModelId
MapNpcs.csv        MapName,NpcId,X,Y,#Note
LevelTable.csv     Level,NeedExp,AddMaxHp,AddMaxMp,AP,SP,#Note
SummonUnits.csv    Key,Name,Level,Meso,ModelId,#Note
WorldMapNodes.csv  MapName,Label,Region,Type,NodeEntity,X,Y,ShowLabel,LabelDX,LabelDY,Enabled,#Note
PortalRoutes.csv   RouteId,FromMap,FromPortal,ToMap,ToPortal,Enabled,#Note
```

| 표 | 비고 |
|---|---|
| **`LevelTable`** | 🔴 **GDD의 레벨 곡선 역할을 이 표가 겸한다.** 새 표를 만들지 않는다 — `MonsterCatalog.mlua` 가 이미 `LevelTableName` 으로 읽고 있다. **10(전직) / 16 / 21(침범·발록)** 이 페이즈 기대 지점 |
| **`WorldMapNodes`** | ⚠️ GDD에 없던 표. **이 문서가 유일한 기록** |
| **`PortalRoutes`** | ⚠️ GDD에 없던 표. 포탈 **물리 배선**. `MatchRouteConfig`(이동 허용 목록)와는 층이 다른 별개 표 — 키 이름을 `RouteId` / `MatchRouteId` 로 갈라 혼동을 막는다 |

---

## A-2. MVP(Phase 1)에 필요한 신규 표 — 헤더 확정

> 좌표가 들어가는 표는 **MapBuilder 실측 후 값을 채운다.** 헤더는 지금 확정한다.

### A-2-1. `VillageConfig` — 마을·레인 정의

> ✅ **2026-09-06 등록됨** — `RootDesk/MyDesk/VillageConfig.csv` 6행(GDD §4.1 레인표 · `LITH` 는 `Enabled=false` 공용 시작 지역) + `.userdataset` + `check-integrity` CANONICAL/KEY. `CoreX/CoreY` 는 디자이너 실측 대기(빈칸). 읽는 곳: `Npc/NpcCatalog`(마을 맵 ↔ `VillageId`).

```
VillageId,VillageMapName,Lane2MapName,Lane1MapName,RearMapName,BossMapName,ThemeId,CoreX,CoreY,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `VillageId` | **키.** 6종 |
| `VillageMapName` | 마을 맵 루트 엔티티 이름 (실제 이름) |
| `Lane2MapName` | 사냥터2 — **1차포탑**이 있는 맵 |
| `Lane1MapName` | 사냥터1 — **2차포탑·억제기**가 있는 맵 |
| `RearMapName` | 후방 지선 (미니언 안 옴) |
| `BossMapName` | 보스맵 |
| `CoreX` / `CoreY` | 코어 앵커 — 여섯갈래길 방향 포탈 옆 (실측) |

**6행 고정.** GDD §4.1 매핑표가 값의 출처다.
⚠️ **커닝시티만 레인이 4단**(`SubwayLine1 → Village_SubwayEntrance → ConstructionSite → 마을`). `Village_SubwayEntrance` 는 포탑 없이 통과만 시키고, 길이 차이는 `LaneConfig` 의 미니언 이동 속도로 보정한다.

### A-2-2. `LaneConfig` — 레인 지형·슬롯

```
VillageId,MapName,LaneRole,PathMinX,PathMaxX,PathY,SpawnX,SpawnY,EndTriggerX,TowerSlotX,TowerSlotY,RopeX,MinionSpeedMul,DefenderSlotCount,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `VillageId` + `MapName` | **복합키.** 마을당 3행(사냥터2 · 사냥터1 · 마을) |
| `LaneRole` | `LANE2` / `LANE1` / `VILLAGE` / `PASSTHROUGH`(커닝시티 지하철역) |
| `PathMinX` · `PathMaxX` · `PathY` | **하단 1자 길**의 x 범위와 y (실측) |
| `SpawnX` / `SpawnY` | 미니언 진입 지점. **빈칸이면 오른쪽 끝(`PathMaxX − 0.6`)에서 들어와 왼쪽으로 간다(우→좌).** 좌→우 구간(엘리니아·노틸러스)은 **`LANE1`·`VILLAGE` 행에도 값을 넣는다**. 진행 방향 = `EndTriggerX` 가 출발 x 보다 오른쪽이면 좌→우 — 수비대 대기 위치(시설 뒤 = 마을 쪽)도 이 방향을 따른다 (2026-09-18) |
| `EndTriggerX` | 레인 끝 트리거 — 미니언이 닿으면 전선이 다음 맵으로 |
| `TowerSlotX` / `TowerSlotY` | 포탑 슬롯. `LANE1` 은 2차포탑·억제기 2행으로 나눠 쓴다 |
| `RopeX` | 위 발판 ↔ 아래 길을 잇는 밧줄 위치 |
| `MinionSpeedMul` | 레인 길이 차이 보정 |
| `DefenderSlotCount` | 수비 몬스터 배치 슬롯 수 — ⚠️ 추가기획1 로 배치 UI 폐기. 열은 남기되 코드는 읽지 않는다(상한 10묶음은 `LaneStateService.cap`) |

✅ **2026-09-07 등록(WO-012 ①)**: 헤네시스 3행 실측(`LANE2` GolemsTemple y −7.0 · `LANE1` HillNorth y −9.0 · `VILLAGE` MinimiMain y −6.5). 통로 발판·포탈(`P_Lane_To_*`)·밧줄은 `.map` 에 커스텀 발판으로 깔았다(빌더 커버리지 밖 = 스크립트 `lane_corridor_henesys.cjs`).

> 🔴 **난이도 편차는 지형이 아니라 이 표로 보정한다.**
> ⚠️ **하단 1자 길이 없는 맵은 발판을 새로 깔아야 한다.** 발판 저작은 빌더 커버리지 밖 = **Maker 수작업**. 마을당 3맵 × 6 = **18개 맵**이 이 마일스톤 최대 물리 작업이다.

### A-2-3. `VillageCoreConfig` — 코어

```
VillageId,MaxHp,RequiredLevel,ExposeCondition,Enabled,#Note
```

`RequiredLevel` 기본 **10**(전직 완료 = 연결 자격). `ExposeCondition` 기본 `SUPPRESSOR_DESTROYED`.

### A-2-4. `TowerConfig` — 포탑 3종 + 코어

```
Stage,Level,MaxHp,Attack,AttackSpeed,Range,UpgradeCost,RepairCostPerHp,RebuildCost,SuppressorMinionHpMul,SuppressorMinionAtkMul,SuppressorExpMul,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Stage` + `Level` | **복합키.** `Stage` = `TOWER`/`SUPPRESSOR`/`CORE` **3값**(추가기획1 · 2026-09-07 등록: 포탑 1종으로 축소), `Level` = 1~3 (코어는 1) |
| `MaxHp` · `Attack` · `AttackSpeed` · `Range` | 코어는 `MaxHp` 만 의미 있다 |
| `UpgradeCost` · `RepairCostPerHp` · `RebuildCost` | **빅토리아 주화**(2026-09-07 확정 — 메소·엘리트 재료 아님). `RepairCostPerHp` 는 소수(잃은 HP × 계수 → 올림). 코어 `RebuildCost=0` = 재건 불가 |
| `Suppressor*Mul` | 🔴 **억제기의 역설** — 억제기 파괴 시 미니언 HP·공격력 배율과 **경험치 상승률**. `SUPPRESSOR` 행에만 값이 있다 |

⚠️ **1차포탑은 3페이즈쯤 거의 반드시 부서지도록** 수치를 잡는다. 안 부서지면 전선이 안 밀려 판이 끝나지 않는다.

### A-2-4b. `FacilitySprite` — 시설 3종 그림 (마을별)

> ✅ **2026-09-08 등록됨** — `RootDesk/MyDesk/FacilitySprite.csv` 15행(마을 5 × `Stage` 3 · `LITH` 는 점유 불가라 없다) +
> `.userdataset` + `check-integrity` CANONICAL/KEY.
> 배경: 시설 그림이 `.model` 3개와 `.ui` 2개, `.mlua` 1곳에 **각각 박제**돼 있어 마을별로 다르게 할 수 없었다.
> 사용자 결정(2026-09-08): **마을마다 시설 아트가 다르다** → 그림을 표로 빼고 런타임에 꽂는다.

```
VillageId,Stage,WorldRuid,IconRuid,FlipY,GroundOffset,BarOffset,Enabled,FlipX,Scale,#Note
```

| 열 | 설명 |
|---|---|
| `VillageId` + `Stage` | **복합키.** `VillageId` = `VillageConfig` 의 5종(`LITH` 제외) · `Stage` = `TOWER`/`SUPPRESSOR`/`CORE` (`TowerConfig` 와 같은 열거값) |
| `WorldRuid` | 월드 엔티티 `SpriteRendererComponent.SpriteRUID`. 비면 `.model` 기본값 유지 |
| `IconRuid` | UI `SpriteGUIRendererComponent.ImageRUID`. 🔴 **월드와 갈라놓은 이유** — 임시 넥서스 `8adca861` 은 UI 에 쓰면 `[LEA-3044]` 라 아이콘만 `dab6ddee` 를 쓴다. 전용 아트는 보통 두 칸이 같다 |
| `FlipY` | 월드·UI 공통 상하반전. 임시 억제기 스프라이트가 뒤집혀 있어 `true` |
| `GroundOffset` | 발판 y 에서 얼마나 **올려서** 스폰하는가 = 피벗 → 스프라이트 바닥 거리. 🔴 **계산이 아니라 실측이다** — 업로드 자산의 픽셀 크기는 리소스 API·런타임 API 어디서도 못 읽는다. 그림이 바뀌거나 `Scale` 이 바뀌면 반드시 다시 잰다 |
| `BarOffset` | 엔티티 피벗 → HP바 y. 피벗 기준이라 **스프라이트 높이가 다르면 같은 값이라도 다른 위치**에 붙는다 |
| `Enabled` | `false` 면 그 행을 무시하고 스크립트 기본값 |
| `FlipX` | 월드 좌우반전 (2026-09-09 추가 · `#Note` 앞에 삽입). `.model` 의 `FlipX` 기본값을 **덮는다**. 포탑은 `TurretAI.FixedFacing=true` 라 표적을 따라 돌지 않으므로 이 값이 최종 방향이다 |
| `Scale` | 월드 엔티티 배율 (2026-09-14 추가 · `#Note` 앞에 삽입). 빈 칸이면 `.model` 의 `(0.25,0.25,1)`(헤네시스 전용 아트 기준) 유지. 🔴 **임시 그림 행에만 값을 둔다** — 임시 스프라이트(180×320 등)는 0.25 로는 0.4~0.8 유닛(손톱)이라 헤네시스 아트 높이 **2.25** 에 맞춘 값. `GroundOffset`·`BarOffset` 은 이 배율 기준이므로 `Scale` 을 바꾸면 둘 다 다시 잰다 |

읽는 곳: `Lane/LaneStateService`(서버 · 로드 + `FacilityWorldRuid`/`FacilityIconRuid`/`FacilityFlipY`/
`FacilityGroundOffset`/`FacilityBarOffset`/`FacilityFlipX`/`FacilityScale`) → `Lane/LaneFacilityService`(스폰 시 월드 스프라이트 · 배율 · 스폰 y · HP바 y) ·
방어/모집 뷰 행에 아이콘을 실어 보내 `Npc/VillageDefenseUIController` · `Npc/VillageLifeUIController` 가 칠한다.

> 새 마을 아트가 나오면 **이 표의 행만 고친다.** 코드·`.model`·`.ui` 는 건드리지 않는다.

### A-2-5. `MinionPhaseConfig` — 페이즈 시계

```
Phase,StartSeconds,SpawnIntervalSeconds,HpMul,AtkMul,ExpBase,MesoBase,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Phase` | **키.** `MatchPhase` **5종** (2026-09-08 개편) |
| `StartSeconds` | 🔴 **매치 경과 시간** 기준 전환 시각. **레벨 기준이 아니다** — 개인 레벨로 갈리면 "일부러 안 크는 버티기"가 성립한다 |
| `SpawnIntervalSeconds` | 미니언 투입 간격 |
| `HpMul` · `AtkMul` | GDD 4페이즈 곡선 — 1(없음) / 2(HP↑ 공격↓) / 2.5(공격 급상승) / 3(둘 다 급상승) |
| `ExpBase` · `MesoBase` | 미니언 처치 보상 기준값 |

**5행 고정** (2026-09-08 추가기획2 · 구 4행). `PHASE0-1` `PHASE0-2` 는 미니언이 없으므로 `SpawnIntervalSeconds` 를 `0` 으로 둔다.

✅ **2026-09-07 등록(WO-012 ②)** — 열 2개를 **뒤에 추가**(중간 삽입 없음): `Profile`(`TEST`/`LIVE` · 복합키 `Phase`+`Profile`) · `CoinDropChance`(미니언 처치 시 빅토리아 주화 확률 드랍). 실제 헤더: `Phase,StartSeconds,SpawnIntervalSeconds,HpMul,AtkMul,ExpBase,MesoBase,Enabled,Profile,CoinDropChance,#Note`. 리모컨이 Profile 을 토글한다.

✅ **2026-09-08 개편(WO-014 · 추가기획2)** — **5단 · 헤더 변경 없음 · Phase 값과 StartSeconds 만 교체.** 5행 × 2프로필 = **10행**.

| Phase | 뜻 | LIVE Start | LIVE Interval | TEST(÷8) Start | TEST Interval |
|---|---|---:|---:|---:|---:|
| `PHASE0-1` | 0페이즈 · 리스항구 개척 (미니언 없음) | 0 | 0 | 0 | 0 |
| `PHASE0-2` | 0.5페이즈 · **전직 유예 30초** (미니언 없음) | 240 | 0 | 30 | 0 |
| `PHASE1` | 1페이즈 · 집중 성장 · 미니언 등장 | 270 | 120 | 34 | 15 |
| `PHASE2` | 2페이즈 · 견제 구간 · 미니언 공격력 상승 | 720 | 120 | 90 | 15 |
| `PHASE3` | 3페이즈 · 최종 · **시설 풀데미지 개방** | 1200 | 120 | 150 | 15 |

- **"미니언 2분 간격 4회"는 코드로 세지 않는다.** 1페이즈 450초 · 2페이즈 480초 ÷ 120초 = 각각 정확히 4웨이브가 나온다. 3페이즈(600초)는 5웨이브.
- 🔴 **TEST 행 = LIVE 의 1/8**(`PHASE1` 33.75 → 34 반올림). 릴리즈 직전 `MatchSessionLogic.Profile` 기본값을 `TEST` → `LIVE` 로 되돌린다.
- 🔴 **페이즈 판정은 인덱스가 아니라 `Phase` 값(이름)으로 한다.** 행이 4 → 5 로 늘면서 인덱스가 한 칸 밀렸다 (`LaneFacility` 의 "3페이즈 전 시설 저항 5%" 게이트가 조용히 1페이즈부터 풀데미지가 되는 회귀). 앞으로 행을 더 늘려도 안 깨지도록 이름 비교를 정본으로 한다.

⚠️ 매치 종료 시각은 이제 이 곡선이 아니라 **`MatchConfig.MatchDurationSeconds`(A-2-5a)** 가 정한다.

🔄 **2026-09-22 개편 예정(`a/plan-0914` B3 · WO-027 결정 · 헤더 변경 없음 · 값만):** 페이즈 경계 = **1P 4:30 · 2P 16:30 · 3P 24:30** → LIVE `StartSeconds` `PHASE1` 270 · `PHASE2` **990** · `PHASE3` **1470**(TEST ÷8 = 34 · 124 · 184). **미니언은 이 표의 `SpawnIntervalSeconds` · `HpMul` · `AtkMul` · `ExpBase` · `MesoBase` 를 더 읽지 않는다**(열·값은 남긴다 · 삭제 금지) — 웨이브 시각·마릿수·스탯은 **A-2-6a `MinionWave`** 가 정본. `CoinDropChance` 는 `0`(미니언은 빅토리아 주화를 주지 않는다 · 주화 출처 = 엘리트뿐). 페이즈 배율 열은 억제기 파괴 배율(`TowerConfig.SuppressorMinion*Mul`)과 별개로 계속 존재만 한다.

### A-2-5a. `MatchConfig` — 매치 전역 설정 (2026-09-08 신규 · WO-014)

```
Key,Profile,MatchDurationSeconds,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Key` | 설정 묶음 이름. M1 은 `DEFAULT` 한 종류 |
| `Profile` | `MinionPhaseConfig` 와 같은 `TEST`/`LIVE`. **복합키 `Key`+`Profile`** |
| `MatchDurationSeconds` | 🔴 **매치 만료 시각.** LIVE `1800`(30분) · TEST `225`(÷8). 만료되면 서버가 미니언 투입을 멈추고 전체 토스트를 띄운다 |

**2행** (`DEFAULT`×`LIVE`/`TEST`). 앞으로 난이도·목표 레벨 같은 매치 전역 값이 늘면 **열을 뒤에 추가**한다.
> 순위 판정·결과 화면·보상은 이 표가 아니라 후속 `RankReward` / `Progression` 몫이다. WO-014 는 **만료 시각만** 만든다.

### A-2-6. `MinionComposition` — 미니언 구성

```
Phase,MonsterId,Weight,Level,Enabled,BaseHp,BaseAttack,VillageId,#Note
```

한 페이즈에 여러 종이면 **여러 행**. `Weight` 로 투입 비율을 정한다. `MonsterId` 는 `MonsterInfo.Id` 참조 (안농 도감 77종에서 고름).

✅ **2026-09-07 등록(WO-012 ②)** — 열 2개를 **뒤에 추가**: `BaseHp` · `BaseAttack`(미니언 기준 HP·공격력. `MonsterInfo.MaxHp` 는 사냥터 몹 기준이라 레인용 값을 따로 둔다. 최종 = 기준 × 페이즈 배율 × 억제기 파괴 배율). 실제 헤더: `Phase,MonsterId,Weight,Level,Enabled,BaseHp,BaseAttack,#Note`. 임시 구성: 좀비버섯 `2230101`(2~3페이즈) · 좀비머쉬맘 `2400572`(3페이즈 · `MonsterInfo` 행 추가) — 파괴된 헤네시스·페리온 몬스터 모델이 오면 교체.

✅ **2026-09-18 열 추가(A · `a/lane-ellinia-nautilus` · #40 공지 5725998438)** — `VillageId` 를 `#Note` 앞에 추가. **빈칸 = 전 마을**, 값이 있으면 그 마을 웨이브만 그 행들에서 뽑는다 — 그 마을·페이즈 행이 하나라도 있으면 빈칸 행은 그 마을에 안 쓴다. 기본 키 `Phase+MonsterId+VillageId`. 첫 사용: 노틸러스 전용 물버섯 `12230101`(좀비버섯 복제 + 하늘색 틴트).

🔄 **2026-09-22 (`a/plan-0914` B3 · 2026-09-23 main 병합 뒤 정정):** 웨이브의 **시각·마릿수·스탯**은 **A-2-6a `MinionWave`** 가 정본이다. 이 표는 **마을별 개체 종류**(`Phase`+`VillageId` → `MonsterId` · 가중치)만 제공한다 — 마을 행이 있으면 그 마을 웨이브의 개체는 그 행에서 뽑고, 없으면 `MinionWave.MonsterId`. `Level`·`BaseHp`·`BaseAttack` 열은 더 안 읽는다(헤더 유지).

### A-2-6a. `MinionWave` — 웨이브 표 (2026-09-22 · `a/plan-0914` B3 · WO-027 §7)

```
Profile,WaveKey,Phase,StartSeconds,Count,SpawnGapSeconds,MonsterId,Hp,Attack,Exp,Meso,ZombieCount,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Profile` + `WaveKey` | **복합키.** `Profile` = `TEST`/`LIVE`(A-2-5 와 같은 뜻 · TEST 시각 = LIVE ÷ 8) · `WaveKey` = `P1-0` `P1-1` `P1-2` · `P2-1` `P2-2` `P2-3` · `P3-1`~`P3-5` |
| `Phase` | `MatchPhase`(§0-2). 표시·게이트용 |
| `StartSeconds` | 🔴 **매치 경과 시각**(레벨 아님). LIVE: 450 · 660 · 870 / 1020 · 1200 · 1380 / 1500 · 1560 · 1620 · 1680 · 1740 |
| `Count` · `SpawnGapSeconds` | 마을당 마릿수(**8**) · 등장 간격(**1**초) |
| `MonsterId` · `Hp` · `Attack` · `Exp` · `Meso` | 그 웨이브 개체 스탯. `MonsterInfo` 를 읽지 않는다(레인 전용 값). Exp·Meso = **같은 시기 일반몹과 동일** · 재료 드롭 없음 |
| `ZombieCount` | 그 웨이브에 **추가로** 나오는 좀비머쉬맘 수(기본 `0` · 3P 는 1 → 2분마다 +1). 난이도 배율(`ZOMBIE_MUL` · ★1 = 0)은 `DifficultyService` 가 곱한다 |

- 값의 정본은 WO-027 §7(허브) · 저장소 안 요약은 `Docs/추가기획3/구현항목-결정.md`. 초당 1회 평타 가정으로 역산한 초안이며 **TEST 플레이(웨이브 뒤 포탑 HP 로그)로 보정**한다.
- 억제기 파괴 배율(`SuppressorMinion*Mul`)은 이 표의 `Hp`/`Attack` 위에 그대로 곱한다. 3P 웨이브를 넘어서는 시각(`P3-5` 뒤)은 마지막 행을 반복한다.
- ★1 의 3P 는 `DifficultyRule.MINION_P3_BOOST=0` → `P2-3` 행 값으로 대체.
- ✅ **2026-09-23 구현(B3 · PR #71)**: `Match/MatchSessionLogic.LoadWaves/FireWave` → `Lane/MinionFlowService.QueueWave`(스폰 큐 · `OnUpdate`). **좀비 개체 스탯은 같은 `Profile` 의 `WaveKey=P3-ZOMBIE` 행**(`StartSeconds=-1` · `Count=0` → 스케줄 없음 · 템플릿 전용). `Exp` = 같은 시기 일반몹의 B2 레벨 보간값(Lv12 27 … Lv30 30 → 실제 표 값 참조) · 좀비 Exp/메소 = 엘리트급(×10). 표가 끝나면(`P3-5`) 마지막 두 행의 시각 차(60초 · TEST 8초)로 마지막 행 반복.
- 이 표가 들어오면서 **`MinionComposition`(A-2-6)은 미사용**(파일·헤더 유지 · 로더 제거) · `MinionPhaseConfig` 는 `Phase`·`StartSeconds`·`Profile`·`Enabled` 만 소비(`SpawnIntervalSeconds`·`HpMul`·`AtkMul`·`ExpBase`·`MesoBase`·`CoinDropChance` = 0 · 헤더 유지). 페이즈 시각 LIVE 270 / 990 / 1470.
- 억제기 파괴 폭발(전 마을 공통 · 사용자 2026-09-22)은 CSV 가 아니라 `Lane/LaneStateService` property(`SuppressorBoomDamage` 10,000 · `SuppressorBoomRadius` 3.0 · `SuppressorBoomRuid` ICBM special `807816a6…` ×0.8 · `SuppressorBoomSoundRuid` `5e8b2bcc…`). 대상 = `MinionUnit` 만. `TowerConfig` 값 ×75(포탑 60,000/90,000/135,000 · 억제기 90,000/135,000/195,000 · 넥서스 150,000 · `RepairCostPerHp` 0.00067/0.0013) · `PlayerDamageMul` 12.

### A-2-7. `DifficultyConfig` — 매치 전체 난이도

```
Difficulty,HeartCost,SoloDiscount,RankerChallenge,Enabled,#Note
```

> 🔄 **2026-09-22 정정** — 위 헤더가 **실제 파일**(WO-022 · PR #50)이다. 이 절에 오래 남아 있던 `DifficultyLevel,DisplayName,MonsterHpMul,…` 12열 헤더는 만들어진 적이 없어 지운다. 배율은 A-2-7a `DifficultyRule` 로 간다.

**5행 유지(1~5).** 로비에서 고른다. 🔴 **2026-09-22(`a/plan-0914` B6): ★1 / ★3 / ★5 세 개만 쓴다** — `2`·`4` 행은 `Enabled=false`(행 삭제 금지). 지금은 `Enabled` 를 아무 코드도 읽지 않으므로 B6 이 `BalrogHeartService.IsEnabled` + 로비 생성/참가 거절 + 별 버튼 비활성으로 **실제로 소비**하게 만든다.

🔴 **규약**
- **기존 표에 난이도 열을 추가하지 않는다.** 배수·수치·문구는 **A-2-7a `DifficultyRule` 에만**(이 표는 심장 비용·활성만)
- ✅ **2026-09-23 (`a/plan-0914` B6 · PR #75):** `Enabled` 를 실제로 소비한다 — `BalrogHeartService.IsEnabled(d)` → `DifficultyService.IsEnabled(d)` → `MatchLobbyGateway` 가 생성·참가를 **거절**(`[Lobby] create/join refused … reason=disabled`). ★2·★4 행은 `Enabled=false`. 로비는 서버가 실어 보낸 ★ 만 그린다(★2·★4 버튼 숨김).
- **배수 적용은 서버 한 곳에서만** — 각자 곱하면 이중 적용·누락이 생긴다. 적용 지점은 **`Match/DifficultyService`**(B6 신설 · `GetMul(key)` / `GetNum(key)` / `GetText(key)`) 하나. `DamageFormula` · `BossSpawner` · `MinionFlowService` 등은 이 서비스를 부르기만 한다
- 난이도는 매치 시작 시 확정(`MatchSessionLogic.Difficulty` · 인스턴스 룸이면 룸마다), `MatchStartedEvent` payload에 포함
- `MinionPhaseConfig` 의 페이즈별 상승과 **다른 축**이다. **최종값 = 기준값 × 페이즈/웨이브 값 × 난이도 배율** (이 순서 고정)

### A-2-7a. `DifficultyRule` — 난이도별 배율·수치·문구 (2026-09-22 · `a/plan-0914` B6 · WO-027 §9-0)

```
Difficulty,Key,Value,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Difficulty` + `Key` | **복합키.** `Difficulty` = 1/3/5 · `Key` = `DifficultyRuleKey`(§0-2) |
| `Value` | 숫자 키는 소수/정수 문자열(`0.5` · `200`), 문구 키는 그대로 문자열(줄바꿈 없음 · 설명이 여러 줄이면 `INTRO` `NEG` `POS` `REWARD` `HARD` `TIP` 여섯 줄로 나눈다) |

확정값(WO-027 §9-0): ★1 `BALROG_HP_MUL`/`BALROG_ATK_MUL`/`BOSS_HP_MUL`/`BOSS_ATK_MUL` 0.5 · `MATCH_EXP_MUL` 1.5 · `ACCOUNT_EXP_MUL` 0.5 · `MINION_P3_BOOST` 0 · `ZOMBIE_MUL` 0 · `BALROG_TICKET` 5 · `BOSS_SOULSTONE` 20 · `ELITE_COUNT` 1 / ★3 전부 1 · 티켓 30 · 영혼석 30 / ★5 발록 2 · 보스 1.5 · `ZOMBIE_MUL` 2 · `ELITE_COUNT` 2 · `REGION_DROP_MUL` 2(타 지역) · 티켓 200 · 영혼석 60 · `UNLOCK_ACCOUNT_LV` ★3 5 · ★5 13. `ELITE_PER_KILLS` 25 공통.
- 문구 키는 **서버가 CSV 문자열로 실어 보내고** 로비 컨트롤러는 그리기만 한다(낙관적 UI 금지 · WO-023 규약 그대로).
- `BalrogRoomService.TicketCosts` 속성(`"1,5,10,15,20"`)은 B6 부터 `BALROG_TICKET` 으로 대체(속성은 폴백으로만).

✅ **2026-09-23 구현(`a/plan-0914` B6 · PR #75):** `DifficultyRule.csv` 63행(★1/3/5 × 14 수치 + 7 문구) + `.userdataset`. 단일 적용점 `Match/DifficultyService.mlua`(@Logic · ServerOnly): `Current()`(진행 중 매치의 ★ · 없으면 0 → 배율 1·수치 0) · `GetMul/GetNum/GetText(key)` · `GetMulFor/GetNumFor/GetTextFor(d, key)` · `IsEnabled(d)` · `IsUnlocked(d, userId)`(`UNLOCK_ACCOUNT_LV` vs `AccountData.GetLevel`) · `UnlockLevelFor(d)` · `Recommended(userId)` · `Describe(d, userId)`(선택창 문구 `TITLE|INTRO|NEG|POS|REWARD|HARD|TIP|심장|솔로|해금Lv|잠김|추천`) · `BossHpMulFor/BossAtkMulFor(mapName)`(발록 방 접두면 `BALROG_*`, 아니면 `BOSS_*`). 소비처: `MatchSessionLogic.FireWave`(`MINION_P3_BOOST` <0.5 → 3P 행을 2P 마지막 행 값으로 · `ZombieCount × ZOMBIE_MUL`) · `Boss/BossSpawner.ApplyDifficulty`(스폰 직후 + ★ 변경 때 MaxHp·AttackPower 재주입 · HP 비율 유지 · `[Boss] diff= … maxHp=`) · `Farm/FarmReward.GiveRewards`(`MATCH_EXP_MUL`) · `Farm/DropTableLogic.RegionMul`(`REGION_DROP_MUL` · 맵 지역 ≠ 내 마을 지역일 때만) · `Boss/BalrogRoomService.TicketCost`(`BALROG_TICKET` · `[Balrog] ticket=`)/`BossMaxHp`(`BALROG_HP_MUL` · BeginMatch 에서 굳힘) · `Boss/BossRewardService`(`BOSS_SOULSTONE` 폴백) · `MatchLobbyGateway`(거절 + `BuildBrowseCsv` 에 `alv=` · `d1/d3/d5=`) . `ELITE_*` 는 B7 · `ACCOUNT_EXP_MUL` 은 B8 이 소비한다.

### A-2-8. `FunctionalNpcCatalog` — 기능 NPC 계약

```
CatalogNpcId,RoleKey,DisplayName,UiGroupName,UiRoute,ActionRoute,OwnershipMode,SectorId,SlotOrder,Enabled,Hidden,#Note
```

✅ **2026-09-09 등록** — `Hidden` 열을 `#Note` **앞**에 추가(§0-1(3) 패턴 · 중간 삽입 아님).
`true` 면 `NpcSpawner` 가 스폰 직후 스프라이트 알파를 0 으로 만들고 `NameTagComponent` 를 끈다 —
**모양만 감추고 클릭은 살린다**(`Enable=false` 로 끄면 터치 판정까지 죽는다). 빈칸 = `false`.
현재 유일한 사용처는 `VD_COMMON_DIMENSION_GATE` — 맵에 이미 차원 관문 오브젝트가 있어 NPC 그림이 겹쳤다(사용자 지시).

🔄 **2026-09-09 변경** — `VD_COMMON_DISPATCH_OFFICER` 가 `PUBLIC`/무섹터(허브 전용) →
**`OWNER_ONLY` · `SectorId=DEFENSE` · `SlotOrder=2`**. 파병 접수는 이제 **내 마을**에서 한다
(GDD §4.3 · §4.14 동시 수정 · `MapNpcs_Village` 의 여섯갈래길 행 삭제).
`VillageNpcSector` 의 `DEFENSE` 행 `SlotPerRow` 는 1 → **2**.

| 열 | 설명 |
|---|---|
| `CatalogNpcId` | **키.** `VD_` 접두 (`VD_WORKSHOP_CRAFT`) |
| `RoleKey` | `NpcRole` §0-2 |
| **`UiGroupName`** | 열 UIGroup — **라우터가 이 값으로 그룹을 켠다** |
| `UiRoute` | 그룹 안에서 열 팝업 (`craft`) |
| `ActionRoute` | 서버 액션 라우트 |
| `OwnershipMode` | `OWNER_ONLY` / `PUBLIC` |
| `SectorId` | 마을 기능 NPC의 구역. 공용 2종은 빈 칸이며 개별 좌표 오버라이드가 필요하다 |
| `SlotOrder` | 같은 구역 안에서 앵커 오프셋을 계산할 순서. 1부터 시작 |

**M1 기준 12행** (마을 역할 10 + 공용 2). **확장 가능한 표다** — 역할을 늘리면 행을 추가한다.
🔄 **2026-09-22 (`a/plan-0914` B1):** 17행 — `VD_SHOP_POTION_NOVICE`(SHOP_POTION · PUBLIC · 리스항구 · 빨강·파랑만) · `VD_COMMON_JOB_CHANGE`(COMMON_JOB_CHANGE · PUBLIC · 리스항구 · 클릭 → B 스킬 창) 추가. 둘 다 무섹터라 `MapNpcs_Village` 오버라이드로 위치를 준다(`LithHarbor_Village_MinimiMain` · 기존 NPC 9010022 양옆 · 눈 확인 뒤 조정). 모델 `Models/Npcs/VD_SHOP_POTION_NOVICE` · `VD_COMMON_JOB_CHANGE`(기존 모델 복제 · 그림은 일괄 교체 예정).
런타임 인스턴스는 10 × 6마을 = **60개** + 공용 2개.

> `UiGroupName` 열 덕분에 **팝업 추가 = 새 `.ui` 노드 + CSV 행 1개**가 되고 라우터 스크립트를 수정하지 않는다.

### A-2-8a. `VillageNpcSector` — 마을 기능 NPC 앵커

```
VillageId,SectorId,AnchorX,AnchorY,SlotDX,SlotDY,SlotPerRow,FlipX,Enabled,#Note
```

- 기본 키는 `VillageId` + `SectorId`. 6마을 × 4구역, **24행**이다.
- `AnchorX`/`AnchorY`는 디자이너가 Maker에서 실측해 채운다. 좌표는 world unit이다.
- `SlotDX`/`SlotDY`/`SlotPerRow`로 `FunctionalNpcCatalog.SlotOrder`의 자동 배치 좌표를 계산한다.
- 개별 위치가 필요하면 선택적 `MapNpcs_Village.csv` 오버라이드가 우선한다. 행도 앵커도 없으면 스폰을 건너뛰고 서버 경고 로그를 남긴다.

### A-2-8b. 플레이어 장비 · 강화 표

> 📝 **2026-09-06 값 정정**: `EnhanceSlotBonus` 1티어 방어구의 `AddDefense` 0 이던 5행(TOP/BOTTOM/SHOES +1 · GLOVES +1·+2)을 **1** 로 — 매 강화 단계에 기본(보석 무관) 상승이 있어야 한다는 결정. 강화 창·툴팁·장비 상세는 고정치 **초록**, 보석치 **보라**로 구분해 표시.

#### `ItemInfo`

```
ItemId,Name,ItemType,EquipSlot,ReqLevel,ReqJob,IconRUID,BaseStr,BaseDex,BaseInt,BaseLuk,BaseAttack,BaseMagic,BaseDefense,BaseSpeed,BaseJump,BaseAccuracy,BaseAvoid,BaseMaxHp,BaseMaxMp,Mastery,MaxDurability,MaxEnhanceLevel,Stackable,MaxStack,SellMeso,Enabled,#Note,AvatarSlot
```

`ItemId`가 기본 키다. `ItemType=EQUIP`만 `EquipSlot` 7종(`WEAPON` · **`SUBWEAPON`**(방패 · 2026-09-08 추가) · `HAT` · `TOP` · `BOTTOM` · `SHOES` · `GLOVES`)과 `Mastery`를 가진다.
`SUBWEAPON` 은 아바타의 `CustomSubWeaponEquip` 이고, **두손무기가 그 슬롯을 함께 점유**하므로 둘은 동시에 착용할 수 없다(서버가 상대를 자동 해제한다). 아이콘은 `.ui`에 고정하지 않고 `IconRUID`로 런타임 주입한다.

**`AvatarSlot`** (2026-09-08 추가 · 맨 뒤 · 중간 삽입 금지 규칙): 아바타에 장착할 때 쓰는 `CostumeManagerComponent` 슬롯.
`TWO_HAND` 면 `CustomTwoHandedWeaponEquip`, 빈칸이면 부위 기본(무기는 `CustomOneHandedWeaponEquip`).
**엔진이 공격 모션을 이 카테고리로 고르기 때문에 반드시 실제 아바타 아이템의 category 와 맞춰야 한다** —
두손 활을 한손 슬롯에 넣으면 활을 들고 한손 검처럼 휘두른다(2026-09-08 실제로 그랬다).
`IconRUID` 의 avataritem 이 `twohandweapon` 이면 `TWO_HAND`, `weapon` 이면 빈칸.

#### `EnhanceTable`

```
EnhanceLevel,MesoCost,GemCount,SuccessRate,DestroyRate,DowngradeOnFail,AddDefense,Enabled,#Note
```

`EnhanceLevel`이 기본 키다. M1은 1~3단계, `SuccessRate=1.0`, `DestroyRate=0`으로 시작한다. `AddDefense`는 2026-09-05 부로 `EnhanceSlotBonus`로 이관됐다 — 열은 남기되(삭제 금지) 값은 0을 유지하고, **코드는 이 열을 더 이상 읽지 않는다**(`EquipService.RecalcLayer` · 강화창 미리보기).

✅ **2026-09-22 값 변경(`a/plan-0914` B2 · WO-027 §5):** `MesoCost` 1000/3000/8000 → **850 / 2,550 / 6,800**(약 15% 완화 · 사용자 "10~20%만"). 헤더 변경 없음. 보석 교환(안 쓰는 보석 3 → 원하는 보석 1 · `SinkType=GEM_EXCHANGE`)은 강화 장인 창의 부가 동작이며 표가 없다(비율은 `EnhanceService` property).

#### `EnhanceSlotBonus` (2026-09-05 추가 · ⑫)

```
EquipSlot,ReqLevel,EnhanceLevel,AddStr,AddDex,AddInt,AddLuk,AddAttack,AddMagic,AddDefense,AddSpeed,AddJump,AddAccuracy,AddAvoid,AddMaxHp,AddMaxMp,Enabled,#Note,FixedAttack
```

기본 키는 `EquipSlot` + `ReqLevel` + `EnhanceLevel`. 아이템은 표의 `ReqLevel` 중 **"자기 `ReqLevel` 이하의 최대"** 티어에 매칭된다(예: 요구레벨 15 → 티어 10). `Add*` 열은 `ItemInfo`의 `Base*` 13종과 같은 순서다. `FixedAttack` 은 2026-09-08 에 **맨 뒤로** 추가됐다(중간 삽입 금지 규칙 · `#Note` 뒤).

**의미(B안 · 사용자 확정 2026-09-05 / 무기 고정치 2026-09-08)** — 강화 단계마다:
- **부위 고정 상승치** = 보석과 무관하게 그 단계에서 그냥 오르는 값(툴팁 초록). 부위별로 열이 다르다:
  - 방어구(HAT·TOP·BOTTOM·SHOES·GLOVES) → `AddDefense`(방어력)
  - 무기(WEAPON) → **`FixedAttack`(공격력)** · 기본값 2 (전 티어·전 단계 균일 · 밸런싱 미조정)
  - `AddAttack` 을 재사용할 수 없는 이유: 그 열은 이미 "다이아 1개당 상승량"이다.
- 나머지 `Add*` = 그 단계에 소모한 **보석 1개당** 그 보석 스탯의 상승량. 보석은 "어느 스탯에 줄지"만 고르고 양은 이 표가 정한다(다이아 → `AddAttack`, 힘의 결정 → `AddStr` …). 값이 0인 열의 보석은 그 부위에 효과가 없으며 UI가 비활성·서버가 거절한다(무기 외 부위의 다이아·사파이어). 툴팁 보라.

규칙: 요구레벨이 높은 장비일수록 단계당 상승이 크고, 10렙 장비 풀강(주스탯 보석 6개) ≈ 20렙 장비 기본치 ×1.05. 30렙 티어는 상한 없이 높게 잡는다. ENHANCE 레이어 = 고정치 누적 + 보석별 양 누적(`ItemCatalog.ComputeEnhance` 한 곳에서 계산 · 장비창/툴팁/강화창 미리보기가 같은 식을 쓴다).

#### `GemInfo`

```
GemId,StatId,AddPerLevel,DisplayName,IconRUID,Enabled,#Note
```

`GemId`가 기본 키다. 12종 보석은 `StatId`와 1:1이며, `DEFENSE`는 보석이 아니라 `EnhanceSlotBonus.AddDefense`로만 오른다. `AddPerLevel`은 2026-09-05 B안부터 **미사용**(열은 남기고 값 0 유지 · 상승량은 `EnhanceSlotBonus`의 해당 스탯 열).

#### `GemDropTable`

```
SourceId,SourceKind,GemId,Chance,CountMin,CountMax,Enabled,#Note
```

기본 키는 `SourceKind` + `SourceId` + `GemId`. `SourceKind`는 `MONSTER` / `ELITE` / `BOSS`다. `SourceId`는 해당 도감·보스 ID를 가리킨다.

🔄 **2026-09-22 (`a/plan-0914` B2):** 보석·지역 재화·몬스터별 재화 드롭은 **A-2-19 `DropTable`** 로 옮긴다(사냥터 단위). 이 표는 파일·헤더·행 유지(삭제 금지) · 코드는 더 안 읽음.

### A-2-8c. 공방 거래 표 — 제작·상점·물약·수리 (2026-09-06 추가)

기획 정본(`Docs/추가기획1/기획 정리…md` §2-2): **지역 장비 상점 = 그 지역 직업 장비를 지역재화로**(헤네시스 궁수 · 커닝시티 도적 · 엘리니아 마법사 · 노틸러스 해적 · 페리온 전사), **리스항구 = 초보자 장비를 메소로**. 사용자 결정(2026-09-06): **제작 장인과 지역 장비 상인은 같은 표(`CraftRecipe`)를 연다**(장비 구입 = 제작). 직업 장비 `ItemInfo` 행은 `{SLOT}_{JOB}_T{10|20|30}` · `ReqJob` 은 `WARRIOR / MAGICIAN / ARCHER / THIEF / PIRATE` **임시 열거값**(B 의 `JobId` 확정 시 치환).

#### `CraftRecipe`

```
RecipeId,ShopKey,ResultItemId,ResultCount,MesoCost,Mat1ItemId,Mat1Count,Mat2ItemId,Mat2Count,Enabled,#Note
```

`RecipeId`가 기본 키다. `ShopKey` = `REGION_HENESYS` / `REGION_KERNING` / `REGION_ELLINIA` / `REGION_NAUTILUS` / `REGION_PERION` / `NOVICE` — NPC 가 선 맵의 지역으로 정한다(`VillageConfig` 전까지 맵 이름 접두로 임시 판별). `Mat*ItemId` 는 `ItemInfo` `MATERIAL`(지역재화) · 빈칸이면 메소만. 서버는 요청자 현재 맵의 `ShopKey` 와 행의 `ShopKey` 가 같을 때만 실행한다.

#### `ShopItem`

```
ShopItemId,ShopKey,ItemId,PriceMeso,MaxCount,Enabled,#Note
```

`ShopItemId`가 기본 키다. 메소로 사는 소모품(물약 상인 `ShopKey=POTION`). 장비는 여기 없다(`CraftRecipe`).

#### `ConsumeInfo`

```
ItemId,HealHp,HealMp,Enabled,CooldownStartSeconds,CooldownEndSeconds,CooldownEndLevel,#Note
```

`ItemId`(= `ItemInfo` `CONSUME` 행)가 기본 키다. 인벤토리에서 클릭 → 서버 `RequestUse` → 1개 소모 + 회복.

✅ **2026-09-08 추가(WO-014 · 추가기획2)** — 열 3개를 **뒤에 추가**(중간 삽입 없음):

| 열 | 설명 |
|---|---|
| `CooldownStartSeconds` | 1레벨 쿨타임. 물약 = `13` |
| `CooldownEndSeconds` | `CooldownEndLevel` 이상에서의 쿨타임. 물약 = `6` |
| `CooldownEndLevel` | 최소 쿨에 도달하는 레벨. 물약 = `30` (⚠️ 30 인지 50 인지 `Docs/추가기획2/미정-값.md` B-1 확인 대기) |

**선형 보간**: `cd = start + (end - start) × min(1, (level - 1) / (endLevel - 1))`.
쿨 중 사용 요청은 **아이템을 소모하지 않고** 거절 + 토스트. 원장은 서버 메모리(`InventoryService.potionReadyAt[userId]`)이며 `_UtilLogic.ServerElapsedSeconds` 기준이다.
> 지금 `CONSUME` 은 `POTION_RED` 한 종류라 사실상 물약 공용 쿨타임과 같다. 종류가 늘어 "공용 쿨 묶음"이 필요해지면 `CooldownGroup` 열을 **뒤에** 추가한다.

✅ **2026-09-22 등록(`a/plan-0914` B1 `a/p0914-1-match-start` · 헤더+CANONICAL 동시 변경 · #40 comment 5776547020 공지):** `#Note` 앞에 **`HealHpPct` · `HealMpPct`** 추가(최대치 비율 · 빈칸 = 0). 실제 헤더: `ItemId,HealHp,HealMp,Enabled,CooldownStartSeconds,CooldownEndSeconds,CooldownEndLevel,HealHpPct,HealMpPct,#Note`. **HP 를 회복하는 소모품은 전부 한 쿨 묶음("HP" · `InventoryService.CooldownKey`)** — `CooldownGroup` 열은 만들지 않았다. 리스항구 공용 물약 상인은 `ShopItem.ShopKey=POTION_NOVICE`(빨강·파랑) · 마을은 `POTION`(5단계) — `ItemCatalog.PotionShopKeyOfMap` 이 맵으로 정하고 `ShopService.RequestBuy` 가 같은 규칙으로 거절한다. **물약 5단계**(WO-027 §2-1): `POTION_RED` 50·30메소(현행) · `POTION_ORANGE` 150·150 · `POTION_WHITE` 300·500 · `ELIXIR` HP·MP 50%·3,000 · `POWER_ELIXIR` HP·MP 100%·9,000. HP 물약은 한 쿨 묶음(`CooldownGroup` 열은 아직 안 만든다 — HP 회복이 있는 행은 전부 같은 쿨로 취급) · `POTION_BLUE` 별도 쿨 유지. `ShopItem` 에 4행 추가(`ShopKey=POTION` · 마을 물약 상인 · 리스항구 공용 물약 상인은 빨강·파랑만).

#### `RepairConfig`

```
ReqLevel,MesoPerDurability,Enabled,#Note
```

`ReqLevel`이 기본 키다. 수리비 = (최대 − 현재 내구도) × `MesoPerDurability`(티어 = 장비 `ReqLevel` 이하 최대 · `EnhanceSlotBonus` 와 같은 규칙).

### A-2-8d. `MonsterTraining` — 수비 몬스터 병과 훈련 (2026-09-06 추가 · A)

```
TrainClass,Level,DreamPieceCost,StatMul,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `TrainClass` + `Level` | **복합키.** `TrainClass` = `MELEE` / `RANGED` / `TANK`, `Level` = 1~5 |
| `DreamPieceCost` | **그 레벨에 도달하는 데** 드는 꿈의 조각(`DREAM_PIECE`) 개수. Lv1 은 0 |
| `StatMul` | 그 레벨의 수비 몬스터 능력치 배율 (기획 정리 §2-3: 1.0 / 1.5 / 2.2 / 3.3 / 5.0) |

- A-3 예약명 `MonsterTraining`(`MonsterTypeId`+`Level`) 을 **병과 단위**로 확정 — 추가기획1 "훈련은 종류 단위 · 즉시 소모". 시간·과부하 열은 폐기.
- 읽는 곳: `Lane/LaneStateService`(훈련 비용·배율). 15행 고정. 값 튜닝은 `m1-balance-01`.
- 🔄 **2026-09-22 (`a/plan-0914` B5):** `StatMul` 은 **A-2-20 `MonsterRecruit.GuardHp/GuardAttack`**(모집 몬스터 티어로 고정된 기본값) 위에 곱한다. 수비대는 페이즈가 바뀌어도 세지지 않는다 — 티어 높은 몹 모집 + 훈련이 유일한 강화 경로(사용자 2026-09-22).

### A-2-9. `MatchRouteConfig` — 이동 허용 목록

```
MatchRouteId,MapName,RouteKind,Enabled,#Note
```

`RouteKind` = `VILLAGE`/`HUB`/`LANE`/`REAR`/`BOSS`. 키를 `MatchRouteId` 로 둔 것은 `PortalRoutes.RouteId` 와 구분하기 위함이다.
> 이 표가 비어 있으면 테스트 재화로 개발은 가능하나 **M1 릴리스 완료로 판정하지 않는다.**

### A-2-10. 발록 방 — 승리 조건 (WO-017 · 2026-09-13 개정 · PR #51)

**✅ 개인 방 방식 (2026-08-29 확정)** — MSW 의 채널 격리에 의존하지 않는다. 발록 방을 여러 개 만들고 1인 1방으로 쓴다.

> ~~`BalrogChallenge.csv`(`MesoCost` · `RequiredLevel` 21 · `RoomCount` · `OccupancyTimeoutSeconds`)~~ ·
> ~~`BalrogRoom.csv`(`RoomId,MapName,SpawnX,SpawnY,BalrogModelId`)~~ — **채택하지 않았다** (2026-09-10~13 사용자 확정).
> 입장권이 메소가 아니라 아이템이고 · 방을 입장 때가 아니라 매치 시작 때 고정 배정하며 · 발록은 기존 `BossInfo` 1행을 공유하므로
> 새 표가 필요 없다. **새 CSV 0개.**

**설정 위치** — `RootDesk/MyDesk/Boss/BalrogRoomService.mlua` (`@Logic`) 속성과 기존 표

| 항목 | 값 | 위치 |
|---|---|---|
| 방 | `Sleepywood_Boss_JrBalrog` · `…2` ~ `…6` — 6개 · 같은 지형 복제 | `BalrogRoomService.RoomMaps` |
| 발록 | BossId **`8130100` 하나를 6방이 공유** — `BossInfo` 행 추가 없음 · 복제 방 5개 스포너만 `BossSpawner.AllowClonedMap = true` | `BossInfo.csv` · 맵의 `BossSpawner` |
| 입장권 | 인벤토리 아이템 **`BRAND_SOULSTONE`(낙인의 영혼석)** · 난이도 ★1~5 = **1 · 5 · 10 · 15 · 20** 개 | `TicketItemId` · `TicketCosts` — `DifficultyConfig`(A-2 · PR #50) 머지 뒤 그 표의 열로 옮긴다 |
| 도전 시간 | **90초** — 방에 **도착한 순간**부터 | `ChallengeSeconds` |
| 레벨 제한 | 없음 | — |

**규칙 (서버가 소유)**
- 방은 **매치 시작 때 참가 순서대로 1회 고정 배정**한다. 입장할 때마다 배정하지 않는다 — 남의 방에는 못 들어간다.
- 진입은 슬리피우드 마을 포탈 1개 → 서버가 배정된 방으로 보낸다 (그 노선만 엔진 직행 바인딩에서 뺀다 · `PortalNetwork.GatedToMap`).
- 입장권은 요청 때 개수만 보고 **도착했을 때** 뺀다. 환불 없음.
- 도전 종료: 90초 만료 · 사망 · 걸어서 나감 · 탈락 · 접속 끊김 → 마을로. **배정은 유지**한다.
- **여러 번에 나눠 잡아도 된다.** 도전 사이에 발록 HP 는 **회복되지 않는다** — 발록 엔티티가 곧 장부라 따로 저장하지 않는다. 도전마다 입장권을 낸다.
- 도전 창(도착 ~ 90초 · 사망/이탈 전) **밖에서는 발록이 무적**이다 (`HitComponent.Enable = false`).
- 발록 방은 정적 맵이라 판이 끝나도 남는다 → **매치 시작 때 6방 발록을 풀 HP 로 다시 깔고**, 방에 남은 사람은 승강장으로 보낸다.
- 결과 화면 1순위 열 = 자기 방 발록에 준 **누적 피해 = `MaxHp − 현재 HP`** (1인 1방).

🔴 **선취 판정은 서버가 1회만** 한다 — `BalrogRoomService.DeclareWinner` → `MatchSessionLogic:DeclareWinner` **직접 호출** (B-3 의 발록 이벤트 2개는 쓰지 않는다).
같은 프레임에 둘이 잡으면 방 번호가 빠른 쪽. 시간 만료와 같은 프레임이면 `ClaimPendingWinner` 가 한 번 더 본다. 클라가 "이겼다" 를 보내는 경로는 없다.

### A-2-11. `BossReward` — 보스 보상

```
BossId,TopDamageItemId,TopDamageMeso,FirstClaimItemId,FirstClaimMeso,FirstClaimOnce,Enabled,#Note
```

- `TopDamage*` — **최다 누적 피해자** 처치 판정 보상
- `FirstClaim*` — **선착순 1명** 보상. 마노 = 10레벨 제한 **체력 갑옷**(공격력 상승 없음) + 메소
- `FirstClaimOnce` = `true` 면 매치당 1회

✅ **2026-09-23 구현(`a/plan-0914` B6 · PR #75 · 헤더+CANONICAL 동시 변경 · #40 공지 5776547020):** `#Note` 앞에 **`SoulstoneStar1` · `SoulstoneStar3` · `SoulstoneStar5`** 추가 — 지역 보스 1마리가 최다 피해자에게 주는 낙인의 영혼석(★1/3/5 = **20 / 30 / 60** · 마노 0). 발록 입장권은 ★1/3/5 = **5 / 30 / 200**(`DifficultyRule.BALROG_TICKET` · 도전마다 소모). 마노 행: `FirstClaimItemId=ARMOR_HP_LV10`(`ItemInfo` 새 행 · TOP · Lv10 · `BaseMaxHp` 200 · 공격·방어 0) · `FirstClaimMeso=500`(B1). 지역 보스 **입장**은 지역 재화 25개(`SinkType=BOSS_ENTRY` · 포탈 + 확인 팝업 · 마노 무료) — B6.
  - 구현(2026-09-23 B6): 행 = 지역 보스 5종(`2400572` 좀비머쉬맘=헤네시스 실제 보스 · `9300003` · `3220000` · `5250007` · `8510000`) 20/30/60 · 마노 `2220000` 0/0/0 · `6130101`(Enabled=false 보스) 행도 20/30/60. 지급 경로 = `Boss/BossSpawner`(스폰한 보스의 `HitEvent` 를 이어 플레이어별 누적 피해 원장 · 사망 감지 때) → **`Boss/BossRewardService.OnBossKilled`**(신규 @Logic · 최다 피해자 · 동률은 먼저 때린 사람 · 매치 진행 중 + 참가자만 · 영혼석 = `SoulstoneStar{★}` 비면 `DifficultyRule.BOSS_SOULSTONE` · `TopDamageItemId/Meso` · 선취 `FirstClaim*` 매치당 1회 · `[BossReward]` 로그 + 토스트). 발록은 행이 없어 미지급(순위 보상은 B8). 입장권 = `PortalNetwork.RequestEnterBoss`(지역 보스 맵은 발록 방처럼 바인딩 안 함 · ↑ 1회 = 안내, `BossEntryConfirmSeconds`(8초) 안에 ↑ 재입력 = `RemoveItem` 25 + 이동 · `[Sink] BOSS_ENTRY` · 마노(`BossEntryFreeVillages=LITH`) 는 평소대로 바인딩). 확인 팝업 UI 는 토스트 2단계로 대체(그림·UI 일괄 때 팝업으로 교체 가능).

### A-2-12. `MapNpcs` 분할

핫 파일 완화를 위해 담당별로 나눈다. `NpcCatalog` 는 **쉼표 구분 테이블 이름 목록**을 읽어 순회한다.

| 파일 | 담당 | 내용 |
|---|---|---|
| `MapNpcs.csv` | 맵 임포트 | 원본·장식 NPC (기존 행 전부 보존) |
| **`MapNpcs_Village.csv`** | 마을 기능 NPC 좌표 오버라이드 | `VillageNpcSector` 자동 배치로 해결되지 않는 `VD_*` 개별 위치만. 파일 자체는 선택적 |

헤더 동일: `MapName,NpcId,X,Y,#Note`. 실제로 만드는 CSV마다 `.userdataset` 짝이 필요하다.

```
property string NpcMapTableNames = "MapNpcs,MapNpcs_Village"
```

`NpcCatalog`는 선택적 `MapNpcs_Village`가 없으면 이를 오류로 처리하지 않고 섹터 앵커 배치만 사용한다.

---

### A-2-13. `BossInfo` — 보스 정의 · 밸런싱 · 스폰

```
BossId,Name,Level,MaxHp,ModelId,MoveType,AiType,
AttackPower,Defense,KnockbackResist,MoveSpeed,
MapName,SpawnX,SpawnY,RespawnSeconds,Enabled,
DamagedSoundRUID,DieSoundRUID,
ContactScaleX,ContactScaleY,ContactOffsetX,MovePlayRate,DetectionRange,
JumpForce,#Note
```

**보스는 `MonsterInfo` 에 넣지 않는다.** 극소수(7종)이고 일반 몹과 축이 다르다 — 페이즈 스킬, 최다피해 판정, 선취 보상, 승리 조건이 전부 보스에만 있다. `MonsterInfo` 에 열을 붙이면 77행 전부가 빈칸을 갖게 된다.

| 열 | 의미 |
|---|---|
| `MaxHp` | **기준선 = 최상위 일반몹 15,000 × 150 = 2,250,000.** 패턴 수로 보정(적을수록 높게) |
| `Defense` | 0~100. 보스 20~50 (일반몹은 0) |
| `KnockbackResist` | 0~100. 보스 `100` = 안 밀림 |
| `SpawnX`/`SpawnY` | **빈칸이면 기존 배치를 그대로 쓴다** (피아누스가 그렇다) |
| `RespawnSeconds` | 보스 기본 `300` |
| `DamagedSoundRUID` | 보스가 **맞을 때** 나는 소리. 팩의 `_audio/Damage` |
| `DieSoundRUID` | 보스가 **죽을 때** 나는 소리. 팩의 `_audio/Die` |
| `ContactScaleX`/`Y` | 🔴 **몸통 접촉 판정 박스 보정 배율.** 접촉 박스는 스프라이트 **전체 크기**에서 뽑는데 원본에는 더듬이·꼬리·여백이 같이 들어 있어 실제 몸통보다 훨씬 크게 잡힌다. `1.0` 이면 보정 없음 |
| `ContactOffsetX` | 몸통이 스프라이트 중앙에 없을 때 좌우로 민다(월드 단위). **보는 방향으로 부호가 뒤집힌다** — 방향 전환은 `Scale.x` 가 아니라 `sprite.FlipX` 라 이걸 안 뒤집으면 한쪽을 볼 때만 박스가 앞으로 튀어나온다 |
| `MovePlayRate` | 평상시(이동·대기) 애니메이션 재생 속도. **이동 속도와 어긋나면 제자리에서 발버둥치는 것처럼 보인다** (킹슬라임 `0.5`) |
| `DetectionRange` | 처음 발견하는 거리. 음수면 모델 기본값. 보스는 `10` (화면 폭 12.8 대비 5는 40%뿐이라 늦게 반응했다) |
| `JumpForce` | 🔴 **양수일 때만 점프한다.** 이게 곧 스위치다 — 코드에 몹 이름을 박지 않는다. **보스 7종은 전부 `-1`**(점프 안 함, 사용자 확정). 실측상 도달 높이 ≒ `JumpForce` |

> ⚠️ **마노만 기준선이 다르다.** GDD 상 1페이즈 선착순 보스라 초반에 잡혀야 한다.
> 같은 ×150 배율을 **Lv10 구간 일반몹 125** 에 적용해 `18,750`. 배율 규칙 자체는 안 깬다.
> `AttackPower` 는 전 행 **튜닝 대상** — `LevelTable` 이 아직 자리표시자라 역산 근거가 없다.

🔄 **2026-09-22 (`a/plan-0914` B2 · WO-027 §2):** 위 "×150 기준선"은 폐기. **주니어 발록 `MaxHp` = 만렙(30) T30 풀강(보석 없이 고정치만) DPS × 75초**(★3 기준 · 평타 간격 실측 뒤 확정 · 초당 1회면 약 3~5만). 난이도 배율(★1 ×0.5 · ★5 ×2 · 지역 보스 ★5 ×1.5)은 이 표에 넣지 않고 `DifficultyService` 가 스폰 시 곱한다. `Defense` 열은 플레이어 피해 계산에서 **아직 안 읽힌다**(B4 에서 `ApplyDefense` 로 연결). ✅ **2026-09-23 확정: `MaxHp` 157,500 → 38,000**(전사 Lv30 T30 F 로테이션 508 DPS × 75초 · 평타 진입 간격 0.59s 실측 · 가장 약한 직업 기준이라 전 직업 솔플 가능 · B 스킬 조율 목표 500~600 DPS). 같은 커밋: `LevelTable` Lv1~30 만 남김(Lv31~50 행 삭제 → 만렙 30 · NeedExp 1차안 곡선 누적 24,595) · `MonsterInfo.Exp` 레벨 보간(Lv1 5 … Lv24 70 · 초과 +4.57/Lv).

**보상은 이 표에 없다.** `BossReward`(A-2-11) 가 갖는다. 보석은 `GemDropTable`(A-3).

### A-2-14. `BossSkills` — 보스 스킬 (1:N)

```
BossId,SkillId,SkillName,Priority,ShapeType,RangeX,RangeY,OffsetX,OffsetY,
CastDelay,Cooldown,Damage,HitCount,MinHpPercent,MaxHpPercent,
ProjectileModelId,ProjectileSpeed,CastRUID,Enabled,
ProjectileRUID,HitEffectRUID,RecoverTime,CastEffectRUID,CastEffectBehind,
CastSoundRUID,HitSoundRUID,CastEffectOffsetX,CastEffectOffsetY,CastEffectScale,
CastPlayRate,CastRUID2,CastDelay2,CastHitFrame,CastEffectAtStart,
CastEffectPlayRate,CastEffectStartFrame,CastEffectEndFrame,
ShotCount,ShotInterval,AimAtTarget,BuffMoveRate,BuffDuration,
Reach,LoopStartFrame,LoopEndFrame,CloseEndFrame,#Note
```

보스당 1~9행. 상한은 리소스 팩의 `attack*`/`skill*` 클립 수 + 소환/페이크 행이다 —
마노 2 · 머쉬맘 3 · 킹슬라임 3 · 스텀피 5 · 주니어발록 6 · 피아누스 7 · 에피네아 9.

> ⚠️ **`#Note` 에 쉼표를 쓰지 않는다.** csv 가 따옴표로 감싸면 MSW DataSet 파서가 열을 통째로 민다.
> 쉼표 대신 ` ·` 를 쓴다.

| 열 | 의미 |
|---|---|
| `ShapeType` | **`target`** 전방 최근접 1명 / **`box`** 전방 전원 / **`ground`** 점프로 회피(지진형) / **`crouchable`** 숙여서 회피(상단 휘두르기) / **`projectile`** / **`fake`** 판정 없음(페이크 모션) / **`summon`** 부하 소환 / **`buff`** 범위 내 아군 강화 |
| | ⚠️ 회피는 **박스 높이가 아니라 상태로 판정**한다. 플레이어 `HitComponent` 는 점프·숙이기로 크기가 바뀌지 않으므로 박스만으로는 절대 못 피한다 |
| `ProjectileModelId` | 이 스킬이 **스폰하는 모델**. `projectile` 이면 투사체, `summon` 이면 소환수 |
| `ProjectileRUID` | 투사체 스프라이트. **보스 자기 팩의 `clip/info/ball`** 을 쓴다. 비면 모델 기본값 |
| `HitEffectRUID` | 명중 시 **맞은 대상 자리**에 뜨는 이펙트. 팩의 `clip/info/hit` |
| `RecoverTime` | 판정 후 시전 자세를 붙잡는 시간. **0 이면 판정 순간 idle 로 튄다** |
| `CastEffectRUID` | 시전 중 보스 주변에 띄우는 이펙트. **날아가지 않는다** (마법진 등) |
| `CastEffectOffsetX`/`Y`/`CastEffectScale` | 🔴 **이펙트 전용 좌표·배율.** 판정 박스의 `OffsetX`/`OffsetY` 와 **별개다** |
| | 원본 이펙트는 pivot 에 위치가 박혀 있는데 **기준이 자산마다 다르다.** `attack2/info/effect`=`(-55,-57)` 은 원점에 놓으면 손 높이에 맞고, `attack3/info/effect`=`(138,-27)` 은 원점에 놓으면 몸통에 겹친다. 하나의 규칙으로 못 맞추므로 **자산별로 실측해서 넣는다.** `OffsetX` 는 보는 방향으로 부호가 뒤집힌다 |
| `CastEffectBehind` | `true` 면 보스 **뒤**(`OrderInLayer` 1), `false` 면 앞(3). 배경보다는 항상 앞 |
| `OffsetX` | 보스가 보는 방향으로 **부호가 뒤집힌다** |
| `CastDelay` | 예고~판정 (초). 이 구간에 `CastRUID` 모션이 돈다 |
| `MinHpPercent`~`MaxHpPercent` | 🔴 **페이즈 게이트** — 보스 HP% 가 이 구간일 때만 후보 |
| `CastRUID` | 시전 모션 animationclip. 팩의 `attack*`/`skill*` |
| `Damage` | 이 스킬 고유. `BossInfo.AttackPower` 와 **별개** |
| `CastSoundRUID` | 시전 순간 나는 소리. 팩의 `_audio/AttackN` · `_audio/SkillN` |
| `HitSoundRUID` | **플레이어가 맞았을 때** 나는 소리. 팩의 `_audio/CharDamN` |
| `CastDelay` **`= -1`** | 🔴 **클립 실측 자동.** `AnimationClip.Frames[i].Delay` 합계 ÷ `CastPlayRate`. 프레임 지연을 모르면 추측하지 말고 이걸 쓴다 — 머쉬맘 5프레임이 실측 **1.85초**(가정은 0.5초)였다 |
| `CastPlayRate` | 시전 모션 재생 속도. 클립을 복제해 프레임을 늘릴 필요 없다. **`CastDelay -1` 과 연동**되어 속도를 낮추면 시전 시간이 따라 늘어난다 |
| `CastHitFrame` | 판정이 나는 프레임 수(앞에서부터). **점프 클립은 마지막 프레임이 착지 후 복귀 자세라 길다** — 클립 전체로 재면 바닥에 닿고도 한참 기다린다. `0` 이하면 전체 |
| `CastRUID2`/`CastDelay2` | 2단 시전. 준비 동작 뒤에 다른 그림을 한 번 더 끼운다. 판정은 둘 다 지난 뒤(점프면 착지 시점) |
| `CastEffectAtStart` | `true` 면 **시전 시작**에, `false` 면 **판정 순간**에 이펙트를 튼다. `CastEffectBehind`(레이어)와 **별개다** — 원본 이펙트에 도입 구간이 있으면(머쉬맘 지진파 f0~f7 이 잔먼지) 시작에 틀어야 착지에 본 장면이 온다 |
| `CastEffectPlayRate` | 이펙트 재생 속도. 여러 단계로 된 이펙트가 "두 번 터지는 것처럼" 보일 때 뭉친다 |
| `CastEffectStartFrame`/`EndFrame` | 이펙트 재생 구간. 원본에 폭발이 여러 번 들어 있을 때 한 번만 쓴다(킹슬라임 지진파 13프레임에 폭발 2회 → `f0~f2`). `EndFrame` 이 음수면 끝까지 |
| `ShotCount`/`ShotInterval` | **연사.** 발수와 발 사이 간격(초). `1` 이면 예전과 동일 |
| `AimAtTarget` | `true` 면 **발사 시점의 플레이어 좌표를 정조준.** 유도가 아니다 — 쏜 뒤엔 직진 |
| `LoopStartFrame`/`LoopEndFrame` | 연사 중 **반복 재생할 프레임 창.** 한 바퀴 = `ShotInterval` 이 되도록 재생속도를 자동 계산한다 (스텀피 `f5~f9` = 입을 오므렸다 열 때마다 한 발) |
| `CloseEndFrame` | 연사가 끝난 뒤 한 번 재생할 마무리 구간(입 닫기) |
| `CrouchBand` | number | `shapeType=crouchable` 전용. 판정 박스 **아래끝에서 위로 이만큼**만 숙이기가 통한다. 음수면 박스 전체(기존 동작). 없으면 이펙트 한가운데 2층에 서 있어도 숙이기만 하면 무조건 면제된다 — 숙이기가 위치와 무관한 전역 상태 검사이기 때문이다 |
| `FireEffectRUID` | string | **판정 순간**에 띄우는 두 번째 이펙트. `CastEffectRUID` 가 예고(시전 시작)라면 이것은 타격 연출이다. 비면 없음. 피아누스 S7 의 노란 전기 |
| `FireEffectScale` | number | `FireEffectRUID` 배율. 기본 1.0 |
| `FireEffectFloors` | string | **비우면 foothold 세그먼트 자동** — 판정 박스에 걸리는 발판 세그먼트를 전부 열거해 **세그먼트마다 그 길이에 딱 맞게 가로만 늘린 이펙트 1장**(경사는 각도까지). 긴 바닥은 박스 폭으로 잘리고 짧은 발판은 그 길이만큼만. 값을 주면 보스 y 기준 고정 오프셋을 `;` 로 나열(`0;0.6`). 🔴 쉼표가 아니라 `;` 다 |
| `FireEffectTiles` | integer | **고정 오프셋 모드 전용.** tile 자산을 판정 폭에 가로로 몇 장 이어 붙일지. 자동(세그먼트) 모드에서는 쓰지 않는다. 기본 1 |
| | 🔴 **프레임 창은 렌더러에 남는 전역 상태다.** 안 되돌리면 다음 클립이 남의 구간에 갇혀 몇 프레임만 깜빡인다 |
| `Reach` | 이 스킬이 닿는 거리. `-1` 이면 `OffsetX + RangeX/2` 자동 계산. 🔴 **몸통 접촉 박스 반폭보다 작으면 플레이어가 몸에 막혀 판정 범위 안으로 못 들어간다** — 그 스킬은 영원히 안 맞는다 |
| `BuffMoveRate`/`BuffDuration` | `shapeType=buff` 의 이동속도 배율과 지속시간(초) |

> 🔴 **`summon` — 부하 소환.** 열 3개를 빌려 쓴다:
> `ProjectileModelId` = 소환할 모델 · `HitCount` = 마리 수 · `RangeX` = 좌우로 퍼지는 폭
> (3마리면 `-RangeX` / `0` / `+RangeX`). `CastEffectRUID` 는 **각 소환 자리**에 깔린다.
> 소환수 능력치는 그 모델의 `BossInfo` 행을 그대로 쓴다 — 마노를 소환하면 진짜 마노(HP 18,750)가 나온다.
> `RespawnOn=false` 로 세워 죽으면 사라진다(안 그러면 무한 증식).
> **사거리 판정을 받지 않는다** — 추격 대상만 있으면 쓴다.
> 주니어발록 `S4` 가 이 방식이다 (HP 5% 이하 · 쿨 9999 = 사실상 1회).

> 🔴 **`fake` — 페이크 모션.** 시전 모션·소리는 진짜와 똑같고 발사 순간 **아무 판정도 안 낸다.**
> `RecoverTime` 을 `0` 으로 두면 발사 직전에 모션이 끊겨 "나올 줄 알았는데 안 나온" 느낌이 난다.
> `CastEffectRUID` / `HitEffectRUID` / `HitSoundRUID` 는 **반드시 비운다** — 하나라도 나오면 들통난다.
> `Priority` 를 진짜 스킬보다 낮게 둬야 진짜가 준비됐을 때 페이크가 새치기하지 않는다.
> 주니어발록 `S2F` / `S3F` 가 이 방식이다 (쿨 3배 · HP 70% 이하 전용).

> 🔴 시전 모션은 `SpriteRUID` 직접 대입 후 복원(Pattern A)으로 한다.
> `ChangeState("ATTACK")` 은 몬스터에 `ATTACK` 이 자동 등록되지 않아 **`[LEA-3005]`** 가 난다.

### A-2-15. 엘리트 3표 — 구조만 (2026-09-02)

A-3 에 예약돼 있던 것을 **열까지 확정해 여기로 옮긴다.** 파일도 만들었다(행은 0개).
로스터·재료 종수는 **여전히 미정**(로드맵 미정 #4)이며, 구조가 먼저 있어야 스키마 충돌 없이 각자 붙일 수 있다.

```
EliteMonsterInfo.csv
EliteId,Name,BaseMonsterId,Level,MaxHp,ModelId,MoveType,AiType,
AttackPower,Defense,KnockbackResist,MoveSpeed,
MaterialId,MaterialMin,MaterialMax,Exp,Meso,Enabled,#Note

EliteSpawnTable.csv
MapName,BaseMonsterId,EliteId,Chance,Enabled,#Note

EliteMaterialInfo.csv
MaterialId,Name,Description,IconRUID,Enabled,#Note
```

- `BaseMonsterId` — **어느 일반 몹 자리를 대신 차지하는가.** `Chance` 는 0~1 소수
- `MaterialId` — 🔴 **대문자 열거값 문자열** (§0 — 신규 도메인은 숫자 ID 를 쓰지 않는다)
- `다일`(`6220000`, Lv50 HP 300,000)이 첫 엘리트 후보다. 원본이 보스인데 일반몹 도감에 있다.

✅ **2026-09-22 확정(`a/plan-0914` B7 · WO-027 §9-0 21 · 사용자 결정):** 엘리트 = **자이언트**(베이스 몹 스케일업 · 추가기획1 결정 그대로). **확률이 아니라 누적** — 그 맵에서 누가 잡든 **25마리째마다 1마리**(★5 는 2마리 · `DifficultyRule.ELITE_PER_KILLS`/`ELITE_COUNT`) · 리스항구 제외. 세기 = 베이스 몹 HP ×25 · 공격 ×4 · Exp ×10 · 메소 ×10 · 크기 ×1.6/1.8/2.0(사냥터 1/2/3). 드랍 **전부 확정**(막타 귀속): 빅토리아 주화 2/4/6 · 꿈의 조각 2/4/6 · 낙인의 영혼석 1.
- ✅ **2026-09-23 구현(`a/plan-0914` B7 · PR #76 · 헤더+CANONICAL 동시 변경 · #40 공지 5776547020):** `EliteMonsterInfo` `#Note` 앞에 **`ScaleMul` · `CoinDrop` · `DreamDrop` · `SoulstoneDrop` · `Tier`** 추가 · `EliteSpawnTable` `#Note` 앞에 **`KillsPerElite`** 추가(`Chance` 는 0 유지 · 안 읽음). 행: 스폰되는 24종 × 1(EliteId = `E<BaseMonsterId>`) · 스폰표는 사냥터 맵마다 1행(`BaseMonsterId` 빈칸 = 그 맵 아무 몹). `MaterialId/Min/Max` 열은 안 읽음(값 0). `EliteMaterialInfo` 는 폐기(파일·헤더 유지).
- 스포너 위치: `Monster/EliteSpawner.mlua`(신규 · B-4 폴더 배정) — 정합성 검사 C6 의 로더 후보에 추가.
  - 구현(2026-09-23 B7): `EliteMonsterInfo` 24행(MapMonsters 에 스폰되는 몹 전부 · `E<Id>` · MaxHp×25 · AttackPower×4 · Exp×10 · Meso = 도감 동전 평균×10 · Tier = 몹 레벨 ≤12/≤20/그 외 · ScaleMul 1.6/1.8/2.0 · CoinDrop/DreamDrop 2/4/6 · SoulstoneDrop 1). `EliteSpawnTable` 15행(리스항구 제외 `_Hunt_` 맵 · `BaseMonsterId`/`EliteId` 빈칸 = 죽은 몹의 `E<Id>` · `Chance` 0 · `KillsPerElite` 25). `EliteMaterialInfo` 0행 유지. 런타임: `Farm/FarmReward.GiveRewards` 가 일반 몹(엘리트·미니언 제외) 사망마다 `Monster/EliteSpawner.OnMonsterKilled(map, monsterId, pos, mapEntity)` → 맵별 카운터(매치 시작 `ResetForMatch`) · `KillsPerElite`(매치 중 `DifficultyRule.ELITE_PER_KILLS` 우선) 배수마다 `ELITE_COUNT`(★5 2)마리를 죽은 자리에 스폰(`SpawnByModelId` 베이스 모델 · `Transform.Scale`×ScaleMul · `Monster.MaxHp` · `FarmReward.EliteId=E<Id>`). 엘리트 개체: Exp/메소 = `EliteMonsterInfo`(`FarmReward.Effective*`) · 접촉 공격 = `AttackPower`(`MonsterAttack.CalcDamage`) · GROUND/MONSTER 드롭은 일반 몹과 같이 굴리고(`DropTable` 무변경 · ★5 타 지역 ×2 는 `RegionMul`) · 확정 드랍은 `EliteSpawner.GrantEliteDrops`(빅토리아 주화 `SummonManager.GrantCoin` · 꿈의 조각 `DREAM_PIECE` · 낙인의 영혼석 `BRAND_SOULSTONE` · 막타 귀속 · 토스트). `EliteSpawnedEvent`/`EliteKilledEvent` 는 만들지 않음(로그 `[Elite]`).
  ⚠️ **엘리트 스포너가 생기기 전까지는 `MonsterInfo` 에 그대로 둔다** — 지금 빼면
  `KerningCity_Hunt_SewerApproach` 스폰이 죽는다

### A-2-16. 스킬 · 전직 3표 (2026-09-09 · B)

`feature/skill` 스냅샷의 `SkillDataSet` / `JobDataSet` 을 §0-1 규약(PascalCase · 첫 열 기본 키 · `#Note` 마지막)에 맞춰 이름과 열을 확정한다. 파일은 `feature/skill`(PR #32) 에서 Maker 로 만든다 — 이 PR 에는 헤더만 있다.

```
SkillInfo.csv
SkillId,Tab,SlotOrder,SkillName,IconRUID,Description,MaxLevel,SpCost,EffectPerLevel,Cooldown,
ReqJob,ReqTier,ReqLevel,Behavior,MpCost,BaseEffect,Range,Speed,Duration,HitCount,UseLimit,
EffectUnit,MaxStack,SecondaryEffect,SecondaryPerLevel,DurationPerLevel,CooldownPerLevel,BuffTag,SpawnOffsetY,
TertiaryEffect,TertiaryPerLevel,RecastMpCostMul,#Note

JobInfo.csv
JobId,Name,PrimaryStat,SecondaryStat,ApRatioStr,ApRatioDex,ApRatioInt,ApRatioLuk,#Note

JobTier.csv
JobId,Tier,ReqLevel,DisplayName,BaseAttack,#Note
```

| 표 | 열 | 설명 |
|---|---|---|
| `SkillInfo` | `SkillId` | **기본 키.** §0-4 대문자 문자열 `SK_W11` 꼴 |
| | `Tab` · `SlotOrder` | 스킬 창 탭(= 전직 차수 1~3) · 탭 안 순서 |
| | `ReqJob` · `ReqTier` · `ReqLevel` | 배우기 조건. `ReqJob` 은 `JobId` 열거값, `ReqLevel` 은 **매치 레벨**(`SummonManager.econ.level`) |
| | `Behavior` | `SkillBehavior` 열거값. 실행기(`SkillExecutors`)가 분기 |
| | `SpCost` · `MpCost` | 배울 때 SP(`_SummonManager:SpendSp`) · 시전마다 MP(`_SummonManager:SpendMp`) |
| | `BaseEffect` · `EffectPerLevel` · `EffectUnit` | 효과 = `BaseEffect + EffectPerLevel × (레벨−1)`, 단위는 `EffectUnit` |
| | `Cooldown` · `CooldownPerLevel` · `Duration` · `DurationPerLevel` | 초. `*PerLevel` 은 레벨당 증감(음수 가능) |
| | `Range` · `Speed` · `SpawnOffsetY` | world unit · unit/초 · 투사체 생성 높이 |
| | `HitCount` · `UseLimit` · `MaxStack` | 타수 · 매치당 사용 제한(0 = 무제한) · 스택 상한 |
| | `SecondaryEffect` · `SecondaryPerLevel` · `BuffTag` | 부효과 수치 · 레벨당 증감 · `BuffTag` 열거값(빈칸 = 없음) |
| | `TertiaryEffect` · `TertiaryPerLevel` | (2026-09-17 추가) **제3 효과 수치 · 레벨당 증감.** 부효과와 **독립된 열**이며 서로 유도하지 않는다. 한 스킬이 레벨마다 서로 다른 값 2개를 가져야 할 때 쓴다(에너지 차지 `SK_P21` = 부효과 이동속도 · 제3 효과 점프력). 안 쓰면 **0** · 레벨 공식은 부효과와 **같은 꼴** = `TertiaryEffect + TertiaryPerLevel × (레벨−1)`(`SkillDatabase.TertiaryAt`) — 한 표 안에 레벨 규칙이 둘 생기지 않게 맞췄고, 그래서 저장된 수치가 곧 **레벨 1 값**이다 |
| | `RecastMpCostMul` | (2026-09-17 추가) **재시전 1회의 MP 배율.** 재시전이 있는 스킬만 읽는다 — 재시전 비용 = `MpCost × RecastMpCostMul`, 초기 시전은 항상 `MpCost` 그대로. 에너지 차지 `SK_P21` = `0.25`(20 → 5). 🔴 **기본값은 0 이 아니라 `1`**(= 초기 시전과 같은 비용) — 배율에서 0 은 "없음"이 아니라 "무료"라서, 빈 칸을 0 으로 읽으면 열을 안 채운 재시전 스킬이 **조용히 무료**가 된다. 그래서 이 열만 `SkillDatabase.ParseNumberOr(… , 1)` 로 읽는다(다른 열은 빈 칸 = 0 이 맞으므로 `ParseNumber` 유지). `0` 은 "무료"라는 의도적 값으로 쓸 수 있다. 적용 순서는 **재시전 배율 먼저 · 매직 가드 배율 나중**, 내림은 마지막에 한 번(`SkillCaster` 클라 `Cast`/서버 `RequestCast` 양쪽 동일 식) |
| | `Description` | 🔴 쉼표가 들어가면 **큰따옴표로 감싼다**(정합성 검사는 첫 열만 보므로 안전) |
| `JobInfo` | `JobId` | **기본 키.** §0-2 `JobId` 6종. 🔴 **`NOVICE` 행 필수** — `StatService` 신규 유저 기본값. 없으면 하드코딩 폴백과 조용히 갈라진다 |
| | `Name` · `PrimaryStat` · `SecondaryStat` · `ApRatio*` | A 의 `StatService.LoadJobs` 계약 그대로(`PrimaryStat/SecondaryStat` 은 `StatId`, 비율은 합 1.0 소수). `NOVICE` 행 = STR/DEX 0.6/0.4 |
| `JobTier` | `JobId`+`Tier` | **복합 키.** `Tier` 0 은 `NOVICE` 한 행뿐, 1~3 은 5직업 × 3 |
| | `ReqLevel` · `DisplayName` · `BaseAttack` | 전직 가능 매치 레벨(10/20/30) · 표시명(`전사 1차`) · 직업 기본 공격력(임시 75 균일) |

- 빈 칸 = 기본값(§0-1 5). 로더(`SkillDatabase` · `JobDatabase`)는 모든 열에 폴백을 둔다
- 🔴 헤더 변경은 공지 후 단독 작업(§0-1 9). 열 추가는 `#Note` 앞에만


### A-2-16. `DispatchRule` — 파병 비용 · 상한 · 취소 기한 (2026-09-09)

A-3 에 예약돼 있던 것을 **열까지 확정해 여기로 옮긴다.** 기본 키는 예약대로 `Phase`.

```
DispatchRule.csv
Phase,MesoCost,VillageCap,CancelDeadlineSeconds,Enabled,#Note
```

| 열 | 뜻 |
|---|---|
| `Phase` | `MatchPhase` 4값. 기본 키 |
| `MesoCost` | 묶음 1개당 메소 비용. **0 = 무료** — 파병 비용 체계는 로드맵 미정 #10 이라 열만 확보하고 값은 0 |
| `VillageCap` | 한 마을이 **동시에 대기시킬 수 있는** 파병 묶음 상한 |
| `CancelDeadlineSeconds` | 다음 투입 몇 초 전까지 취소할 수 있는지. **0 = 투입 직전까지** |
| `Enabled` | `false` 면 그 페이즈에는 파병 접수를 막는다 |

- `PHASE1_PIONEER` 은 미니언이 없는 개척 구간이라 `Enabled=false`
- 파병은 **묶음 단위 FIFO** — 보유 수비대 앞에서부터 통째로 나간다. 몬스터 종류를 고르지 않는다
- 접수된 대기열은 `Dispatch/DispatchService` 의 **서버 메모리에만** 있고 저장하지 않는다
  (매치가 끝나면 사라진다 — `MinionFlowState` 와 같은 성격)

### A-2-17. `VillageFacilityTrait` — 마을별 시설 특성 (2026-09-13 · WO-021 S1)

> ✅ **2026-09-13 등록** — `RootDesk/MyDesk/VillageFacilityTrait.csv` 5행(S1 = 헤네시스·커닝·페리온) + `.userdataset` + `check-integrity` CANONICAL/PK.
> 배경: 기획-원문 §"마을별 넥서스, 억제기, 포탑의 특성" 이 통째로 미구현이었다. 사용자 지시(2026-09-09)로 M1 안으로 끌어왔다.
> 🔴 **마을별 분기를 코드에 박지 않고 이 표 하나로 한다.** 5마을 × 2시설 × 3레벨이 코드에 흩어지면 손을 못 댄다.

```
VillageId,Stage,TraitKey,Lv1,Lv2,Lv3,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `VillageId` + `Stage` + `TraitKey` | **복합키.** `Stage` = `TOWER`/`SUPPRESSOR`/`CORE`(`TowerConfig` 와 같은 열거값) · `TraitKey` = §0-2 |
| `Lv1` · `Lv2` · `Lv3` | 시설 **자신의** Lv 별 값. `Lv2`/`Lv3` 빈 칸 = `Lv1` 값. 뜻은 `TraitKey` 마다 다르다(아래) |
| `Enabled` | `false` = 행이 없는 것과 같다 |

| `TraitKey` | 값의 뜻 | 적용 위치 |
|---|---|---|
| `SUPPRESSOR_ATTACK` | 억제기가 공격한다. 데미지 = **포탑 데미지 표(억제기 Lv 로 읽음) × 값**. 사거리·쿨은 `TowerConfig` SUPPRESSOR 행의 `Range`/`AttackSpeed`(F-1 확정 2026-09-13: 포탑과 동일 값 기입 · 0/빈칸이면 같은 Lv 포탑 값 차용) | `Lane/LaneFacilityService.ApplyCombat` |
| `MAX_TARGETS` | 한 번의 공격이 때리는 최대 대상 수(**가까운 순**). 행 없음/0 = 무제한 | `Faction/FactionAttack.MaxTargets` |
| `TOWER_SINGLE_MUL` | 포탑이 **원거리 단일**(대상 1 · 상자를 표적에 좁게)로 기존 데미지 × 값 | `ApplyCombat` → `FactionAttack` |
| `AURA_SPEED` · `AURA_ATK` · `AURA_REGEN` | 범위 내 아군(플레이어+수비대) 이속 +값 / 공격력 +값 / 초당 회복 값. 범위 = **시설 가로너비(`HitWidth` 2.0) × `AuraWidthMul` 3 = 가로 6.0 · 세로 3.0 · 시설 중심 대칭 · 세로 중심 바닥 +1.0**(F-2 확정 2026-09-13) | `Faction/FactionAuraController`(억제기에 부착) · 플레이어 이속은 `Stat/StatService.SetAuraSpeedMul` |
| `REFLECT` | 받은 피해 × 값을 공격자에게 되돌림(플레이어에게도 같은 배율). **무적 게이트 통과분만** · 상한 없음(F-6 확정 2026-09-13 · `LaneFacility.ReflectCap 0` 은 안전망) | `Lane/LaneFacility.HandleHitEvent` → `FactionAttack.DoReflect` |

S1 5행(전부 기획 원문 값): `HENESYS SUPPRESSOR SUPPRESSOR_ATTACK 2` · `HENESYS SUPPRESSOR MAX_TARGETS 3` · `KERNING TOWER TOWER_SINGLE_MUL 5` · `KERNING SUPPRESSOR AURA_SPEED 0.10/0.20/0.30` · `PERION SUPPRESSOR REFLECT 10`.
**S2**(엘리니아 `AURA_ATK` · 노틸러스 `AURA_REGEN` + 포탑 스플래시 `TOWER_SPLASH`)는 레인 지형 6맵 뒤 **행 추가**로 온다 — 오라 2종은 코드 수정 없음, 스플래시만 `FactionAttack` 2단 판정 추가.
✅ **S2 값 확정(2026-09-22 · `a/plan-0914` B3b · 사용자):** `ELLINIA SUPPRESSOR AURA_ATK 0.10/0.20/0.30`(플레이어+수비대 · 범위는 `AURA_SPEED` 와 동일) · `NAUTILUS SUPPRESSOR AURA_REGEN 25/50/100`(고정 HP/초 · **억제기 본인 포함** — `FactionAuraController` 가 자기를 제외하면 포함하도록) · `NAUTILUS TOWER TOWER_SPLASH 1.5/1.5/1.5`(값 = 폭발 반경 · 주 표적 ×2 · 반경 안 ×1 · 현행 3발 분할(`SplitHits`)은 끈다). 넥서스는 특성 없음 · Lv1 고정. 🔴 **억제기 파괴 폭발**(전 마을 공통 · 반경 3.0 · **미니언에게만 10,000**)은 특성이 아니라 `LaneFacility` 공통 규칙(B3) — 연출은 ICBM `special` `807816a6794a4241b09572b384f475c0`(×0.8) + `_audio/Hit` `5e8b2bcc46c848a7b50a48710988d700`.
✅ **2026-09-23 구현(B3b · PR #72)**: 행 3개 추가(`ELLINIA SUPPRESSOR AURA_ATK` · `NAUTILUS SUPPRESSOR AURA_REGEN` · `NAUTILUS TOWER TOWER_SPLASH 1.5`). `AURA_ATK` 는 플레이어에게도 — `Stat/StatService.CalcPlayerDamage` 가 `Buff.AtkMul`(오라가 붙인 Buff)을 곱한다. `AURA_REGEN` 본인 = `FactionAuraController.RegenSelf`(시설 오라만 true) → `ApplyRegen` 이 `LaneFacility` 를 보면 `LaneStateService.HealFacility`(원장 HP · 소수 이월 · 2초 누적 로그 `[Regen]`). `TOWER_SPLASH` = `FactionAttack.SplashRadius`(표 값) · `SplashPrimaryMul` 2 · `SplashMul` 1 — `HitTarget`/`DoAttack` 뒤 `SplashAround`(주 표적 중심 원 · `[Splash]` 로그) · `ApplyCombat` 이 표적 1 로 고정. `FacilityAttackFx` NAUTILUS TOWER 3행 `SplitHits=false`(포탄 3발은 전부 주 표적에).
읽는 곳: `Lane/LaneStateService.LoadTraitDef` → `TraitValue(villageId, stage, key, lv)` / `HasTrait`.

> 🔴 **표가 비면 현행 동작과 똑같아야 한다.** `FactionAttack.MaxTargets` 기본 0(무제한 · 포탑 광역 유지) · 억제기 `TurretAI` 는 공격 특성이 있을 때만 켜진다.

### A-2-18. `FacilityAttackFx` — 시설 공격 연출 (2026-09-14 · WO-025)

> ✅ **2026-09-14 등록** — `RootDesk/MyDesk/FacilityAttackFx.csv` 15행(마을 5 × 포탑 Lv3) + `.userdataset` + `check-integrity` CANONICAL/PK.
> 사용자 배정(2026-09-14): 헤네시스 화살 3~4/6~8/6~8 · 커닝 표창→수리검→대형 수리검(빗나가면 땅에 0.35초) · 엘리니아 서클 오브 썬더 → VI → 엘리멘탈 블래스트(시전·적중 분리) · 노틸러스 포탄 3발 0.1초 간격 · 페리온 궁니르 디센트 일반/special/둘 다.
> 🔴 **피해는 연출이 도착하는 순간에 들어간다** (2026-09-14 개정 · 사용자 "유도탄은 맞아야 피격"). 행이 있는 시설은 `TurretAI` 가 `LaneAttackFx.Fire` 를 부르고,
> `Fire` 는 `FactionAttack.SelectTargets`(DoAttack 과 같은 상자 · 피해 없음)로 표적을 고른 뒤 연출을 띄우고 **`FlightSec` 뒤** `Land` 가 살아 있는 표적마다
> `FactionAttack.HitTarget`(표적당 1회 · 발사체 수와 무관)으로 피해를 준다. 발사체(`LaneShot`)는 그 표적을 유도 추적한다. 행이 없으면 옛날처럼 `DoAttack` 즉시 판정 · 연출 없음.

```
VillageId,Stage,Level,Mode,BallRuid,Ball2Ruid,HitRuid,CastRuid,CountMin,CountMax,IntervalSec,Scale,HitScale,FlightSec,ArcHeight,StickSec,LaunchOffsetX,LaunchOffsetY,CastOffsetX,CastOffsetY,CastFlipX,FaceDeg,Enabled,SplitHits,HitStartFrame,HitOffsetX,HitOffsetY,HitFlipX,#Note
```

| 열 | 설명 |
|---|---|
| `VillageId` + `Stage` + `Level` | **복합키.** `Stage` = `TOWER`/`SUPPRESSOR` · `Level` 1~3 (시설 자신의 Lv · 없는 Lv 는 바로 아래 행으로 내려간다) |
| `Mode` | `SHOT`(발사체 → 적중) · `CAST_HIT`(시전 이펙트 + 적중 이펙트만 · 발사체 없음) |
| `BallRuid` · `Ball2Ruid` | 날아가는 그림(animationclip/sprite). `Ball2Ruid` 가 있으면 짝수 번째 발사체는 그 그림 |
| `HitRuid` · `CastRuid` | 도착 적중(표적 위치 + `HitOffsetX/Y`) · 시전(시설 위치 + `CastOffsetX`(2026-09-15 추가 · `CastOffsetY` 앞)/`CastOffsetY`) 이펙트. `_EffectService:PlayEffect` |
| `CountMin` · `CountMax` | 한 스윙에 쏘는 발사체 수(범위 안 랜덤). 표적이 더 적으면 나눠 맞는다 |
| `IntervalSec` | 발사 간격(초). 0 = 동시(출발점 ±0.15 · 포물선 ±25% 흩뿌림) |
| `Scale` · `HitScale` | 발사체(시전) · 적중 그림 배율. 1유닛 = 100px 기준 어림값 → 실물 보고 조정 |
| `FlightSec` · `ArcHeight` | 비행 시간(거리 무관 고정) **= 피해가 들어가는 시각**(발사 뒤 이만큼) · **낙하 곡률**(2026-09-14 의미 변경 · 사용자 "위로 솟지 말고 수평 발사 뒤 떨어지는 포물선"): `0` = 직선(페리온 창), `>0` = 수평으로 나가 `u^(1+값)` 으로 표적 높이까지 떨어진다(클수록 끝에서 급강하 · ±25% 흩뿌림). `CAST_HIT` 에선 `FlightSec` = 시전(마법진) → 적중(번개 · 표적 위치)+피해 사이 지연 |
| `StickSec` | 표적을 잃었을 때 발 높이에 박혀 있는 시간. 0 = 그 자리에서 적중 이펙트 |
| `LaunchOffsetX` | (2026-09-15 추가 · `LaunchOffsetY` 앞) 출발점 x 보정(유닛 · + 오른쪽). **월드 기준** — 시설 그림 `FlipX`(`FacilitySprite`)와 무관(마을마다 그림 방향이 고정이라 원하는 부호를 그대로 적는다). 빈칸 = 0. 헤네시스 포탑·억제기 석궁 위치 맞추기용 |
| `LaunchOffsetY` | 출발점 = 시설 위치 + 값. **빈칸 = 시설 그림 절반 높이(`GroundOffset`) = 머리 위** |
| `FaceDeg` | ball 그림이 원래 향하는 각도(180 왼쪽 · 0 오른쪽 · 90 위 · 270 아래). 진행 방향 회전 기준 |
| `SplitHits` | (2026-09-14 추가) `true` 면 **표적마다 `CountMin~CountMax` 개를 쏘고 발사체마다 도착 시 1/개수 피해** — 표적마다 합은 온전한 한 방(노틸러스 3발 3분할 · 사용자 결정 · 표적 5이면 15발이 3파로). `false`/빈칸 = 발사체 수 = max(개수, 표적 수) · 첫 발 도착 시각에 표적당 1회 온전 피해. `SHOT` 전용 |
| `HitStartFrame` | (2026-09-15 추가 · `#Note` 앞) 적중 클립(`HitRuid`)을 **이 프레임(0-based)부터만** 보여 준다. `0`/빈칸 = 전체(`PlayEffect`). `>0` 이면 `Lane/LaneFxLogic.PlayClip` 이 `laneshot` 모델을 스폰해 `SpriteRendererComponent.StartFrameIndex` 로 재생하고 끝 프레임 이벤트(안전 3초)에 지운다. 엘리니아 Lv3 = 엘리멘탈 블래스트 `effect0`(39프레임) 26 → 뒷 13프레임. 지금은 `CAST_HIT` 적중에만 적용(발사체 도착 hit 은 전체 재생) |
| `HitOffsetX` · `HitOffsetY` | (2026-09-15 추가 · `#Note` 앞) 적중 이펙트 위치 보정(유닛 · 표적 몸통 = 발 +0.5 기준). 🔴 메이플 이펙트 클립은 피벗이 **시전자 원점**이라 그림 중심이 표적에서 벗어난다 — 리소스 API `payload.frames[i].pivot`(px · y 는 아래에서)로 `(w/2−px, h/2−py)×배율/100` 을 재서 그 반대 부호를 넣으면 그림 중심이 표적에 온다. `CAST_HIT` 적중과 발사체 도착 hit 둘 다 적용. 엘리니아 Lv1·2 는 사용자 지시로 `−0.5` |
| `CastFlipX` · `HitFlipX` | (2026-09-15 추가 · 각각 `CastOffsetY` 뒤 / `HitOffsetY` 뒤) `true` 면 시전/적중 이펙트를 좌우 반전(x 배율 음수 · 프레임 클립은 `FlipX`). 메이플 이펙트는 왼쪽을 보므로 오른쪽 적 쪽으로 돌릴 때. 빈칸 = false |

읽는 곳: `Lane/LaneStateService.LoadFxDef` → `AttackFx(villageId, stage, lv)` → `Lane/LaneFacilityService.ApplyAttackFx` → `Lane/LaneAttackFx`(시설 컴포넌트 · `Fire`/`Land` · 발사 규칙 + 지연 피해) → `Lane/LaneShot`(발사체 · `Models/Effects/LaneShot.model`) · 피해는 `Faction/FactionAttack.SelectTargets`/`HitTarget`.

### A-2-19. `DropTable` — 드롭 (2026-09-22 · `a/plan-0914` B2/B7 · WO-027 §6)

```
SourceKind,SourceId,ItemId,Chance,CountMin,CountMax,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `SourceKind` + `SourceId` + `ItemId` | **복합키.** `SourceKind` = `DropSourceKind`(§0-2). `GROUND` 면 `SourceId` = 사냥터 번호 `1`/`2`/`3`(그 사냥터의 모든 일반몹 공통) · `MONSTER` 면 도감 ID(개별 예외) · `ELITE` 면 `EliteId` |
| `ItemId` | `ItemInfo` `MATERIAL`(보석 `GEM_*` · 지역 재화 `REGION_*` · 몬스터별 `MAT_*` · `VICTORIA_COIN`은 카운터라 `ItemId=VICTORIA_COIN` 특수 처리 · `DREAM_PIECE` · `BRAND_SOULSTONE`) |
| `Chance` · `CountMin` · `CountMax` | 확률(소수) · 개수 범위. **확정 드롭 = `Chance` 1.0** |

- ✅ **2026-09-22 등록·구현(B2 `a/p0914-2-balance-data`)** — 6행: 보석 사냥터 1/2/3 = 20% / 35% / 55% ×1 · 지역 재화 35% / 75% / **3~4 확정**(3 확정 + 50% 1개 = `CountMin 3, CountMax 4` · 같은 키 행 중복을 피한 표현). 몬스터별 재화(`MAT_*`)는 `ItemInfo` 행 자체가 아직 없어 **B5 에서 행 추가**. 엘리트(주화 2/4/6 · 꿈의 조각 2/4/6 · 영혼석 1 · 1.0)는 B7.
- **`ItemId` 토큰 2개**(`Farm/DropTableLogic.ResolveItem`): `GEM_RANDOM` = 12종 보석 중 무작위 1종 · `REGION_LOCAL` = 그 맵 지역의 지역 재화(`ItemCatalog.ShopKeyOfMap` → `REGION_*` · 매핑은 `DropTableLogic.RegionItems`). 그 외는 실제 `ItemId`.
- 규칙: **막타 친 플레이어**에게(`FarmReward` · 2026-09-22 막타 복귀) **인벤토리 직접 지급 + 토스트 1줄**(바닥 드롭 없음 · 동전만 바닥). 사냥터 번호는 `VillageConfig` 의 `Lane1MapName`=1 · `Lane2MapName`=2 · `RearMapName`=3 으로 정하므로 리스항구·마을·보스 맵은 GROUND 드롭이 없다(리스항구 = 메소만). 미니언(`MonsterId 0`)은 재료 없음. ★5 타 지역 재화 ×2 는 `DropTableLogic.RegionMul` 훅 — B6 의 `DifficultyService.REGION_DROP_MUL` 이 채운다(지금은 1).
- `GemDropTable`(A-2-8b) 대체(코드 소비처가 원래 없었다). 읽는 곳: `Farm/DropTableLogic`(신규 `@Logic`) ← `Farm/FarmReward.GiveRewards`.

### A-2-20. `MonsterRecruit` — 수비대 모집 (2026-09-22 · `a/plan-0914` B5 · A-3 예약 해소 · WO-027 §7-1)

```
MonsterId,Tier,MaterialItemId,MaterialCount,BundleSize,GuardHp,GuardAttack,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `MonsterId` | **키.** 모집 가능한 사냥 몹(`MonsterInfo.Id` · 지역당 3종 = 15행) |
| `Tier` | `RecruitTier`(§0-2) 1/2/3 = 사냥터 1/2/3 |
| `MaterialItemId` · `MaterialCount` | 그 몹의 몬스터별 재화(`MAT_*`) · **8** |
| `BundleSize` | 묶음 마릿수 **5**(기획의 10 → 5 · 사용자 2026-09-22) · 10칸 = 최대 50마리 |
| `GuardHp` · `GuardAttack` | 수비대 개체 기본값 = **대응 페이즈 첫 웨이브 미니언의 절반**(티어 1 500/150 · 2 1,600/315 · 3 2,500/1,850) · 훈련 `StatMul` 을 위에 곱함 · **페이즈 갱신 없음** |

- `SummonUnits`(메소 모집 임시 경로) 는 B5 부터 안 읽음(파일 유지). `LaneStateService.SpawnBundle(…, 8, …)` 의 8 → `BundleSize`.
- ✅ **2026-09-23 구현(B5 · PR #74)**: 14행(커닝은 Lv24 사냥터가 없어 2종) — HENESYS 1210102/2220100/5130102 · ELLINIA 2230100/3230100/4230100 · KERNING 1120100/2130103 · PERION 3230300/130100/2230102 · NAUTILUS 1210100/1210101/1210104. 재료 = `ItemInfo` `MAT_<MonsterId>` 14행(MATERIAL · 아이콘 = 몹 아이콘 임시) · 드롭 = `DropTable` MONSTER 14행(사냥터 1/2/3 = 0.25/0.35/0.50). 읽는 곳: `LaneStateService.LoadRecruitDef/RecruitRow` → `RequestRecruit`(재료·묶음) · `DefenderService.SpawnOne`(개체 스탯 × 훈련) · 모집 창 `U` 행에 재료 이름·보유·묶음 추가. 도감 해금(Lv1) 게이트는 유지.
- 🔴 **사냥터 번호(`DropTable.GROUND SourceId`) = 그 맵 몹의 레벨대**(≤12 → 1 · ≤20 → 2 · 그 외 3 · `DropTableLogic.EnsureLoaded` 가 `MonsterCatalog.GetMapSpawns` 로 정정). 레인 역할(Lane1/Lane2/Rear)로 매기던 B2 규칙은 헤네시스 외 마을에서 레벨과 안 맞았다(엘리니아 Rear=Lv10 · 페리온 Lane2=Lv17).

### A-2-21. `RankReward` — 매치 순위 보상 (2026-09-22 · `a/plan-0914` B8 · WO-027 §8 · 9/14 기획 표)

```
Rank,Difficulty,Hearts,AccountExp,Condition,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Rank` + `Difficulty` | **복합키.** `Rank` 1~5(5 = 5위 이하) · `Difficulty` 1/3/5 |
| `Hearts` | 발록의 심장(9/14 표: 1위 2×★ · 2위 1×★ · 3위 0.5×★ 반올림 · 4·5위 0.5×★ 반올림 — `Condition` 충족 시, 아니면 0) |
| `AccountExp` | 계정 경험치(초안 ★3: 120/100/80/60/50 · ★1 ×0.5 · ★5 ×2) |
| `Condition` | 빈칸 / `BALROG_HP_HALF`(매치 종료 시 발록 HP 가 절반 이하여야 지급) |

- **탈락자(넥서스 파괴) = 최하위 순위로 지급 · 중도 이탈자 = 0**(사용자 2026-09-22). 지급은 `RewardGrantedEvent`(단일 통로 · `SettlementId` = 매치ID+userId) 로만.
- 계정 레벨 곡선(초안: 다음 레벨 = 80 + 20×(Lv−1)) · 레벨업 심장(Lv2~4 1 · 5~12 2 · 13+ 3) · 해금(★3 = 계정 Lv5 · ★5 = Lv13)은 `DifficultyRule.UNLOCK_ACCOUNT_LV` + `AccountLevelService` property. ~~도감·칭호 실물은 M2.~~ → **M1 로 당김**(2026-09-26 기록·칭호 등록서 · A-2-23~26).

✅ **2026-09-23 구현(`a/plan-0914` B8 · PR #77):** `RankReward.csv` 15행(Rank 1~5 × ★1/3/5 · Hearts 1위 2×★ · 2위 1×★ · 3위~5위 0.5×★ 반올림 · 4·5위 `BALROG_HP_HALF` · AccountExp ★3 120/100/80/60/50 · ★1 ×0.5 · ★5 ×2) + `.userdataset`. 정산 = `Match/RankRewardService.Settle(rows, difficulty, leftUsers)`(신규 @Logic · `MatchSessionLogic.ShowResult` 가 정렬한 순서 = 순위 · 탈락 행 = Rank 5 · `leftUsers` = 0 · 매치당 1회 `settled` · `StartMatch` 가 `ResetForMatch`) → 심장 `AccountData.AddBalrogHeart` · 계정 경험치 `Progression/AccountLevelService.AddExp`(신규 @Logic · 누적 `AccountData.exp` · 레벨 = `LevelForExp` · 필요치 80+20×(Lv−1) · 레벨업 심장 Lv2~4 1 / 5~12 2 / 13+ 3 · 토스트 · `[Account]` 로그). `RewardGrantedEvent` 는 코드에 없어 직접 지급 + `[Rank]` 로그. `ACCOUNT_EXP_MUL` 은 표가 이미 ★별 값이라 순위 보상에는 곱하지 않는다. 결과 화면 `reward=` = "n위 보상: 발록의 심장 +h (보유 t) · 계정 경험치 +e (Lva → Lvb)" · 로비 `aprog=lv|현재|필요` → 하트 라벨에 "계정 Lv". 저장: `AccountProfile` **SchemaVersion 3** = `account_exp`(0) · `account_guideOff`(false · B10) · `Deserialize` 는 없는 키를 기본값으로(역호환) · `AccountStorageLogic.SchemaVersion=3`.

### A-2-22. `GuideStep` — ★1 안내 네비게이션 (2026-09-22 · `a/plan-0914` B10 · WO-028)

```
StepKey,Kind,Text,MarkerMap,Once,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `StepKey` | **키.** 목표 `G1`~`G14` · 토스트 `T1`~`T18` · 체크 `C1`~`C13`(WO-028 §1~§3) |
| `Kind` | `GuideStepKind`(§0-2) |
| `Text` | 표시 문구(줄바꿈 없음 · 진행률 자리는 `{n}/{max}` 토큰) — 코드 수정 없이 문구 교체 |
| `MarkerMap` | 월드맵 목표 마커를 둘 `MapName`(`GOAL` 전용 · 빈칸 = 마커 없음 · `@OWN_LANE1` 같은 자리표시자는 서버가 내 마을로 치환) |
| `Once` | `TOAST` 전용 · `true` = 매치당 1회 |

✅ **2026-09-23 구현(`a/plan-0914` B10 · PR #79):** `GuideStep.csv` 45행(G1~G14 + G7B(3P 웨이브 문구) · T1~T18 · C1~C13) + `.userdataset`. 서버 `Guide/GuideService`(1초 폴링으로 레벨·직업(`StatService.JobOf`)·마을·시설·재화·페이즈·웨이브 창을 읽어 단계 선택 `PickGoal` · 진행률 치환 · 마커 자리표시자 `@OWN_VILLAGE/@OWN_LANE1/@OWN_LANE2/@OWN_REAR/@OWN_BOSS/@EMPTY_VILLAGES` 치환 · 토스트 Once · 체크리스트 CSV) · 훅 7곳(EliteSpawner 스폰/처치 · LaneStateService 파괴 · BossRewardService · BalrogRoomService 입장/처치 · EnhanceService). 클라 `Guide/GuideUIController` + 새 그룹 `GuideGroup`(상단 목표 바 = 클릭 시 체크리스트 펼침 · "안내 끄기" · 꺼두면 "안내 켜기" 칩) · 마커 `WorldMapController.ShowGoalMarkers(csv)/ClearGoalMarkers`(GoalMarker_0~5 · 열 때 재적용). 토글은 `RequestToggle` → `AccountData.SetGuideOff`(v3 저장). 미구현: T16(플레이어 HP 조회 API 없음 · B PlayerHit) · 스탯창 자동획득 옆 토글 자리는 Footer 가 꽉 차 목표 바로 대체(UI 일괄 때 이동).

- 게이트: `MatchSessionLogic.Difficulty == 1` 이고 `AccountProfile.account_guideOff == false`. 상태·전이는 서버 `Guide/GuideService`(유저별 현재 단계 · 1회 플래그 · 체크리스트) → 클라 문자열 RPC(any 금지). UI = 새 `GuideGroup`(상단 목표 바 + 클릭 시 체크리스트 펼침 + 진행률) · 토스트는 기존 `ToastGroup` · 마커는 `WorldMapController.ShowGoalMarker(mapName)`(HereMarker 와 같은 방식).

### A-2-23. `AchievementConfig` — 업적 (2026-09-26 · `a/m1-finish` · WO-031 §5 · A-3 예약 해소)

```
AchievementId,Name,Desc,CondType,CondParam,Target,AccountExp,TitleId,SortOrder,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `AchievementId` | **키.** `ACH_*` |
| `Name` · `Desc` | 계정 창 표시 이름 · 조건 설명 문구 |
| `CondType` | `AchievementCondType`(§0-2) |
| `CondParam` | 조건 보조값(정수 · 빈칸 = 0). `RANK_FIRST` = ★(`0` = 무관) · 그 외 `0` |
| `Target` | 달성 목표 수(정수 ≥ 1). `DEX_FOUND` / `DEX_BOSS` 는 `0` = 도감 전 종 |
| `AccountExp` | 달성 즉시 계정 경험치(`AccountLevelService.AddExp` · `SourceType.ACHIEVEMENT`) |
| `TitleId` | 달성 즉시 주는 칭호(`TitleInfo.TitleId` · 빈칸 = 없음) |
| `SortOrder` | 창 정렬(오름차순) |

- 진행 = 계정 누적 카운터(`AccountAchievement.c["<CondType>:<CondParam>"]` · A-4) · 달성 = 목표 이상이 되는 순간 서버가 **자동 지급** + `d[AchievementId]` 기록(두 번 안 준다 · 창에는 "받음"). 도감 도출 조건(`DEX_*`)은 도감이 바뀔 때 다시 센다. `ENHANCE_ALL_MAX` 는 매치 안에서 판정해 만족한 순간 카운터 +1.
- 행을 나중에 추가하면 이미 목표를 넘긴 계정은 **로드 직후 한 번** 도는 판정에서 받는다.

### A-2-24. `TitleInfo` — 칭호 (2026-09-26 · `a/m1-finish` · WO-031 §5)

```
TitleId,Name,Desc,SortOrder,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `TitleId` | **키.** `TTL_*` |
| `Name` | 표시 이름 — 머리 위 이름표 **"칭호 닉네임"** 의 앞부분(8자 안팎 권장) |
| `Desc` | 얻는 법 문구(계정 창) |
| `SortOrder` | 창 정렬(오름차순) |

- 보유 = `AccountProfile.account_titles` · 장착 = `account_title`(하나 · 빈칸 = 없음). 장착 변경은 로비 계정 창 → 서버 검증(보유한 칭호만) → 저장 + 이름표 갱신. 이름표는 서버가 룸 입장 · 장착 변경 때 `NameTagComponent.Name` 에 쓴다. `Enabled=false` 칭호는 목록·이름표에서 숨긴다(보유 기록은 남김).
- 표시처: 머리 위 이름표 · 생존자 통계 상세(`PublicPlayerSummary.TitleName`) · 매치 결과 화면. 목록·장착은 로비 계정 창에서만.

### A-2-25. `CollectionReward` — 계정 도감 단계 보상 (2026-09-26 · `a/m1-finish` · WO-031 §4)

```
Kind,Tier,Kills,AccountExp,Name,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Kind` + `Tier` | **복합키.** `CollectionKind`(§0-2) · 단계 `1`, `2`, `3`… |
| `Kills` | 그 단계에 필요한 누적 처치 수(`MONSTER` = 그 종 · `ELITE` = 그 종의 자이언트 · `BOSS` = 그 보스) |
| `AccountExp` | 도달 즉시 계정 경험치(`SourceType.COLLECTION`) |
| `Name` | 단계 이름(발견 · 숙련 · 정복 …) |

- 도감 칸 = 일반 종(`MonsterInfo.Id` · `MapMonsters` 에 나오는 종) + 보스(`BossInfo.BossId`). 처치 인정: 일반·자이언트 = **막타**(`FarmReward` 보상 귀속과 같음) · 보스 = **최다 피해 1명**(보스 보상 · `BossKillCount` 와 같은 기준 · 사용자 결정 2026-09-26) · 발록 = 선취 승자. 자이언트 처치는 그 종 `MONSTER` 처치 수에도 +1. 미니언(`MonsterId 0`)은 도감에 없다.
- 한 번에 여러 단계를 넘으면 넘은 단계를 **전부** 지급. 칸마다 받은 최고 단계를 저장(A-4 `tm` / `te` / `tb`) — 표에 단계를 늘려도 이미 받은 단계는 다시 안 준다.

### A-2-26. `GrowthPoint` — 마을 성장도 점수 (2026-09-26 · `a/m1-finish` · WO-031 §6 · GDD §4.12)

```
ActionType,Points,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `ActionType` | **키.** `GrowthActionType`(§0-2) |
| `Points` | 한 번 가산할 점수 |

- 같은 **액션 키**(유형 + 대상 + 단계 · 예 `FACILITY_LEVEL:TOWER1:3`)는 매치당 **한 번만** 가산(GDD §4.12 — 같은 소비 반복으로 무한 획득 금지). 공개는 `PublicPlayerSummary.VillageGrowth`(정수)로만 — 숨은 자원량을 역산하지 못하게 액션 단위 점수다. 매치 원장(서버 메모리 · `Match/MatchTallyService`) · 저장 안 함.

---

## A-3. Phase 2+ 표 (이름·키·용도만 예약)

> 열은 해당 Phase 착수 시 이 문서에 추가한다. **미리 파일을 만들지 않는다.** `GemInfo`와 `GemDropTable`은 플레이어 장비·강화 계약으로 승격되어 A-2-8b가 정본이다.
> (예외: 엘리트 3표는 2026-09-02 에 구조만 먼저 확정해 **A-2-15** 로 옮겼다 — 사용자 지시)

| 표 | 키 | 용도 |
|---|---|---|
| ~~`DispatchRule`~~ | ~~`Phase`~~ | ✅ 2026-09-09 열 확정 → **A-2-16** |
| ~~`CraftRecipe`~~ | ~~`RecipeId`~~ | ✅ 2026-09-06 열 확정 → **A-2-8c** |
| ~~`MonsterRecruit`~~ | ~~`MonsterTypeId`~~ | ✅ 2026-09-22 열 확정 → **A-2-20** (`MonsterId` 키 · 재료 8 · 묶음 5) |
| `MonsterTraining` | `MonsterTypeId` + `Level` | 시간, 비용, 병과 보너스 |
| ~~`AchievementConfig`~~ | ~~`AchievementId`~~ | ✅ 2026-09-26 열 확정 → **A-2-23** (+ `TitleInfo` A-2-24 · `CollectionReward` A-2-25 · `GrowthPoint` A-2-26) |

### 🔴 두 성장 축이 갈린다 (2026-08-29 확정)

| 축 | 재화 | 어디서 |
|---|---|---|
| **플레이어 강화** | 메소 + **보석** | `WORKSHOP_ENHANCE`(강화 장인) — 보석 종류가 오르는 능력치를 결정 |
| **방어 강화** | 메소 + **엘리트 재료** | `DEFENSE_TOWER`(포탑 관리관) — 포탑 3종 + 코어 |

**보석은 포탑에 쓰지 않고, 엘리트 재료는 장비에 쓰지 않는다.** 이 분리가 "엘리트를 잡아야만 후반 방어가 된다"는 압박을 만든다.

---

## A-3-1. 🔴 진영 번호 체계 (2026-09-02 확정)

`script.Faction.Team` 은 **문자열이지만 번호로 쓴다.** `FactionLogic` 이 정본이다.

| 대역 | 뜻 |
|---|---|
| `1` ~ `5` | **플레이어 캐릭터.** 캐릭터 본인만 들어간다 |
| `11` ~ `15` | **미니언.** `11` = "플레이어 1 을 치러 온 미니언" |
| `21` ~ `25` | **방어시설·자기 유닛.** `21` = "플레이어 1 의 터렛·수비 유닛" |
| `Monster` | **공통 몬스터.** 야생 몹 · 보스 · 파밍 몹 |
| `Neutral` | 그 외 전부. 공격도 버프도 안 통한다 |

**소유자 = 번호 % 10.** `1` · `11` · `21` 은 전부 플레이어 1 의 것이다.
**지정 대상은 별도 칸에 저장하지 않는다 — 팀 번호 자체가 지정 대상이다.**

### 관계 규칙

| A | B | 관계 | 왜 |
|---|---|---|---|
| `1` | `2`~`5` | **ALLY** | 🔴 플레이어 **캐릭터끼리는** 공격 불가. 경쟁은 사냥 스틸로만 |
| `1` | `21` | ALLY | 자기 시설 |
| `1` | `22`~`25` | **ENEMY** | 🔴 **남의 방어시설은 때릴 수 있다.** 보정은 시설 쪽 시스템이 갖는다 |
| `11` | `1` · `21` | ENEMY | 지정 대상의 것 |
| `11` | `2`~`5` · `22`~`25` | ALLY | 지정 대상이 아니면 건드리지 않는다 |
| `11` | `12`~`15` | ALLY | 미니언끼리는 안 싸운다 |
| `21` | `22`~`25` | ENEMY | 서로 다른 진영의 시설 |
| `Monster` | 전부 | ENEMY | 야생 몹·보스는 모두와 적대 |
| `Neutral` | 전부 | NEUTRAL | 공격·버프 전부 차단 |

같은 미니언이 상대에 따라 적/아군이 갈리므로 **팀 문자열 비교만으로는 표현이 안 된다.**
`FactionLogic.BandRelation` 특례가 일반 규칙(팀 다르면 적대)보다 먼저 적용된다.

### 진영이 없는 엔티티

`script.Faction` 을 가진 모델은 **10종뿐**(미니언 7 · 터렛 2 · 중립 1). 나머지 100종은 `GetTeam` 이 자동 판정한다:

1. `script.Faction` 이 있으면 그 팀 — `NormalizeTeam` 이 예전 이름을 흡수한다 (**`TeamA`→`21`**, `TeamB`→`11`)
2. `PlayerComponent` 가 있으면 `PlayerTeam` (기본 `1`)
3. `script.Monster` 또는 `script.FarmReward` 가 있으면 `Monster`
4. 그 외 `Neutral`

> 🔴 **3번이 없으면 `Models/Monsters` 82종(일반 몹 76 + 보스 8)이 전부 `Neutral` 로 떨어져 플레이어가 아무도 못 때린다.**
> 실제로 그 상태였다. 모델 82개에 컴포넌트를 붙이는 대신 판정 한 곳에서 해결한다.

> ⚠️ `TeamA` 는 `1` 이 아니라 **`21`** 로 간다. 그건 플레이어 캐릭터가 아니라 그 진영의 유닛·터렛이었다.
> `FactionAuraController.PlayerTeam` 만은 캐릭터 진영이라 `TeamA` 를 `1` 로 따로 받는다.

### 공격 규칙

**예외 없이 진영으로만 판정한다.** `PlayerAttack` / `FactionAttack` 모두 `_FactionLogic:IsEnemy` 하나만 본다.
"파밍 몹은 진영 무시" 같은 예외를 두면 진영 밖 엔티티가 생겨 위 사고가 재발한다.

---

## A-4. 공개 타입

GDD §5.1(2026-08-29 정정본)이 정본이다. 핵심만 재기재:

```
VillageConfig       VillageId · VillageMapName · Lane2MapName · Lane1MapName
                    RearMapName · BossMapName · ThemeId

NpcInstanceKey      OpaqueRuntimeKey  (클라에는 불투명 문자열)
                    서버 내부 해석: MatchId : VillageId : CatalogNpcId : SpawnOrdinal

PlayerStatState     BaseStat + EquipStat + EnhanceStat + BuffStat = FinalStat
                    STR · DEX · INT · LUK · ATTACK · MAGIC · DEFENSE
                    SPEED · JUMP · ACCURACY · AVOID · MAX_HP · MAX_MP

ItemInstance        InstanceId / ItemId / EnhanceLevel / Durability / AppliedGemsCsv
                    MatchPlayerState.match_inventory 또는 match_equipped[EquipSlot]

LaneState           VillageId · Stage(1~4) · SuppressorDown · TowerHp

MinionFlowState     MatchId · Phase · ElapsedSeconds · HpMul · AtkMul
                    StageByVillage (VillageId → Stage)

VillageCoreState    VillageId · OwnerUserId(nil이면 미점유) · ClaimedAtTime
                    CurrentHp · IsExposed(억제기 파괴 후)

PublicPlayerSummary UserId · DisplayName · AvatarSnapshot · EquippedGearSummary
                    MatchLevel · VillageGrowth
                    BossKillCount · MonsterKillCount
                    LineStage (1~4. 낮을수록 잘 버티는 중)
                    TitleName (장착 칭호 표시 이름 · 없으면 "" · 2026-09-26 append)

── 계정 저장 레코드 (DataStorage 키 = 필드명 · JSON · PlayerDBManager 단일 창구 · 2026-09-26 기록·칭호 등록서) ──

AccountCollection   {"v":1,
                     "mon":{"<MonsterId>":{"k":처치,"e":자이언트 처치,"tm":받은 MONSTER 단계,"te":받은 ELITE 단계}},
                     "boss":{"<BossId>":{"k":처치,"tb":받은 BOSS 단계}}}
                    발견 = k ≥ 1 · 칸이 없으면 미발견 · 없는 필드는 0 으로 읽는다

AccountAchievement  {"v":1,"c":{"<CondType>:<CondParam>":누적},"d":{"<AchievementId>":1}}
                    c = 계정 누적 카운터 · d = 달성(지급 완료) 기록 — d 에 있으면 다시 안 준다

MatchHistory        {"v":1,"r":[{"t":정산 UTC 초,"d":★,"rk":순위(0 = 없음),"n":참가 인원,"o":MatchOutcome,
                                 "h":발록의 심장,"x":계정 경험치,"k":처치 수,"lv":매치 레벨,"ti":TitleId}, …]}
                    최신이 앞 · 20줄 초과분은 뒤에서 버린다 · 정산 받은 판(탈락자 포함)마다 1줄 · 끝나기 전에 나간 판은 나가는 순간 o=LEFT 1줄

MatchRoomHandoff    공유 메모리(RoomService:GetSharedMemory) 이름 = 인스턴스 룸 키 (§0-3 · match_{MatchId}_{세대})
                    difficulty (정수 문자열 1~5) · participants (UserId 쉼표 목록) · host (UserId · 기록용)
                    로비(정적 룸) MatchLobbyGateway.StartMatch 가 심장 차감 전에 쓰고,
                    룸 안 MatchSessionLogic 이 읽어 참가자가 다 들어오면 시작한 뒤 지운다 (룸마다 한 번)
```

🔴 **`MatchRoomHandoff` 는 룸 사이 인계 전용이다 — 저장소가 아니다.** 값은 전부 문자열이고, 읽을 때 변수별 결과 `Code` 를 **`SharedMemoryResultCode.OK`** 와 비교한다(enum 이라 `== 0` 은 항상 거짓 · 2026-09-23 실측). 룸 Logic 이 로비의 쓰기보다 먼저 뜰 수 있어 읽는 쪽은 재시도한다. 계정 값(심장 등)은 여기 싣지 않는다 — 룸이 `PlayerDBManager` 로 다시 로드한다(이동 전 동기 저장이 전제).

🔴 **비공개 필드는 `PublicPlayerSummary` 에 넣지 않는다** — 매치·계정 재화 / 창고 / 시설 레벨·HP·파괴 상태 / 수비 몬스터 종류·수·HP·훈련 레벨 / 파병 편성. 서버가 허용 필드만 복사해 반환한다.

---

# B부. 이벤트

## B-1. EventBus

**왜**: 직접 호출은 "A가 B를 호출 → A의 파일이 B를 안다 → 구독자를 추가할 때마다 발행자 파일을 고쳐야 한다"가 된다. 여러 명이 붙으면 그 발행자 파일이 충돌 지점이 된다.

> **기능 추가 = 새 파일 + 데이터 행. 기존 파일 수정 0.**

### 위치와 API

`RootDesk/MyDesk/Core/EventBus.mlua` — `@Logic`. 서버·클라 양쪽에 각각 존재.

```
_EventBus:Publish(ev)                              -- 같은 쪽 구독자에게 발행 + 디버그 로그 1줄
_EventBus:PublishToClients(name, payload)          -- @ExecSpace("Multicast")
_EventBus:PublishToUser(name, payload, forUserId)  -- @ExecSpace("Client")
```

### 발행

```lua
local ev = MonsterKilledEvent()
ev.MonsterId = 1001
ev.MapName = "Henesys_Hunt_HillNorth"
ev.KillerUserId = userId
_EventBus:Publish(ev)
```

🔴 **발행자는 구독자를 모른다.** 발행자 파일에 구독자 이름이 등장하면 규칙 위반.

### 구독 — 구독자 자기 파일에서

**정적 (권장, 정리 불필요):**

```lua
@EventSender("Logic", "EventBus")
handler HandleMonsterKilled(MonsterKilledEvent event)
    -- 몬스터 처치 시 도감 진행도를 갱신한다.
end
```

**동적 (조건부로 붙였다 뗄 때만):** `_EventBus:ConnectEvent(...)` + **`OnEndPlay` 에서 `DisconnectEvent` 필수**, 핸들은 **`property any`** 에 보관.

⚠️ 주의
- `@EventSender` → `handler`, `ConnectEvent` → `method void`. **섞으면 컴파일되고 영영 안 불린다**
- `ConnectEvent` 는 Entity/Logic/Service 에만 있다 → **버스가 Logic이라 Component 안에서도 붙일 수 있다**(버스를 두는 실용적 이점)
- 동적 구독 핸들을 지역 변수에 두면 GC된다
- `handler` 는 예약어 → `eventHandler`

### 서버 / 클라 경계

이벤트는 **같은 쪽 안에서만 전파된다고 가정한다.** `EventType` 객체는 **RPC를 넘지 못한다**(허용 타입 목록에 없다).
반대편에 알려야 하면 `PublishToClients(name, payload)` 로 **이름 + 문자열 payload** 를 넘기고 받는 쪽에서 재구성해 `Publish` 한다. 재구성 표는 `EventBus.mlua` 안에 두되 **크로스가 실제로 필요한 이벤트만** 등록한다.

> ⚠️ **미확인**: 실제 전파 범위와 `EventType` 파라미터 허용 여부는 EventBus 첫 구현 때 로그로 확인하고 **결과를 여기에 적는다.**
>
> 확인 결과: _(EventBus 구현 시 기입)_

## B-2. 작성 규약과 도입 순서

한 이벤트 = 한 파일, **발행 도메인 폴더**에.

```lua
@Event
script TowerDestroyedEvent extends EventType
    property string MatchId = ""
    property string VillageId = ""
    property string Stage = ""      -- TOWER1 / TOWER2 / SUPPRESSOR
end
```

1. payload는 **원시 타입과 문자열만.** `any`/table 금지 — RPC로 넘기면 `[LEA-3036]` 이고 **빌드에 안 잡힌다.** 목록은 문자열 직렬화
2. 열거값은 **문자열**
3. 🔴 **중복 방지 키를 포함** — `SettlementId` 등. 같은 결과를 두 번 반영하면 안 된다
4. 🔴 **payload는 append-only.** 필드 추가는 안전(기존 구독자는 모르는 필드를 무시). **이름 변경·삭제는 구독자를 깨뜨린다** → 공지 후 단독 작업

**도입 순서** — 이 문서만 고쳐서 push하면 안 된다. 정의 없이 구독하면 빌드 로그에 `Symbol not found` 가 난다.
발행자가 **3종 세트를 한 번에** push: ① 이 문서 B-3에 행 추가 ② `{도메인}/{Name}Event.mlua` ③ refresh로 생성된 **`.codeblock`**(EntryKey가 `codeblock://{소문자명}` 이라 결정론적 → 커밋해도 안전).

🔴 **정의 PR이 먼저 머지되고, 구독자 PR은 그 뒤다.**

## B-3. 이벤트 명세

> **규격은 전부 확정. `.mlua` 구현은 해당 기능을 만들 때.** "폴더" 가 곧 **소유권 배정 단위**다.
> ⚠️ = M2 예약 · **굵은 필드** = 중복 방지 키 · 🔴 = 다대다 지점(반드시 이벤트로)

### `Match/` — 매치 진행 · 맵 이동

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `MatchStartedEvent` | MatchId, PlayerIdsCsv, **DifficultyLevel**, StartServerTime | 마을 초기화 · HUD · 통계 · **난이도 배수 적용** |
| `MatchPhaseChangedEvent` | MatchId, Phase, ElapsedSeconds | 미니언 흐름 · HUD · 침범/발록 해금 |
| `MatchLevelChangedEvent` | UserId, PrevLevel, NewLevel | 🔴 **10=전직 자격 · 21=침범·발록 자격** · HUD · 통계 |
| `ExpGainedEvent` | UserId, Amount, SourceType, SourceId | 레벨 계산 · 통계 |
| `PlayerEliminatedEvent` | MatchId, UserId, Reason | 파병 취소 · 통계 · 정산 · 로비 퇴장. **구현됨**(2026-09-08 · `Match/PlayerEliminatedEvent` · `LaneStateService.Eliminate` 가 넥서스 파괴 시 발행 · Reason `CORE_DESTROYED` · MatchId 는 매치 식별자 전이라 `""`) |
| `MatchEndedEvent` | MatchId, WinnerUserId, Reason | 정산 · 상태 폐기 · 결과 UI |
| `MapEnteredEvent` | UserId, MapName, PrevMapName | 맵별 컨트롤러 · 통계 · BGM |
| `MapLeftEvent` | UserId, MapName | 정리 |

### `Stat/` — 플레이어 성장·전투 계산

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `StatRecalculatedEvent` | UserId, FinalStatsCsv, Source | 데미지 계산 · 이동/점프 반영 · 스탯 UI |
| `ApAllocatedEvent` | UserId, AllocatedStatsCsv, RemainingAp | 레벨 원장 · 스탯 UI |
| `SpSpentEvent` | UserId, SkillId, Amount, RemainingSp | B의 스킬 트리 · HUD · 스탯 UI. **발행 = A** — B 는 `_SummonManager:SpendSp(userId, skillId, amount) → boolean`(ServerOnly · 부족하면 false · 성공 시 차감→Push→이벤트)을 부르고, 읽기는 `GetEcon(userId).sp`, 환불은 `GrantSp(userId, amount)`. `econ.sp` 직접 쓰기 금지 (2026-09-08 구현) |

`FinalStatsCsv`와 `AllocatedStatsCsv`는 ExecSpace 경계를 넘는 table 대신 문자열로 직렬화한다. `StatService`만 Base·Equip·Enhance·Buff를 합산한다.

### `Village/` — 코어 · 소유권

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `VillageClaimedEvent` | VillageId, OwnerUserId, ClaimedAtTime | 🔴 **NPC 10종 · 포탑 · 모집/훈련 잠금 해제** · 통계 · 토스트 |
| `VillageClaimDeniedEvent` | UserId, VillageId, Reason | 거절 토스트 |
| `VillageCoreDamagedEvent` | VillageId, AttackerUserId, Amount, RemainingHp | **주인에게 즉시 알림** · HUD |
| `VillageFallenEvent` | MatchId, VillageId, OwnerUserId | 🔴 **게임 오버 처리** · 파병 취소 · 통계 · 정산 |

### `Lane/` — 미니언 상시 흐름 · 전선

> ⚠️ **전투 인스턴스가 없다.** 미니언은 그냥 계속 걸어오고 포탑·수비 몬스터·플레이어가 그때그때 막는다. "전투 시작/종료" 이벤트를 만들지 않는다.

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `MinionSpawnedEvent` | MatchId, VillageId, MonsterTypeId, InstanceId | 전투 AI · 통계 |
| `MinionKilledEvent` | MatchId, VillageId, InstanceId, KillerUserId, IsDispatched | 🔴 **경험치·메소**(파병 몬스터는 보상 없음) · 통계 |
| `TowerDamagedEvent` | VillageId, Stage, RemainingHp | 포탑 UI · HUD |
| `TowerDestroyedEvent` | MatchId, VillageId, Stage | 🔴 **전선 판정** · 재건 UI · 통계. **1회성** |
| `SuppressorDownEvent` | MatchId, VillageId | 🔴 **억제기의 역설** — 미니언 강화 배율 + 경험치 상승 적용. **1회성** |
| `LaneStageAdvancedEvent` | MatchId, VillageId, NewStage(2~4) | 전선 이동 · HUD · 공개 통계 `LineStage`. **1회성** |
| `TowerUpgradedEvent` / `TowerRepairedEvent` / `TowerRebuiltEvent` | VillageId, Stage, NewLevel | **성장 포인트** · 통계 · UI |
| `DefenderEnteredLaneEvent` / `DefenderLeftLaneEvent` | UserId, VillageId, MapName | 파밍 시간 손실 통계 · HUD |

### `Dispatch/` — 파병

> ⚠️ **아래 3개는 예약이며 아직 만들지 않았다** (2026-09-09 · `a/dispatch-real-move`).
> 파병 본체는 구현됐지만 구독자가 A 자신뿐이라 `LaneStateService` ↔ `DispatchService` ↔ `MinionFlowService`
> **직접 호출**로 이었다. 통계·B 쪽 구독이 생기면 그때 §6-3 3종 세트(계약서 행 → `{Name}Event.mlua` → `.codeblock`)로 정의한다.

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `DispatchSubmittedEvent` ⬜ 예약 | **OrderId**, SenderUserId, TargetVillageId, Count | 보유 목록 차감 · 미니언 흐름 합류 · UI |
| `DispatchCancelledEvent` ⬜ 예약 | **OrderId**, SenderUserId | 보유 목록 복귀 · UI |
| `DispatchJoinedEvent` ⬜ 예약 | **OrderId**, TargetVillageId | 방어자 "파병 섞임" 경고(총량 등급만) · 통계 |

### `Monster/` — 몬스터 · 엘리트

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| 🔴 **`MonsterKilledEvent`** | MonsterId, MapName, KillerUserId | **경험치 · 메소 · 드랍 · 도감 · 업적 · 통계 (6)** |
| `EliteSpawnedEvent` | MapName, EliteMonsterId, BaseMonsterId | 알림 · 통계 |
| `EliteKilledEvent` | EliteMonsterId, MapName, KillerUserId | 🔴 **엘리트 재료 지급** · 도감 · 업적 · 통계 |

> `MonsterKilledEvent` 가 이 설계의 핵심 사례다. 직접 호출로 만들면 **6명이 한 파일을 경합**하고, 이벤트로 하면 각자 자기 파일에 `handler` 하나씩이다.

### `Boss/` — 보스 · 발록

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `BossDamageRecordedEvent` | BossId, UserId, Amount | 누적 피해 원장 |
| `BossKillResolvedEvent` | BossId, WinnerUserId | **최다 피해 판정** → 보상 독식 · 도감 · 업적 |
| `BossFirstClaimedEvent` | BossId, UserId, **SettlementId** | **마노 선취 1회** — 10렙제 체력 갑옷 + 메소 |
| ~~`BalrogChallengeStartedEvent`~~ | ~~MatchId, UserId, ChannelKey~~ | **미사용 (2026-09-13)** — 입장은 `BalrogRoomService.RequestEnter` (A-2-10) |
| ~~`BalrogKilledEvent`~~ | ~~MatchId, UserId, SettlementId~~ | **미사용 (2026-09-13)** — 승리는 `BalrogRoomService.DeclareWinner` → `MatchSessionLogic:DeclareWinner` 직접 호출 (A-2-10) |

### `Npc/` — NPC 상호작용

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `NpcOpenedEvent` | UserId, NpcInstanceKey, CatalogNpcId, UiGroupName, UiRoute | UI 라우팅 · 통계 |
| `NpcOpenDeniedEvent` | UserId, NpcInstanceKey, Reason | 거절 토스트 |
| `NpcActionExecutedEvent` | VillageId, CatalogNpcId, ActionId, UserId | 🔴 **성장 포인트** · 통계 · 업적 |
| `ItemCraftedEvent` | UserId, RecipeId, ResultItemId | **성장 포인트** · 도감 · 업적 |
| `ItemEnhancedEvent` | UserId, ItemId, GemId, NewLevel | 🔴 **보석 소모** · 장비 상태 · 업적 |
| `ItemRepairedEvent` / `ItemPurchasedEvent` | UserId, ItemId / ShopItemId, Count, Cost | 내구도 / 인벤토리 |
| `StorageChangedEvent` | UserId, SlotIndex, ItemId, Count | 창고 UI |
| `MonsterRecruitedEvent` | UserId, VillageId, MonsterInstanceId, MonsterTypeId | **성장 포인트** · 보유 UI |
| `MonsterDeployedEvent` | VillageId, MonsterInstanceId, SlotIndex | 배치 UI |
| `MonsterDefenderDiedEvent` | VillageId, MonsterInstanceId | 보유 목록 **영구 제거** · 통계 |
| `MonsterHealedEvent` | VillageId, MonsterInstanceId, NewHp | UI |
| `TrainingStartedEvent` | VillageId, MonsterTypeId, TargetLevel | 동시 작업 배수 재계산 · UI |
| `TrainingCompletedEvent` | VillageId, MonsterTypeId, NewLevel | **성장 포인트** · 병과 보너스 · 업적 |

### `Item/` — 인벤토리 · 장비

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `EquipChangedEvent` | UserId, EquipSlot, PrevInstanceId, NewInstanceId | `StatService` · 장비/인벤토리 UI |

`ItemEnhancedEvent`는 기존 `Npc/` 표의 확정 이벤트를 그대로 사용한다. 장비 강화 후에는 `ItemEnhancedEvent` → `StatRecalculatedEvent` 순서로 발행한다.

### `Map/` — 맵 · 포탈 · 월드맵

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `MapEntryDeniedEvent` | UserId, MapName, Reason | 거절 토스트 (21레벨 미만 침범 시도 등) |
| `PortalUsedEvent` | UserId, RouteId, FromMap, ToMap | 통계 · 월드맵 갱신 |

### `Economy/` — 재화

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `ItemDroppedEvent` / `ItemPickedUpEvent` | MapName, ItemId, Count, OwnerUserId / UserId, ItemId, Count | 드랍 엔티티 · 인벤토리 · 도감 |
| `MesoGainedEvent` / `MesoSpentEvent` | UserId, Amount, SourceType·SinkType, SourceId·SinkId | 재화 원장 · HUD · 업적 |
| `GemGainedEvent` / `GemSpentEvent` | UserId, GemId, Count | 재화 원장 · **강화 UI** |
| `EliteMaterialGainedEvent` / `EliteMaterialSpentEvent` | UserId, MaterialId, Count | 재화 원장 · **포탑 강화 UI** |

> `SourceType`/`SinkType` 이 어긋나면 통계·업적이 전부 어긋난다. §0-2 목록을 벗어난 값을 쓰지 않는다.

### `Progression/` — 기록 · 보상

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `AchievementProgressedEvent` / `AchievementUnlockedEvent` | UserId, AchievementId, Current, Required | 업적 UI · 보상 |
| 🔴 **`RewardGrantedEvent`** | UserId, RewardSourceType, RewardSourceId, **SettlementId** | 재화·아이템 지급 · **영구 저장** · 토스트 |
| `CollectionUpdatedEvent` | UserId, MonsterId, Kind(`DISCOVER`/`KILL`) | 도감 UI · 업적 |
| `VillageGrowthPointAddedEvent` | UserId, VillageId, ActionKey, Points | 공개 통계 `VillageGrowth` |

> 🔴 **`RewardGrantedEvent` 가 보상의 단일 통로다.** 업적·보스·도감·발록이 각자 지급하지 않고 **전부 이 이벤트를 발행**한다. `SettlementId` 로 중복 지급을 막는다 (GDD §4.10: "같은 정산 ID를 두 번 지급하지 않는다").
>
> ⚠️ **2026-09-26 기록·칭호 등록서(§1)**: 위 4행은 **예약 유지** — 구독자가 A 자신뿐이라 직접 호출로 구현한다(파병 등록서와 같은 판단). `RewardGrantedEvent` 도 코드에 없다(A-2-21 과 같음) → 계정 경험치는 `AccountLevelService.AddExp` 직접 호출, 중복 지급은 저장 레코드의 달성 기록(A-4 `AccountAchievement.d` · `AccountCollection` `tm`/`te`/`tb`)이 막는다.

### `Skill/` `Job/` — 상대 담당

✅ **등록 완료** (§1 · #37 · 2026-09-09). 발행 = B(서버). payload 는 append-only.

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `JobChangedEvent` | **UserId**, PrevJobId, NewJobId, Tier | 🔴 **10레벨 전직 → 코어 연결 자격**(A `Village/`) · `StatService.SetJob`(주스탯 재계산) · 스킬 트리 갱신. `NewJobId` 는 §0-2 `JobId`, `Tier` 는 **integer** `JobTier`(0~3 · §0-2 값 고정 · append-only — A 의 `VillageOwnership` 이 이 값으로 전직 여부 판정). 전직 서버 로직(`Job/JobDatabase`)이 `JobTier.ReqLevel` 검사 통과 후 발행 |
| `SkillUsedEvent` | **UserId**, SkillId, MapName | 쿨다운 UI · 통계 · 업적. `SkillCaster.RequestCast`(Server) 가 6개 게이트 + `SpendMp` 성공 뒤 **시전당 1회** 발행. 실패한 시전은 발행하지 않는다 |
| `SkillStateChangedEvent` | What (`"sp"` · `"mp"` · `"skill"` · `"job"`) | **B 내부 · 클라 전용.** 스킬 창·핫바 갱신 신호. 타 도메인은 구독하지 않는다 |

구독(B ← A): `SpSpentEvent.RemainingSp`(스킬 창 SP 표시). 레벨은 이벤트 대신 `_StatusHUDController.curLevel`(클라) · `_SummonManager:GetEcon(userId).level`(서버)을 읽는다.

---

## B-4. 도메인 ↔ 폴더 ↔ 담당

| 도메인 | 폴더 | 담당 |
|---|:---:|:---:|
| EventBus | `Core/` | A |
| 매치 진행 · 맵 이동 · 난이도 · 승패 · 계정 | `Match/` | A |
| 코어 · 소유권 | `Village/` | A |
| 미니언 흐름 · 전선 · 포탑 | `Lane/` | A |
| 파병 | `Dispatch/` | A |
| 몬스터 · 엘리트 | `Monster/` | A |
| 보스 · 발록 | `Boss/` | A |
| 맵 · 포탈 · 월드맵 | `Map/` | A |
| NPC · 상호작용 | `Npc/` | A |
| 플레이어 스탯 · 데미지 | `Stat/` | A |
| 인벤토리 · 장비 · 강화 | `Item/` | A |
| 재화 | `Economy/` | A |
| 기록 · 보상 | `Progression/` | A |
| **스킬 · 전직** | **`Skill/` `Job/`** | **B** |

기존 폴더(`Catalog/` `Spawn/` `Faction/` `Farm/` `Summon/` `Camera/` `Terrain/` `WorldMap/`)는 유지하되, 이관 계획은 §협업-규칙에 따른다.

---

## 변경 이력

| 날짜 | 변경 |
|---|---|
| **2026-09-26** | **[등록] 기록 · 칭호 등록서 + 새 표 4 정의 (A · `a/m1-finish` 통합 · 묶음 0 `a/m1-0-contract` · Draft PR #104 · #40 comment 5844015147 공지 · WO-031).** §1 등록서 8항목(매치 집계 · 공개 통계 · 계정 도감 · 업적 · **칭호(신규)** · 플레이 기록 · 파병 등급 경고) · 새 표 **A-2-23 `AchievementConfig`**(A-3 예약 해소) · **A-2-24 `TitleInfo`** · **A-2-25 `CollectionReward`** · **A-2-26 `GrowthPoint`** · §0-2 열거값 4종(`AchievementCondType` · `CollectionKind` · `GrowthActionType` · `MatchOutcome`) + `SourceType += COLLECTION` · §0-5 `SchemaVersion` 4(`account_titles` · `account_title`) · A-4 `PublicPlayerSummary += TitleName` + 저장 레코드 3종(`AccountCollection` · `AccountAchievement` · `MatchHistory`) · B-3 `Progression/` 이벤트 4종 예약 유지(직접 호출). 파일은 각 묶음 구현 PR 에서 생성 — 이 PR 은 문서와 새 표 CANONICAL/PK 만. 기존 표 헤더 변경 없음 |
| **2026-09-23** | **[등록] 인스턴스 룸 키 · 인계 레코드 (A · `a/p0914-11-rooms` · B11 · Draft PR #80 · WO-024 · #40 comment 5794545040 공지).** §0-3 이름 규약에 **인스턴스 룸 키 `match_{MatchId}_{세대}`** + "매치 맵 = 인스턴스 맵"(`IsInstanceMap` + 파일 헤더 `Usage: 1` · 로비 2개만 정적 · 룸 맵 목록 = `PortalRoutes`) · A-4 에 **`MatchRoomHandoff`**(공유 메모리 · `difficulty`·`participants`·`host` · 로비가 쓰고 룸이 한 번 읽고 지움) · 9/14 등록서 1·8번에 B11 추가. 새 CSV·열·이벤트·열거값 없음 |
| **2026-09-18** | **[의미 확장] `LaneConfig.SpawnX/SpawnY`** (A · `a/lane-ellinia-nautilus` · PR #43). `LANE2` 전용 → **모든 역할**. 빈칸 = 오른쪽 끝에서 우→좌(기존 행 동작 그대로), 값이 있으면 그 x 에서 출발 — 좌→우 마을(엘리니아·노틸러스 · 사용자 2026-09-15/18)의 `LANE1`·`VILLAGE` 행이 쓴다. 헤더 변경 없음 |
| **2026-09-18** | **[열 추가] `MinionComposition.VillageId`** (A · `a/lane-ellinia-nautilus` · PR #43 · #40 comment 5725998438 공지). 빈칸 = 전 마을 · 마을 행이 있으면 그 마을은 그 행만. 기본 키 `Phase+MonsterId+VillageId`. 노틸러스 물버섯 `12230101` |
| **2026-09-22** | **[등록] 9/14 기획 반영 등록서 + 새 표 6 정의 + 열 추가 예고 (A · `a/plan-0914` 통합 · B0 `a/p0914-0-contract` · Draft PR #67 · #40 comment 5776547020 공지).** §1 등록서 8항목 · 새 표 **A-2-6a `MinionWave`** · **A-2-7a `DifficultyRule`** · **A-2-19 `DropTable`** · **A-2-20 `MonsterRecruit`**(A-3 예약 해소) · **A-2-21 `RankReward`** · **A-2-22 `GuideStep`** · §0-2 열거값 4종 신설 + `SourceType`/`SinkType` 추가 + `TOWER_SPLASH` 확정 · §0-5 `SchemaVersion` 3 · A-2-7 `DifficultyConfig` 헤더를 실제 파일로 정정(12열 헤더는 만들어진 적 없음) + ★1/3/5 만 · A-2-5 페이즈 시각 270/990/1470 예고 · A-2-6 `MinionComposition` 미사용 예고 · A-2-13 발록 HP 기준선 교체 · A-2-15 엘리트 = 맵별 25킬 자이언트 확정 · A-2-17 S2 값 확정 + 억제기 폭발 · A-2-8b/8c/8d/11 값·열 예고. **기존 표 열 추가(5개 표)는 §0-1 9 대로 각 구현 PR 에서 헤더+CANONICAL 동시 변경** — 이 PR 은 문서와 새 표 CANONICAL/PK 만 |
| **2026-09-18** | **[변경] §0-2 `BuffTag` 열거값 `SUPER_TRANSFORM` → `ENERGY_CHARGE` (B · `b/skill-pirate-energy-charge` · PR #61).** 해적 2차 C 슈퍼 트랜스폼을 에너지 차지로 바꾸면서 `SK_P21` 의 태그가 바뀌었는데 이 표를 안 고쳤던 것을 맞춘다. 1:1 교체라 13종 그대로이고, `SUPER_TRANSFORM` 은 이제 어떤 행·코드도 쓰지 않는다. B 내부용 태그라 열 추가(2026-09-17 정정)와 같은 방식으로 구현 PR 안에서 고쳤다 |
| **2026-09-17 (3차)** | **[변경] `SkillInfo` 에 `RecastMpCostMul` 한 열 추가 (B · `b/skill-pirate-energy-charge` · PR #61 · 공지 = 그 PR 본문 맨 위 · 같은 PR 의 2차 변경과 함께).** `TertiaryPerLevel` 뒤 · `#Note` 앞(§0-1 3·4 — `#Note` 마지막 · 뒤에 추가만). 기존 26행은 전부 `1` = 초기 시전과 같은 비용, 에너지 차지(`SK_P21`)만 `0.25`(MpCost 20 → 재시전 5). 재시전이 있는 스킬만 읽는 열이라 나머지 25행에는 런타임 영향이 없다. 🔴 **기본값이 `1` 인 것이 요점** — 배율에서 빈 칸을 0 으로 읽으면 열을 안 채운 재시전 스킬이 조용히 무료가 되므로, 이 열만 `ParseNumberOr(… , 1)` 로 읽는다. 왜 상수가 아니라 열인가: 0.78s 시전 락에서 30초 차지 창이면 재시전이 ~38회라 비용이 균형 손잡이다 |
| **2026-09-17 (2차)** | **[변경] `SkillInfo` 에 `TertiaryEffect` · `TertiaryPerLevel` 두 열 추가 (B · `b/skill-pirate-energy-charge` · PR #61 · 공지 = 그 PR 본문 맨 위).** `SpawnOffsetY` 뒤 = 기존 열 맨 뒤 · `#Note` 앞(§0-1 3·4 — `#Note` 마지막 · 뒤에 추가만). 기존 26행 전부 `0`(미사용). 한 스킬이 레벨마다 서로 다른 값 2개를 가져야 할 때 — 첫 사용자는 해적 2차 C 에너지 차지(부효과 = 이동속도 · 제3 효과 = 점프력 · 서로 독립). 레벨 공식은 부효과와 같은 꼴. 같은 날 정정된 §1 대로 **등록 PR 없이 구현 PR 안에서** 헤더 정의와 `check-integrity` CANONICAL 을 같이 고쳤다 |
| **2026-09-17** | **[정정] 열 추가는 등록 대상 아님 · 헤더 공지 형식 · 8번과 self-merge 관계** (A · `a/rules-column-selfmerge` · B #40 comment 5700352071 → A 결정 · 공지 5700500503). §1 등록 대상 판정에 "기존 표에 열 추가 = §0-1 9 로 처리, 헤더 정의·CANONICAL 은 구현 PR 안에서" · `Docs/` 자기 몫은 남의 폴더 아님 · §0-1 9 에 Draft PR 공지 형식 · 8번 주석에 "self-merge 는 남의 폴더 파일만 본다" |
| **2026-09-13** | **[개정] A-2-10 발록 방 = 구현대로 (A · `a/balrog-rooms` · PR #51 · WO-017 · #40 comment 5653399527 공지).** `BalrogChallenge` · `BalrogRoom` 표 **미채택**(새 CSV 0개) — 입장권 = 아이템 `BRAND_SOULSTONE` ★1~5 = 1·5·10·15·20 · 매치 시작 때 고정 배정 · 90초 · 도전 창 밖 무적 · HP 회복 없음(나눠 잡기) · BossId `8130100` 6방 공유 · 설정은 `BalrogRoomService` 속성(`TicketCosts` 는 PR #50 `DifficultyConfig` 머지 뒤 열로 이동). B-3 `BalrogChallengeStartedEvent` · `BalrogKilledEvent` **미사용** — 승리는 직접 호출 |
| **2026-09-09 (3차)** | **[구현] `SummonManager` MP 원장 (B · `b/summon-mp-ledger` · PR #41).** A 가 #40 comment 5588167790 에서 "A 가 직접"을 번복해 B 가 네 항목을 한 PR 로: `SpendMp(userId, amount) → boolean`(이벤트 없음) · `GrantMp`(maxMp clamp) · 3초 회복 틱(`MpRegenIntervalSec` · `MpRegenRate` property · 회복한 유저만 Push) · `StatRecalculatedEvent` 구독으로 장비·버프 `maxmp` → `econ.bonusMaxMp` 다리(`econ.maxMp = BaseMaxMp + levelMaxMp + bonusMaxMp` · 바뀔 때만 Push · `StatService.mlua` 수정 없음). §1 등록서 스킬 시전 행 · 8번 · A 답변 주석의 담당 표기 정정. A 승인 필요 |
| **2026-09-09 (2차)** | **[등록 후속] A 답변 반영 (`b/skill-register-fix`).** `JobId` 6종 확정 · `JobTier` integer + 값 고정(0=NOVICE/1/2/3, `VillageOwnership` 판정용) · 등록서 8번에서 `SummonManager` 제거(`SpendMp` · `GrantMp` · 3초 회복 틱은 A 가 별도 브랜치) · `JobInfo` `NOVICE` 행 필수 · maxMp 진상 = `econ.maxMp`(StatService 장비·버프분은 A 가 다리) · `ItemInfo.ReqJob` 은 숫자 0 자리표시자(문자열화는 헤더 계열 변경 → 사전 공지) |
| **2026-09-09** | **[등록] 스킬 시전·스킬 UI·전직 (B · `b/skill-register`).** `JobId` 6종 확정 제안(A 임시값 그대로) · `JobTier` · `SkillBehavior` · `EffectUnit` · `BuffTag` 열거값 · 신규 표 `SkillInfo` · `JobInfo` · `JobTier`(A-2-16, 스냅샷 `SkillDataSet`/`JobDataSet` 을 PascalCase 로) · `JobChangedEvent`(+`Tier`) · `SkillUsedEvent` · 내부 `SkillStateChangedEvent` 명세 · 기존 파일 3종(`SummonManager` `SpendMp` · `StatUIController` `OnClickSkill` · `WeaponMotion` 행) 등록 |
| **2026-09-08** | **[등록] 매치 시계 5단 · 드랍 소유권 · 물약 쿨타임 (WO-014 · 추가기획2 · PR #35).** 🔴 `MatchPhase` 열거값을 **`PHASE0-1`/`PHASE0-2`/`PHASE1`/`PHASE2`/`PHASE3`** 로 교체(구 4값 폐기 · **하이픈 예외** 1건 §0-2) · `MinionPhaseConfig` 4행 → **5행**(헤더 불변 · LIVE 0/240/270/720/1200 · TEST = ÷8) · **신규 표 `MatchConfig`(A-2-5a)** 로 매치 만료 30분 분리 · `ConsumeInfo` 뒤에 `CooldownStartSeconds,CooldownEndSeconds,CooldownEndLevel` 3열(선형 13→6초) · `MatchPhaseChangedEvent.Phase` 에 `"EXPIRED"` 값 추가(append-only) · 유저 econ 에 `autoPickup`. 🔴 **페이즈 판정은 인덱스가 아니라 이름으로** — 행이 늘며 `LaneFacility` 저항 게이트가 한 칸 밀리는 회귀를 같이 고쳤다 |
| 2026-09-08 | SP 원장 API 확정: `SummonManager.SpendSp(userId, skillId, amount)`(A 가 차감·`SpSpentEvent` 발행) · `GrantSp` · 읽기 `GetEcon().sp`. B 스킬 트리는 호출만 (B-3 행 갱신 · GDD §AP/SP 문구 정정) |
| 2026-09-08 | `Match/PlayerEliminatedEvent` 구현 표기(넥서스 파괴 → `LaneStateService.Eliminate` 발행 · Reason `CORE_DESTROYED` · MatchId `""`). 소유권 `Claim` 거절 사유 `DESTROYED`(파괴된 마을) · `ELIMINATED`(탈락 유저) 추가. 관전은 `Match/SpectateService`(카메라 리그 `spectatecamrig`) — 데이터 표 변경 없음 |
| **2026-09-07** | **[등록] 레인 세로 조각(WO-012 ①~③).** `LaneConfig` 헤네시스 3행 실측 등록 · `TowerConfig` `Stage` 3값(`TOWER/SUPPRESSOR/CORE`)으로 축소 + 비용 열 = **빅토리아 주화** 확정 · `DefenderSlotCount` 미사용 표기. 소유권 원장 `Village/VillageOwnership`(넥서스 클릭 → 10레벨·선착순) 이 NPC 허용 판정의 정본. ② `MinionPhaseConfig` 뒤에 `Profile,CoinDropChance` · `MinionComposition` 뒤에 `BaseHp,BaseAttack` 추가(A-2-5/6 각주). ③ `MonsterInfo` 뒤에 **`IconRUID`**(도감·모집 썸네일 · 모델 stand 클립) 추가 · 도감 해금(지역재화 8 → Lv1 모집 가능) / Lv2~5(꿈의 조각 3/5/10/15 · 배율 1.5/2.2/3.3/5.0) 은 `MonsterTraining` 을 몬스터 공용 표로 씀 — 해금·모집 재료는 임시로 `GEM_DIAMOND` |
| **2026-09-03** | **[등록] 플레이어 스탯·장비·강화·기능 NPC·UI.** `StatId` 13종·6장비 슬롯·4아이템 종류·4NPC 섹터, 신규 CSV 6종, `MatchPlayerState` 성장 원장, UI 릴레이 및 이벤트 계약을 추가했다. `FunctionalNpcCatalog`에 `SectorId`·`SlotOrder`를 추가하고 보석 표를 A-2로 승격했다. |
| **2026-08-29 (2차)** | **현행 GDD(정합성 정정본) 기준으로 전면 재작성.** 초안이 개정 전 기획 위에 쓰여 표 이름부터 어긋나 있었다 — `WavePhaseConfig`→`MinionPhaseConfig` / `WaveComposition`→`MinionComposition` / `DefenseMapConfig`→`LaneConfig` / 대포탑 2모드→포탑 3종+코어(`TowerConfig`) / NPC 14행→12행·60인스턴스. **엘리트 · 주니어 발록 · 마노 선취 · 억제기의 역설 · 난이도 신규 반영.** 이벤트를 전투 인스턴스 전제에서 **상시 흐름** 구조로 재설계 |
| 2026-08-29 | 최초 작성 (개정 전 GDD 기준 — 폐기) |
