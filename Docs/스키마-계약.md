# 스키마 계약 — 데이터 표 · 이벤트

> **이 문서가 정본이다.** GDD는 "무엇을 만드는가", 이 문서는 "어떤 모양으로 만드는가"를 정한다.
>
> 🔴 **새 표 · 새 열 · 새 열거값 · 새 이벤트 · 새 시스템을 만들려면 이 문서를 먼저 고친다.**
> 이 문서 변경은 **공지 후 단독 작업**이다 (여러 명이 동시에 고치면 계약 자체가 깨진다). 공지는 **GitHub Issue 또는 Draft PR**로 한다.

| 항목 | 값 |
|---|---|
| 기준 기획 | `VillageDefense-M1-GDD.md` (2026-08-29 정합성 정정본) |
| 최종 갱신 | 2026-08-29 |
| 대상 월드 | `VillageDefense` |
| 협업 방식 | LocalWorkspace + git |

관련: [`협업-규칙.md`](협업-규칙.md) · [`VillageDefense-M1-GDD.md`](VillageDefense-M1-GDD.md) · [`VillageDefense-Roadmap.md`](VillageDefense-Roadmap.md)

---

## 0. 공통 규약

### 0-1. CSV 열 규약

1. `RootDesk/MyDesk/{Name}.csv` + **`{Name}.userdataset` 쌍 필수.** csv만 있으면 런타임에서 안 읽힌다
2. **첫 열이 기본 키.** 복합키는 키 열들을 **앞에 연속 배치**하고, 두 값을 합친 문자열 열을 따로 만들지 않는다
3. 🔴 **`#Note` 는 항상 마지막 열.** 새 열은 반드시 **`#Note` 앞에** 넣는다
4. 🔴 **열 중간 삽입 금지. 뒤에 추가만.** 중간에 넣으면 기존 행의 값이 전부 한 칸씩 밀린다
5. **빈 칸 = 기본값.** 로더는 모든 열에 `tonumber(...) or 기본값` / `or ""` 폴백을 넣는다. 이게 "그 열을 모르는 쪽에서도 안 죽는" 확장 계약의 실체다
6. 열 이름 **PascalCase**, 약어도 첫 글자만 — **`Hp` `Mp` `Ap` `Id`**
7. Boolean 은 소문자 `true` / `false`
8. 단위 — 좌표 **world unit**(1 unit = 100px), 시간 **초**, 확률·배율 **소수**
9. 🔴 **헤더 변경은 공지 후 단독 작업.** `.csv` 는 git `merge=union` 이라 헤더가 두 줄 되면 조용히 깨진다

> ✅ CSV는 MSW가 **로컬 직접 편집을 공식 허용한 유일한 데이터 형식**이다. 데이터를 CSV로 밀어 넣을수록 협업이 안전해진다.

### 0-2. 열거형

전부 **대문자 SNAKE_CASE**. **이 문서가 유일한 정본**이며 GDD 본문에 사본을 두지 않는다.
⚠️ **엔진 enum 은 ExecSpace를 못 넘는다**(`LEA-3036`, 빌드에 안 잡힘) → 경계를 넘는 값은 **문자열 키**로 보내고 받는 쪽에서 되돌린다.

| 이름 | 값 |
|---|---|
| `VillageId` | `ELLINIA` · `HENESYS` · `KERNING` · `PERION` · `NAUTILUS` · `LITH` |
| `MatchPhase` | `PHASE1_PIONEER` · `PHASE2_VILLAGE` · `PHASE25_PRESSURE` · `PHASE3_FINALE` |
| `NpcRole` (마을 10) | `WORKSHOP_CRAFT` · `WORKSHOP_ENHANCE` · `WORKSHOP_REPAIR` · `SHOP_POTION` · `LIFE_STORAGE` · `PRODUCTION_RECRUIT` · `PRODUCTION_TRAIN` · `RECORD_COLLECTION` · `RECORD_STATS` · `DEFENSE_TOWER` |
| `NpcRole` (공용 2) | `COMMON_MATCH_GUIDE` · `COMMON_DISPATCH_OFFICER` |
| `LaneStage` | `1`=1차포탑 · `2`=2차포탑 · `3`=억제기 · `4`=코어 |
| `TowerStage` | `TOWER1` · `TOWER2` · `SUPPRESSOR` · `CORE` |
| `GemId` | `DIAMOND`(공격력) · `SAPPHIRE`(마력) · `AMETHYST`(이동속도) · `AQUAMARINE`(점프력) · `GARNET`(명중률) · `OPAL`(회피율) · `TOPAZ`(HP) · `EMERALD`(MP) · `STR_CRYSTAL` · `DEX_CRYSTAL` · `LUK_CRYSTAL` · `INT_CRYSTAL` |
| `StatId` | `STR` · `DEX` · `INT` · `LUK` · `ATTACK` · `MAGIC` · `DEFENSE` · `SPEED` · `JUMP` · `ACCURACY` · `AVOID` · `MAX_HP` · `MAX_MP` |
| `EquipSlot` | `WEAPON` · `HAT` · `TOP` · `BOTTOM` · `SHOES` · `GLOVES` |
| `ItemType` | `EQUIP` · `CONSUME` · `MATERIAL` · `GEM` |
| `SectorId` | `WORKSHOP` · `LIFE` · `RECORD` · `DEFENSE` |
| `MonsterClass` | `MELEE` · `RANGED` · `TANK` |
| `RouteKind` | `VILLAGE` · `HUB` · `LANE` · `REAR` · `BOSS` |
| `OwnershipMode` | `OWNER_ONLY` · `PUBLIC` |
| `SourceType` (획득) | `MONSTER_KILL` · `ELITE_KILL` · `MINION_KILL` · `BOSS_KILL` · `BOSS_FIRST_CLAIM` · `ACHIEVEMENT` |
| `SinkType` (소모) | `CRAFT` · `ENHANCE` · `REPAIR` · `SHOP_BUY` · `MONSTER_RECRUIT` · `MONSTER_TRAIN` · `MONSTER_HEAL` · `TOWER_UPGRADE` · `TOWER_REPAIR` · `TOWER_REBUILD` · `BALROG_CHALLENGE` · `DISPATCH` |
| `MoveType` *(소문자 유지)* | `hop` · `walk` · `fly` · `stay` |
| `AiType` *(소문자 유지)* | `wander` · `chase` · `static` |
| `JobId` *(상대 담당)* | 시스템 등록서에서 확정 |
| `QuestState` ⚠️ M2 | 예약만 |

> `MoveType`/`AiType` 만 소문자인 것은 **기존 78행이 이미 그 값**이기 때문이다. 대문자로 바꾸면 전 행을 고쳐야 하므로 예외로 둔다.

### 0-3. 이름 규약

| 대상 | 규칙 | 예 |
|---|---|---|
| 맵 파일 · 맵 루트 엔티티 | **`{지역}_{종류}_{이름}`**, **영어 필수** | `Henesys_Village_MinimiMain` |
| `.model` EntryKey | `model://` + **전부 소문자** | `model://chasemonster` |
| 스크립트 | PascalCase, **기능 폴더 아래 필수** (`MyDesk/` 루트 배치 금지) | `Lane/LaneController.mlua` |
| `@Logic` 접근자 | `_` + **정확한 스크립트명**, 축약 금지 | `_EventBus` · `_MinionFlowService` |
| UIGroup 파일 | `{도메인}Group.ui` | `VillageWorkshopGroup.ui` |
| 이벤트 | `{명사}{과거분사}Event` | `TowerDestroyedEvent` |

🔴 **`MapName` 값은 맵 루트 엔티티 이름과 글자 단위로 일치해야 한다.** 하나라도 틀리면 그 스포너는 **에러 없이 조용히 안 돈다** (같은 원인으로 이미 32건이 죽은 이력).

🔴 **엔트리·폴더 이름 제약** (LocalWorkspace가 OS 파일 규칙을 탄다): 금지 문자 `< > : " / \ | ? *` · 예약어 `CON` `PRN` `AUX` `NUL` `COM1~9` `LPT1~9` · 이름 시작/끝의 마침표·공백 · **전체 경로 255자 초과 금지** → clone 위치를 짧게.

### 0-4. ID 대역

| 대상 | 대역 |
|---|---|
| 밍키타 출신 몬스터/NPC | `1001` ~ `1999` |
| 안농 출신 몬스터/NPC | 메이플 원본 ID 그대로 (6~7자리) |
| 기능 NPC | `VD_` 접두 문자열 — `VD_WORKSHOP_CRAFT` |
| **신규 도메인** (아이템·스탯·보석·섹터·포탑·레시피·병과·직업·엘리트 재료) | 🔴 **숫자 ID를 쓰지 않고 대문자 열거값 문자열.** 대역 관리 자체가 불필요해진다 |
| 숫자 ID가 불가피한 곳 | **사람별 하위 대역**을 배정 |

⚠️ `.csv` 는 `merge=union` 이라 **ID가 중복돼도 git이 안 잡아준다.** 두 줄 다 남고 조용히 깨진다 → §정합성 검사 3번이 이걸 막는다.

### 0-5. 상태 객체 필드 규약

- **도메인 접두사 필수** — `account_*` / `match_*` / `village_*`
- 🔴 **매치 재화와 계정 재화를 같은 필드·같은 저장 키에 합치지 않는다**
- `AccountProfile` 은 **`SchemaVersion` 필수**, M1 최초값 `1`
- 영구 저장 진입점은 **`PlayerDBManager` 하나뿐**
- 🔴 **저장은 read-modify-write.** 통째로 덮어쓰면 다른 도메인 필드가 지워진다
- ⚠️ **선언 없는 `self.<name>` 대입은 런타임 에러**(`cannot set <name>, no such field`) → `property` 선언 또는 `self._T.<name>`

상태 객체 (GDD §4.10): `AccountProfile`(영구) · `MatchLobbyState` · `MatchSession` · `MatchPlayerState` · `VillageMatchState` · `MinionFlowState`

`MatchPlayerState`의 플레이어 성장 원장은 `match_stats` · `match_inventory` · `match_equipped` · `match_ap` · `match_sp`다. 장비·인벤토리 목록이 ExecSpace 경계를 넘을 때는 `any` table이 아니라 문자열로 직렬화한다.

---

## 1. 🔴 신규 시스템 등록 절차

**문제**: 이 계약서는 **이미 아는 것**만 다룬다. 아무도 계획하지 않은 새 시스템(예: 플레이어 스킬 시전)은 협약 밖에서 태어나 각자 방식으로 만들어지고 **합칠 때 터진다.** 협약을 아무리 잘 맺어도 "새로 만드는 것"에 대한 협약이 없으면 소용없다.

**해결**: 새 시스템은 **구현 전에 등록을 먼저 머지**한다. 이벤트 3종 세트(§B-2)의 확장판이며 같은 논리다.

### 등록서 8항목

| # | 항목 | 이게 없으면 merge에서 무슨 일이 나나 |
|---|---|---|
| 1 | 시스템 이름 + **소유 폴더** | 폴더가 갈려야 `.mlua` 충돌이 원천 차단된다 |
| 2 | 새 CSV 표 — 파일명 · 헤더 · 기본 키 (없으면 "없음") | 이름·열이 미리 갈려야 `merge=union` 이 안전하다 |
| 3 | 새 이벤트 — **발행할 것 / 구독할 것** | 미리 선언돼야 상대가 구독할 수 있다. 나중에 하면 **발행자 파일을 고쳐야 한다** |
| 4 | 새 열거값 | §0-2 정본에 합류. 같은 개념을 다른 값으로 쓰는 걸 막는다 |
| 5 | ID 대역 / 문자열 접두사 | union 머지는 **ID 중복을 안 잡아준다** |
| 6 | 저장 필드 — 어느 상태 객체에 무슨 필드 + 접두사 | 통짜 덮어쓰기로 남의 필드가 지워지는 사고를 막는다 |
| 7 | 새 `.model` / `.ui` 파일명 | 새 파일이면 안전, 기존 파일이면 소유권 협의 |
| 8 | 🔴 **건드리는 기존 파일 전부** | **여기가 유일한 협의 지점이다** |

### 절차

1. 만들 사람이 이 문서에 **등록 항목을 추가**해 PR을 올린다 — **코드 없이 문서만**
2. 상대는 **8번**과 **2·5번**만 본다
3. 머지된 뒤에 구현을 시작한다

> **8번이 비어 있으면 = "새 파일만 만든다" = 사실상 통과.** 8번에 뭐가 있으면 거기만 이야기하면 된다.

### 등록 대상 판정

아래 중 하나라도 해당하면 등록한다 — **새 CSV 표** / **새 이벤트** / **새 열거값** / **상태 객체 필드 추가** / **남의 폴더 파일 수정**.
해당 없으면(내 폴더 안에서 새 `.mlua` 만 추가) 그냥 만든다.

### 등록된 시스템

| 시스템 | 소유 | 상태 |
|---|---|---|
| 스킬 시전 · 스킬 UI | 상대 (`Skill/`) | ⬜ **등록서 대기** |
| 전직 | 상대 (`Job/`) | ⬜ **등록서 대기** — 🔴 **10레벨 전직이 마을 획득의 전제**라 사용자 골격의 크리티컬 패스에 있다. `JobChangedEvent` → 코어 연결 자격 부여 인터페이스가 최우선 협의 항목 |
| 플레이어 스탯 · 장비 · 강화 · 기능 NPC · UI | A (`Stat/` `Item/` `Npc/`) | 🟡 **등록 문서 PR 진행 중** — 경계면 `PlayerAttack.mlua` · `StatusHUD.ui` · `LevelTable.csv`은 B 리뷰 필요 |

#### 등록서 — 플레이어 스탯 · 장비 · 강화 · 기능 NPC · UI

| # | 항목 | 등록 내용 |
|---|---|---|
| 1 | 시스템·소유 폴더 | `Stat/`(스탯·데미지) · `Item/`(인벤토리·장비·강화) · `Npc/`(기능 NPC), 전부 A |
| 2 | 신규 CSV | `ItemInfo` · `EnhanceTable` · `GemInfo` · `GemDropTable` · `VillageNpcSector` · `FunctionalNpcCatalog` · `EnhanceSlotBonus`(2026-09-05 추가 · ⑫) — 헤더·기본 키는 A-2-8 이하가 정본 |
| 3 | 이벤트 | 발행 `StatRecalculatedEvent` · `EquipChangedEvent` · `ApAllocatedEvent` · `SpSpentEvent`; 구독 `MatchLevelChangedEvent` · `JobChangedEvent` · `ItemEnhancedEvent` · `GemSpentEvent` · `NpcOpenedEvent` |
| 4 | 열거값 | `StatId` 13종 · `EquipSlot` 6종 · `ItemType` 4종 · `SectorId` 4종 |
| 5 | ID | `ItemId` · `StatId` · `SectorId`는 대문자 열거값 문자열. `ItemInstanceId`는 서버가 발급하는 불투명 문자열 |
| 6 | 상태 | `MatchPlayerState`에 `match_stats` · `match_inventory` · `match_equipped` · `match_ap` · `match_sp` |
| 7 | 신규 UI | `CharacterGroup` · `VillageWorkshopGroup` · `VillageLifeGroup` · `VillageRecordGroup` · `VillageDefenseGroup` · `CommonNpcGroup` |
| 8 | 기존 파일 | `PlayerAttack.mlua`(B 승인) · `StatusHUD.ui` · `ToastGroup.ui` · `LevelTable.csv` · 이 계약서 · 협업 규칙 · M1 GDD · 정합성 검사 |

> NPC 스폰을 실제로 켜기 전, 기존 `NpcInfo.ModelId=staticnpc`가 Maker에서 유효한 네이티브 모델인지 확인한다. 유효하지 않으면 스포너 PR에서 검증된 공용 NPC 모델을 추가 등록한다. 모델 누락을 경고로 숨긴 채 스포너를 연결하지 않는다.

---

# A부. 데이터 표

## A-1. 현존 표 8종

### A-1-1. 🔴 정본화 대상 2종 — 두 월드가 갈라져 있다

병합 전 상태: 밍키타 `MonsterInfo` 9열(`…ModelId,MoveSpeed,JumpForce`) vs 안농 10열(`…Level,MaxHP,Exp,…,MoveType,AiType`). `MaxHP` 가 4번째에 삽입돼 뒤 열이 전부 밀렸고 `#Note` 위치도 다르다. `MonsterCatalog.mlua` 도 읽는 열이 서로 다르다.

**정본 (합집합. 안농 열 순서 + 밍키타 2열을 뒤에):**

```
MonsterInfo.csv
Id,Name,Level,MaxHp,Exp,CoinMin,CoinMax,ModelId,MoveType,AiType,MoveSpeed,JumpForce
```

| 열 | 타입 | 기본값 | 설명 |
|---|---|---|---|
| `Id` | string(정수 문자열) | — | **기본 키.** §0-4 |
| `Name` / `Level` | string / integer | `""` / `0` | 표시용 |
| `MaxHp` | integer | `1` | 밍키타 출신 행은 빈칸 → 기본값 |
| `Exp` · `CoinMin` · `CoinMax` | integer | `0` | 처치 보상 |
| `ModelId` | string | `""` | 스폰할 `.model` id |
| `MoveType` · `AiType` | enum(소문자) | `walk` / `wander` | §0-2 |
| `MoveSpeed` · `JumpForce` | number | `-1` | `-1` = 모델 기본값. 안농 출신 행은 빈칸 |

```
MapMonsters.csv
MapName,MonsterId,X,Y,SpawnerId,SpawnId,SpawnCount,RespawnSeconds,Enabled,#Note
```

`MapName`+`MonsterId` 복합키. `X`/`Y` world unit. `SpawnerId` = `<MapName>_SP001`, `SpawnId` = `<SpawnerId>_M<MonsterId>`. `SpawnCount` 만큼 독립 슬롯, 각 슬롯이 `RespawnSeconds` 뒤 한 마리 재생성.

> 🔴 정본화 시 **`MonsterCatalog.mlua` 도 한 벌로 통일**하고 신규 열 전부에 §0-1(5) 폴백을 적용한다.

### A-1-2. 나머지 6종 (그대로 유지)

```
NpcInfo.csv        Id,Name,ModelId
MapNpcs.csv        MapName,NpcId,X,Y,#Note
LevelTable.csv     Level,NeedExp,AddMaxHp,AddMaxMp,AP,SP,#Note
SummonUnits.csv    Key,Name,Level,Meso,ModelId,#Note
WorldMapNodes.csv  MapName,Label,Region,Type,NodeEntity,X,Y,ShowLabel,LabelDX,LabelDY,Enabled,#Note
PortalRoutes.csv   RouteId,FromMap,FromPortal,ToMap,ToPortal,Enabled,#Note
```

| 표 | 비고 |
|---|---|
| **`LevelTable`** | 🔴 **GDD의 레벨 곡선 역할을 이 표가 겸한다.** 새 표를 만들지 않는다 — `MonsterCatalog.mlua` 가 이미 `LevelTableName` 으로 읽고 있다. **10(전직) / 16 / 21(침범·발록)** 이 페이즈 기대 지점 |
| **`WorldMapNodes`** | ⚠️ GDD에 없던 표. **이 문서가 유일한 기록** |
| **`PortalRoutes`** | ⚠️ GDD에 없던 표. 포탈 **물리 배선**. `MatchRouteConfig`(이동 허용 목록)와는 층이 다른 별개 표 — 키 이름을 `RouteId` / `MatchRouteId` 로 갈라 혼동을 막는다 |

---

## A-2. MVP(Phase 1)에 필요한 신규 표 — 헤더 확정

> 좌표가 들어가는 표는 **MapBuilder 실측 후 값을 채운다.** 헤더는 지금 확정한다.

### A-2-1. `VillageConfig` — 마을·레인 정의

```
VillageId,VillageMapName,Lane2MapName,Lane1MapName,RearMapName,BossMapName,ThemeId,CoreX,CoreY,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `VillageId` | **키.** 6종 |
| `VillageMapName` | 마을 맵 루트 엔티티 이름 (실제 이름) |
| `Lane2MapName` | 사냥터2 — **1차포탑**이 있는 맵 |
| `Lane1MapName` | 사냥터1 — **2차포탑·억제기**가 있는 맵 |
| `RearMapName` | 후방 지선 (미니언 안 옴) |
| `BossMapName` | 보스맵 |
| `CoreX` / `CoreY` | 코어 앵커 — 여섯갈래길 방향 포탈 옆 (실측) |

**6행 고정.** GDD §4.1 매핑표가 값의 출처다.
⚠️ **커닝시티만 레인이 4단**(`SubwayLine1 → Village_SubwayEntrance → ConstructionSite → 마을`). `Village_SubwayEntrance` 는 포탑 없이 통과만 시키고, 길이 차이는 `LaneConfig` 의 미니언 이동 속도로 보정한다.

### A-2-2. `LaneConfig` — 레인 지형·슬롯

```
VillageId,MapName,LaneRole,PathMinX,PathMaxX,PathY,SpawnX,SpawnY,EndTriggerX,TowerSlotX,TowerSlotY,RopeX,MinionSpeedMul,DefenderSlotCount,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `VillageId` + `MapName` | **복합키.** 마을당 3행(사냥터2 · 사냥터1 · 마을) |
| `LaneRole` | `LANE2` / `LANE1` / `VILLAGE` / `PASSTHROUGH`(커닝시티 지하철역) |
| `PathMinX` · `PathMaxX` · `PathY` | **하단 1자 길**의 x 범위와 y (실측) |
| `SpawnX` / `SpawnY` | 미니언 진입 지점 (`LANE2` 만) |
| `EndTriggerX` | 레인 끝 트리거 — 미니언이 닿으면 전선이 다음 맵으로 |
| `TowerSlotX` / `TowerSlotY` | 포탑 슬롯. `LANE1` 은 2차포탑·억제기 2행으로 나눠 쓴다 |
| `RopeX` | 위 발판 ↔ 아래 길을 잇는 밧줄 위치 |
| `MinionSpeedMul` | 레인 길이 차이 보정 |
| `DefenderSlotCount` | 수비 몬스터 배치 슬롯 수 |

> 🔴 **난이도 편차는 지형이 아니라 이 표로 보정한다.**
> ⚠️ **하단 1자 길이 없는 맵은 발판을 새로 깔아야 한다.** 발판 저작은 빌더 커버리지 밖 = **Maker 수작업**. 마을당 3맵 × 6 = **18개 맵**이 이 마일스톤 최대 물리 작업이다.

### A-2-3. `VillageCoreConfig` — 코어

```
VillageId,MaxHp,RequiredLevel,ExposeCondition,Enabled,#Note
```

`RequiredLevel` 기본 **10**(전직 완료 = 연결 자격). `ExposeCondition` 기본 `SUPPRESSOR_DESTROYED`.

### A-2-4. `TowerConfig` — 포탑 3종 + 코어

```
Stage,Level,MaxHp,Attack,AttackSpeed,Range,UpgradeCost,RepairCostPerHp,RebuildCost,SuppressorMinionHpMul,SuppressorMinionAtkMul,SuppressorExpMul,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Stage` + `Level` | **복합키.** `Stage` = `TOWER1`/`TOWER2`/`SUPPRESSOR`/`CORE`, `Level` = 1~3 (코어는 1) |
| `MaxHp` · `Attack` · `AttackSpeed` · `Range` | 코어는 `MaxHp` 만 의미 있다 |
| `UpgradeCost` · `RepairCostPerHp` · `RebuildCost` | 메소 + **엘리트 재료** (§A-3) |
| `Suppressor*Mul` | 🔴 **억제기의 역설** — 억제기 파괴 시 미니언 HP·공격력 배율과 **경험치 상승률**. `SUPPRESSOR` 행에만 값이 있다 |

⚠️ **1차포탑은 3페이즈쯤 거의 반드시 부서지도록** 수치를 잡는다. 안 부서지면 전선이 안 밀려 판이 끝나지 않는다.

### A-2-5. `MinionPhaseConfig` — 페이즈 시계

```
Phase,StartSeconds,SpawnIntervalSeconds,HpMul,AtkMul,ExpBase,MesoBase,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `Phase` | **키.** `MatchPhase` 4종 |
| `StartSeconds` | 🔴 **매치 경과 시간** 기준 전환 시각. **레벨 기준이 아니다** — 개인 레벨로 갈리면 "일부러 안 크는 버티기"가 성립한다 |
| `SpawnIntervalSeconds` | 미니언 투입 간격 |
| `HpMul` · `AtkMul` | GDD 4페이즈 곡선 — 1(없음) / 2(HP↑ 공격↓) / 2.5(공격 급상승) / 3(둘 다 급상승) |
| `ExpBase` · `MesoBase` | 미니언 처치 보상 기준값 |

**4행 고정.** `PHASE1_PIONEER` 는 미니언이 없으므로 `SpawnIntervalSeconds` 를 `0` 으로 둔다.
⚠️ **25분경 감당 불가**가 실질 제한 시간이다. 별도 타이머를 두지 않으므로 이 곡선이 곧 타이머다.

### A-2-6. `MinionComposition` — 미니언 구성

```
Phase,MonsterId,Weight,Level,Enabled,#Note
```

한 페이즈에 여러 종이면 **여러 행**. `Weight` 로 투입 비율을 정한다. `MonsterId` 는 `MonsterInfo.Id` 참조 (안농 도감 77종에서 고름).

### A-2-7. `DifficultyConfig` — 매치 전체 난이도

```
DifficultyLevel,DisplayName,MonsterHpMul,MonsterAtkMul,MonsterExpMul,MinionHpMul,MinionAtkMul,MinionIntervalMul,MesoMul,GemDropMul,EliteChanceMul,CostMul,Enabled,#Note
```

**5행 고정 (1~5).** 로비에서 고른다.

🔴 **규약**
- **기존 표에 난이도 열을 추가하지 않는다.** 배수는 이 표에만
- **배수 적용은 서버 한 곳에서만** — 각자 곱하면 이중 적용·누락이 생긴다. 적용 지점은 `Match/` 소유
- 난이도는 매치 시작 시 확정, `MatchStartedEvent` payload에 포함
- `MinionPhaseConfig` 의 페이즈별 상승과 **다른 축**이다. **최종값 = 기준값 × 페이즈 배율 × 난이도 배율** (이 순서 고정)

### A-2-8. `FunctionalNpcCatalog` — 기능 NPC 계약

```
CatalogNpcId,RoleKey,DisplayName,UiGroupName,UiRoute,ActionRoute,OwnershipMode,SectorId,SlotOrder,Enabled,#Note
```

| 열 | 설명 |
|---|---|
| `CatalogNpcId` | **키.** `VD_` 접두 (`VD_WORKSHOP_CRAFT`) |
| `RoleKey` | `NpcRole` §0-2 |
| **`UiGroupName`** | 열 UIGroup — **라우터가 이 값으로 그룹을 켠다** |
| `UiRoute` | 그룹 안에서 열 팝업 (`craft`) |
| `ActionRoute` | 서버 액션 라우트 |
| `OwnershipMode` | `OWNER_ONLY` / `PUBLIC` |
| `SectorId` | 마을 기능 NPC의 구역. 공용 2종은 빈 칸이며 개별 좌표 오버라이드가 필요하다 |
| `SlotOrder` | 같은 구역 안에서 앵커 오프셋을 계산할 순서. 1부터 시작 |

**M1 기준 12행** (마을 역할 10 + 공용 2). **확장 가능한 표다** — 역할을 늘리면 행을 추가한다.
런타임 인스턴스는 10 × 6마을 = **60개** + 공용 2개.

> `UiGroupName` 열 덕분에 **팝업 추가 = 새 `.ui` 노드 + CSV 행 1개**가 되고 라우터 스크립트를 수정하지 않는다.

### A-2-8a. `VillageNpcSector` — 마을 기능 NPC 앵커

```
VillageId,SectorId,AnchorX,AnchorY,SlotDX,SlotDY,SlotPerRow,FlipX,Enabled,#Note
```

- 기본 키는 `VillageId` + `SectorId`. 6마을 × 4구역, **24행**이다.
- `AnchorX`/`AnchorY`는 디자이너가 Maker에서 실측해 채운다. 좌표는 world unit이다.
- `SlotDX`/`SlotDY`/`SlotPerRow`로 `FunctionalNpcCatalog.SlotOrder`의 자동 배치 좌표를 계산한다.
- 개별 위치가 필요하면 선택적 `MapNpcs_Village.csv` 오버라이드가 우선한다. 행도 앵커도 없으면 스폰을 건너뛰고 서버 경고 로그를 남긴다.

### A-2-8b. 플레이어 장비 · 강화 표

#### `ItemInfo`

```
ItemId,Name,ItemType,EquipSlot,ReqLevel,ReqJob,IconRUID,BaseStr,BaseDex,BaseInt,BaseLuk,BaseAttack,BaseMagic,BaseDefense,BaseSpeed,BaseJump,BaseAccuracy,BaseAvoid,BaseMaxHp,BaseMaxMp,Mastery,MaxDurability,MaxEnhanceLevel,Stackable,MaxStack,SellMeso,Enabled,#Note
```

`ItemId`가 기본 키다. `ItemType=EQUIP`만 `EquipSlot` 6종과 `Mastery`를 가진다. 아이콘은 `.ui`에 고정하지 않고 `IconRUID`로 런타임 주입한다.

#### `EnhanceTable`

```
EnhanceLevel,MesoCost,GemCount,SuccessRate,DestroyRate,DowngradeOnFail,AddDefense,Enabled,#Note
```

`EnhanceLevel`이 기본 키다. M1은 1~3단계, `SuccessRate=1.0`, `DestroyRate=0`으로 시작한다. `AddDefense`는 2026-09-05 부로 `EnhanceSlotBonus`로 이관됐다 — 열은 남기되(삭제 금지) 값은 0을 유지하고, **코드는 이 열을 더 이상 읽지 않는다**(`EquipService.RecalcLayer` · 강화창 미리보기).

#### `EnhanceSlotBonus` (2026-09-05 추가 · ⑫)

```
EquipSlot,ReqLevel,EnhanceLevel,AddStr,AddDex,AddInt,AddLuk,AddAttack,AddMagic,AddDefense,AddSpeed,AddJump,AddAccuracy,AddAvoid,AddMaxHp,AddMaxMp,Enabled,#Note
```

기본 키는 `EquipSlot` + `ReqLevel` + `EnhanceLevel`. 아이템은 표의 `ReqLevel` 중 **"자기 `ReqLevel` 이하의 최대"** 티어에 매칭된다(예: 요구레벨 15 → 티어 10). `Add*` 열은 `ItemInfo`의 `Base*` 13종과 같은 순서다.

**의미(B안 · 사용자 확정 2026-09-05)** — 강화 단계마다:
- `AddDefense` = 보석과 무관한 **부위 고정 상승치**(방어구만 값이 있다 · 툴팁 초록).
- 나머지 `Add*` = 그 단계에 소모한 **보석 1개당** 그 보석 스탯의 상승량. 보석은 "어느 스탯에 줄지"만 고르고 양은 이 표가 정한다(다이아 → `AddAttack`, 힘의 결정 → `AddStr` …). 값이 0인 열의 보석은 그 부위에 효과가 없으며 UI가 비활성·서버가 거절한다(무기 외 부위의 다이아·사파이어). 툴팁 보라.

규칙: 요구레벨이 높은 장비일수록 단계당 상승이 크고, 10렙 장비 풀강(주스탯 보석 6개) ≈ 20렙 장비 기본치 ×1.05. 30렙 티어는 상한 없이 높게 잡는다. ENHANCE 레이어 = 고정치 누적 + 보석별 양 누적(`ItemCatalog.ComputeEnhance` 한 곳에서 계산 · 장비창/툴팁/강화창 미리보기가 같은 식을 쓴다).

#### `GemInfo`

```
GemId,StatId,AddPerLevel,DisplayName,IconRUID,Enabled,#Note
```

`GemId`가 기본 키다. 12종 보석은 `StatId`와 1:1이며, `DEFENSE`는 보석이 아니라 `EnhanceSlotBonus.AddDefense`로만 오른다. `AddPerLevel`은 2026-09-05 B안부터 **미사용**(열은 남기고 값 0 유지 · 상승량은 `EnhanceSlotBonus`의 해당 스탯 열).

#### `GemDropTable`

```
SourceId,SourceKind,GemId,Chance,CountMin,CountMax,Enabled,#Note
```

기본 키는 `SourceKind` + `SourceId` + `GemId`. `SourceKind`는 `MONSTER` / `ELITE` / `BOSS`다. `SourceId`는 해당 도감·보스 ID를 가리킨다.

### A-2-9. `MatchRouteConfig` — 이동 허용 목록

```
MatchRouteId,MapName,RouteKind,Enabled,#Note
```

`RouteKind` = `VILLAGE`/`HUB`/`LANE`/`REAR`/`BOSS`. 키를 `MatchRouteId` 로 둔 것은 `PortalRoutes.RouteId` 와 구분하기 위함이다.
> 이 표가 비어 있으면 테스트 재화로 개발은 가능하나 **M1 릴리스 완료로 판정하지 않는다.**

### A-2-10. `BalrogChallenge` · `BalrogRoom` — 승리 조건

**✅ 개인 채널 방식 확정 (2026-08-29, 구 미정 #1 해소)** — MSW의 채널 격리에 의존하지 않는다.
**발록 방을 여러 개 만들고, 누가 들어가 있으면 다른 사람은 입장을 막는다.** 평범한 맵 + 입장 판정이라 특수 기능이 필요 없다.

```
BalrogChallenge.csv
Key,MesoCost,RequiredLevel,RoomCount,OccupancyTimeoutSeconds,Enabled,#Note
```

단일 행. `RequiredLevel` 기본 **21**. `RoomCount` = 발록 방 수(**최대 인원과 같게 둔다 — 6**). `OccupancyTimeoutSeconds` = 점유자가 응답 없을 때 자동 해제 시간.

```
BalrogRoom.csv
RoomId,MapName,SpawnX,SpawnY,BalrogModelId,Enabled,#Note
```

`RoomCount` 만큼의 행. 같은 지형을 복제한 맵들이다.

**점유 규칙 (서버가 소유)**
- 도전 신청 → 메소 차감 → **빈 방을 하나 배정**하고 점유 표시 → 그 방으로 이동
- 점유 중인 방에는 **다른 사람이 입장할 수 없다** (포탈에서 거절 + 토스트)
- 해제 조건: 발록 처치 / 플레이어 사망 / 맵 이탈 / **`OccupancyTimeoutSeconds` 초과**
- 빈 방이 없으면 **거절 토스트** (`RoomCount` 를 최대 인원과 같게 두면 이론상 안 생긴다)
- 발록은 **방마다 따로 스폰**한다

🔴 **선취 판정은 서버가 1회만** 한다 → `BalrogKilledEvent(SettlementId)`. 두 사람이 거의 동시에 잡아도 승자는 하나다.

### A-2-11. `BossReward` — 보스 보상

```
BossId,TopDamageItemId,TopDamageMeso,FirstClaimItemId,FirstClaimMeso,FirstClaimOnce,Enabled,#Note
```

- `TopDamage*` — **최다 누적 피해자** 처치 판정 보상
- `FirstClaim*` — **선착순 1명** 보상. 마노 = 10레벨 제한 **체력 갑옷**(공격력 상승 없음) + 메소
- `FirstClaimOnce` = `true` 면 매치당 1회

### A-2-12. `MapNpcs` 분할

핫 파일 완화를 위해 담당별로 나눈다. `NpcCatalog` 는 **쉼표 구분 테이블 이름 목록**을 읽어 순회한다.

| 파일 | 담당 | 내용 |
|---|---|---|
| `MapNpcs.csv` | 맵 임포트 | 원본·장식 NPC (기존 행 전부 보존) |
| **`MapNpcs_Village.csv`** | 마을 기능 NPC 좌표 오버라이드 | `VillageNpcSector` 자동 배치로 해결되지 않는 `VD_*` 개별 위치만. 파일 자체는 선택적 |

헤더 동일: `MapName,NpcId,X,Y,#Note`. 실제로 만드는 CSV마다 `.userdataset` 짝이 필요하다.

```
property string NpcMapTableNames = "MapNpcs,MapNpcs_Village"
```

`NpcCatalog`는 선택적 `MapNpcs_Village`가 없으면 이를 오류로 처리하지 않고 섹터 앵커 배치만 사용한다.

---

### A-2-13. `BossInfo` — 보스 정의 · 밸런싱 · 스폰

```
BossId,Name,Level,MaxHp,ModelId,MoveType,AiType,
AttackPower,Defense,KnockbackResist,MoveSpeed,
MapName,SpawnX,SpawnY,RespawnSeconds,Enabled,
DamagedSoundRUID,DieSoundRUID,
ContactScaleX,ContactScaleY,ContactOffsetX,MovePlayRate,DetectionRange,
JumpForce,#Note
```

**보스는 `MonsterInfo` 에 넣지 않는다.** 극소수(7종)이고 일반 몹과 축이 다르다 — 페이즈 스킬, 최다피해 판정, 선취 보상, 승리 조건이 전부 보스에만 있다. `MonsterInfo` 에 열을 붙이면 77행 전부가 빈칸을 갖게 된다.

| 열 | 의미 |
|---|---|
| `MaxHp` | **기준선 = 최상위 일반몹 15,000 × 150 = 2,250,000.** 패턴 수로 보정(적을수록 높게) |
| `Defense` | 0~100. 보스 20~50 (일반몹은 0) |
| `KnockbackResist` | 0~100. 보스 `100` = 안 밀림 |
| `SpawnX`/`SpawnY` | **빈칸이면 기존 배치를 그대로 쓴다** (피아누스가 그렇다) |
| `RespawnSeconds` | 보스 기본 `300` |
| `DamagedSoundRUID` | 보스가 **맞을 때** 나는 소리. 팩의 `_audio/Damage` |
| `DieSoundRUID` | 보스가 **죽을 때** 나는 소리. 팩의 `_audio/Die` |
| `ContactScaleX`/`Y` | 🔴 **몸통 접촉 판정 박스 보정 배율.** 접촉 박스는 스프라이트 **전체 크기**에서 뽑는데 원본에는 더듬이·꼬리·여백이 같이 들어 있어 실제 몸통보다 훨씬 크게 잡힌다. `1.0` 이면 보정 없음 |
| `ContactOffsetX` | 몸통이 스프라이트 중앙에 없을 때 좌우로 민다(월드 단위). **보는 방향으로 부호가 뒤집힌다** — 방향 전환은 `Scale.x` 가 아니라 `sprite.FlipX` 라 이걸 안 뒤집으면 한쪽을 볼 때만 박스가 앞으로 튀어나온다 |
| `MovePlayRate` | 평상시(이동·대기) 애니메이션 재생 속도. **이동 속도와 어긋나면 제자리에서 발버둥치는 것처럼 보인다** (킹슬라임 `0.5`) |
| `DetectionRange` | 처음 발견하는 거리. 음수면 모델 기본값. 보스는 `10` (화면 폭 12.8 대비 5는 40%뿐이라 늦게 반응했다) |
| `JumpForce` | 🔴 **양수일 때만 점프한다.** 이게 곧 스위치다 — 코드에 몹 이름을 박지 않는다. **보스 7종은 전부 `-1`**(점프 안 함, 사용자 확정). 실측상 도달 높이 ≒ `JumpForce` |

> ⚠️ **마노만 기준선이 다르다.** GDD 상 1페이즈 선착순 보스라 초반에 잡혀야 한다.
> 같은 ×150 배율을 **Lv10 구간 일반몹 125** 에 적용해 `18,750`. 배율 규칙 자체는 안 깬다.
> `AttackPower` 는 전 행 **튜닝 대상** — `LevelTable` 이 아직 자리표시자라 역산 근거가 없다.

**보상은 이 표에 없다.** `BossReward`(A-2-11) 가 갖는다. 보석은 `GemDropTable`(A-3).

### A-2-14. `BossSkills` — 보스 스킬 (1:N)

```
BossId,SkillId,SkillName,Priority,ShapeType,RangeX,RangeY,OffsetX,OffsetY,
CastDelay,Cooldown,Damage,HitCount,MinHpPercent,MaxHpPercent,
ProjectileModelId,ProjectileSpeed,CastRUID,Enabled,
ProjectileRUID,HitEffectRUID,RecoverTime,CastEffectRUID,CastEffectBehind,
CastSoundRUID,HitSoundRUID,CastEffectOffsetX,CastEffectOffsetY,CastEffectScale,
CastPlayRate,CastRUID2,CastDelay2,CastHitFrame,CastEffectAtStart,
CastEffectPlayRate,CastEffectStartFrame,CastEffectEndFrame,
ShotCount,ShotInterval,AimAtTarget,BuffMoveRate,BuffDuration,
Reach,LoopStartFrame,LoopEndFrame,CloseEndFrame,#Note
```

보스당 1~9행. 상한은 리소스 팩의 `attack*`/`skill*` 클립 수 + 소환/페이크 행이다 —
마노 2 · 머쉬맘 3 · 킹슬라임 3 · 스텀피 5 · 주니어발록 6 · 피아누스 7 · 에피네아 9.

> ⚠️ **`#Note` 에 쉼표를 쓰지 않는다.** csv 가 따옴표로 감싸면 MSW DataSet 파서가 열을 통째로 민다.
> 쉼표 대신 ` ·` 를 쓴다.

| 열 | 의미 |
|---|---|
| `ShapeType` | **`target`** 전방 최근접 1명 / **`box`** 전방 전원 / **`ground`** 점프로 회피(지진형) / **`crouchable`** 숙여서 회피(상단 휘두르기) / **`projectile`** / **`fake`** 판정 없음(페이크 모션) / **`summon`** 부하 소환 / **`buff`** 범위 내 아군 강화 |
| | ⚠️ 회피는 **박스 높이가 아니라 상태로 판정**한다. 플레이어 `HitComponent` 는 점프·숙이기로 크기가 바뀌지 않으므로 박스만으로는 절대 못 피한다 |
| `ProjectileModelId` | 이 스킬이 **스폰하는 모델**. `projectile` 이면 투사체, `summon` 이면 소환수 |
| `ProjectileRUID` | 투사체 스프라이트. **보스 자기 팩의 `clip/info/ball`** 을 쓴다. 비면 모델 기본값 |
| `HitEffectRUID` | 명중 시 **맞은 대상 자리**에 뜨는 이펙트. 팩의 `clip/info/hit` |
| `RecoverTime` | 판정 후 시전 자세를 붙잡는 시간. **0 이면 판정 순간 idle 로 튄다** |
| `CastEffectRUID` | 시전 중 보스 주변에 띄우는 이펙트. **날아가지 않는다** (마법진 등) |
| `CastEffectOffsetX`/`Y`/`CastEffectScale` | 🔴 **이펙트 전용 좌표·배율.** 판정 박스의 `OffsetX`/`OffsetY` 와 **별개다** |
| | 원본 이펙트는 pivot 에 위치가 박혀 있는데 **기준이 자산마다 다르다.** `attack2/info/effect`=`(-55,-57)` 은 원점에 놓으면 손 높이에 맞고, `attack3/info/effect`=`(138,-27)` 은 원점에 놓으면 몸통에 겹친다. 하나의 규칙으로 못 맞추므로 **자산별로 실측해서 넣는다.** `OffsetX` 는 보는 방향으로 부호가 뒤집힌다 |
| `CastEffectBehind` | `true` 면 보스 **뒤**(`OrderInLayer` 1), `false` 면 앞(3). 배경보다는 항상 앞 |
| `OffsetX` | 보스가 보는 방향으로 **부호가 뒤집힌다** |
| `CastDelay` | 예고~판정 (초). 이 구간에 `CastRUID` 모션이 돈다 |
| `MinHpPercent`~`MaxHpPercent` | 🔴 **페이즈 게이트** — 보스 HP% 가 이 구간일 때만 후보 |
| `CastRUID` | 시전 모션 animationclip. 팩의 `attack*`/`skill*` |
| `Damage` | 이 스킬 고유. `BossInfo.AttackPower` 와 **별개** |
| `CastSoundRUID` | 시전 순간 나는 소리. 팩의 `_audio/AttackN` · `_audio/SkillN` |
| `HitSoundRUID` | **플레이어가 맞았을 때** 나는 소리. 팩의 `_audio/CharDamN` |
| `CastDelay` **`= -1`** | 🔴 **클립 실측 자동.** `AnimationClip.Frames[i].Delay` 합계 ÷ `CastPlayRate`. 프레임 지연을 모르면 추측하지 말고 이걸 쓴다 — 머쉬맘 5프레임이 실측 **1.85초**(가정은 0.5초)였다 |
| `CastPlayRate` | 시전 모션 재생 속도. 클립을 복제해 프레임을 늘릴 필요 없다. **`CastDelay -1` 과 연동**되어 속도를 낮추면 시전 시간이 따라 늘어난다 |
| `CastHitFrame` | 판정이 나는 프레임 수(앞에서부터). **점프 클립은 마지막 프레임이 착지 후 복귀 자세라 길다** — 클립 전체로 재면 바닥에 닿고도 한참 기다린다. `0` 이하면 전체 |
| `CastRUID2`/`CastDelay2` | 2단 시전. 준비 동작 뒤에 다른 그림을 한 번 더 끼운다. 판정은 둘 다 지난 뒤(점프면 착지 시점) |
| `CastEffectAtStart` | `true` 면 **시전 시작**에, `false` 면 **판정 순간**에 이펙트를 튼다. `CastEffectBehind`(레이어)와 **별개다** — 원본 이펙트에 도입 구간이 있으면(머쉬맘 지진파 f0~f7 이 잔먼지) 시작에 틀어야 착지에 본 장면이 온다 |
| `CastEffectPlayRate` | 이펙트 재생 속도. 여러 단계로 된 이펙트가 "두 번 터지는 것처럼" 보일 때 뭉친다 |
| `CastEffectStartFrame`/`EndFrame` | 이펙트 재생 구간. 원본에 폭발이 여러 번 들어 있을 때 한 번만 쓴다(킹슬라임 지진파 13프레임에 폭발 2회 → `f0~f2`). `EndFrame` 이 음수면 끝까지 |
| `ShotCount`/`ShotInterval` | **연사.** 발수와 발 사이 간격(초). `1` 이면 예전과 동일 |
| `AimAtTarget` | `true` 면 **발사 시점의 플레이어 좌표를 정조준.** 유도가 아니다 — 쏜 뒤엔 직진 |
| `LoopStartFrame`/`LoopEndFrame` | 연사 중 **반복 재생할 프레임 창.** 한 바퀴 = `ShotInterval` 이 되도록 재생속도를 자동 계산한다 (스텀피 `f5~f9` = 입을 오므렸다 열 때마다 한 발) |
| `CloseEndFrame` | 연사가 끝난 뒤 한 번 재생할 마무리 구간(입 닫기) |
| `CrouchBand` | number | `shapeType=crouchable` 전용. 판정 박스 **아래끝에서 위로 이만큼**만 숙이기가 통한다. 음수면 박스 전체(기존 동작). 없으면 이펙트 한가운데 2층에 서 있어도 숙이기만 하면 무조건 면제된다 — 숙이기가 위치와 무관한 전역 상태 검사이기 때문이다 |
| `FireEffectRUID` | string | **판정 순간**에 띄우는 두 번째 이펙트. `CastEffectRUID` 가 예고(시전 시작)라면 이것은 타격 연출이다. 비면 없음. 피아누스 S7 의 노란 전기 |
| `FireEffectScale` | number | `FireEffectRUID` 배율. 기본 1.0 |
| `FireEffectFloors` | string | **비우면 foothold 세그먼트 자동** — 판정 박스에 걸리는 발판 세그먼트를 전부 열거해 **세그먼트마다 그 길이에 딱 맞게 가로만 늘린 이펙트 1장**(경사는 각도까지). 긴 바닥은 박스 폭으로 잘리고 짧은 발판은 그 길이만큼만. 값을 주면 보스 y 기준 고정 오프셋을 `;` 로 나열(`0;0.6`). 🔴 쉼표가 아니라 `;` 다 |
| `FireEffectTiles` | integer | **고정 오프셋 모드 전용.** tile 자산을 판정 폭에 가로로 몇 장 이어 붙일지. 자동(세그먼트) 모드에서는 쓰지 않는다. 기본 1 |
| | 🔴 **프레임 창은 렌더러에 남는 전역 상태다.** 안 되돌리면 다음 클립이 남의 구간에 갇혀 몇 프레임만 깜빡인다 |
| `Reach` | 이 스킬이 닿는 거리. `-1` 이면 `OffsetX + RangeX/2` 자동 계산. 🔴 **몸통 접촉 박스 반폭보다 작으면 플레이어가 몸에 막혀 판정 범위 안으로 못 들어간다** — 그 스킬은 영원히 안 맞는다 |
| `BuffMoveRate`/`BuffDuration` | `shapeType=buff` 의 이동속도 배율과 지속시간(초) |

> 🔴 **`summon` — 부하 소환.** 열 3개를 빌려 쓴다:
> `ProjectileModelId` = 소환할 모델 · `HitCount` = 마리 수 · `RangeX` = 좌우로 퍼지는 폭
> (3마리면 `-RangeX` / `0` / `+RangeX`). `CastEffectRUID` 는 **각 소환 자리**에 깔린다.
> 소환수 능력치는 그 모델의 `BossInfo` 행을 그대로 쓴다 — 마노를 소환하면 진짜 마노(HP 18,750)가 나온다.
> `RespawnOn=false` 로 세워 죽으면 사라진다(안 그러면 무한 증식).
> **사거리 판정을 받지 않는다** — 추격 대상만 있으면 쓴다.
> 주니어발록 `S4` 가 이 방식이다 (HP 5% 이하 · 쿨 9999 = 사실상 1회).

> 🔴 **`fake` — 페이크 모션.** 시전 모션·소리는 진짜와 똑같고 발사 순간 **아무 판정도 안 낸다.**
> `RecoverTime` 을 `0` 으로 두면 발사 직전에 모션이 끊겨 "나올 줄 알았는데 안 나온" 느낌이 난다.
> `CastEffectRUID` / `HitEffectRUID` / `HitSoundRUID` 는 **반드시 비운다** — 하나라도 나오면 들통난다.
> `Priority` 를 진짜 스킬보다 낮게 둬야 진짜가 준비됐을 때 페이크가 새치기하지 않는다.
> 주니어발록 `S2F` / `S3F` 가 이 방식이다 (쿨 3배 · HP 70% 이하 전용).

> 🔴 시전 모션은 `SpriteRUID` 직접 대입 후 복원(Pattern A)으로 한다.
> `ChangeState("ATTACK")` 은 몬스터에 `ATTACK` 이 자동 등록되지 않아 **`[LEA-3005]`** 가 난다.

### A-2-15. 엘리트 3표 — 구조만 (2026-09-02)

A-3 에 예약돼 있던 것을 **열까지 확정해 여기로 옮긴다.** 파일도 만들었다(행은 0개).
로스터·재료 종수는 **여전히 미정**(로드맵 미정 #4)이며, 구조가 먼저 있어야 스키마 충돌 없이 각자 붙일 수 있다.

```
EliteMonsterInfo.csv
EliteId,Name,BaseMonsterId,Level,MaxHp,ModelId,MoveType,AiType,
AttackPower,Defense,KnockbackResist,MoveSpeed,
MaterialId,MaterialMin,MaterialMax,Exp,Meso,Enabled,#Note

EliteSpawnTable.csv
MapName,BaseMonsterId,EliteId,Chance,Enabled,#Note

EliteMaterialInfo.csv
MaterialId,Name,Description,IconRUID,Enabled,#Note
```

- `BaseMonsterId` — **어느 일반 몹 자리를 대신 차지하는가.** `Chance` 는 0~1 소수
- `MaterialId` — 🔴 **대문자 열거값 문자열** (§0 — 신규 도메인은 숫자 ID 를 쓰지 않는다)
- `다일`(`6220000`, Lv50 HP 300,000)이 첫 엘리트 후보다. 원본이 보스인데 일반몹 도감에 있다.
  ⚠️ **엘리트 스포너가 생기기 전까지는 `MonsterInfo` 에 그대로 둔다** — 지금 빼면
  `KerningCity_Hunt_SewerApproach` 스폰이 죽는다


## A-3. Phase 2+ 표 (이름·키·용도만 예약)

> 열은 해당 Phase 착수 시 이 문서에 추가한다. **미리 파일을 만들지 않는다.** `GemInfo`와 `GemDropTable`은 플레이어 장비·강화 계약으로 승격되어 A-2-8b가 정본이다.
> (예외: 엘리트 3표는 2026-09-02 에 구조만 먼저 확정해 **A-2-15** 로 옮겼다 — 사용자 지시)

| 표 | 키 | 용도 |
|---|---|---|
| `DispatchRule` | `Phase` | 파병 비용, 마을당 동시 상한, 취소 기한 |
| `CraftRecipe` | `RecipeId` | 투입 재료, 비용, 결과 아이템 |
| `MonsterRecruit` | `MonsterTypeId` | 구매 가격, 모델, 기본 전투 수치 |
| `MonsterTraining` | `MonsterTypeId` + `Level` | 시간, 비용, 병과 보너스 |
| `AchievementConfig` | `AchievementId` | 조건, 진행 단위, 보상 |

### 🔴 두 성장 축이 갈린다 (2026-08-29 확정)

| 축 | 재화 | 어디서 |
|---|---|---|
| **플레이어 강화** | 메소 + **보석** | `WORKSHOP_ENHANCE`(강화 장인) — 보석 종류가 오르는 능력치를 결정 |
| **방어 강화** | 메소 + **엘리트 재료** | `DEFENSE_TOWER`(포탑 관리관) — 포탑 3종 + 코어 |

**보석은 포탑에 쓰지 않고, 엘리트 재료는 장비에 쓰지 않는다.** 이 분리가 "엘리트를 잡아야만 후반 방어가 된다"는 압박을 만든다.

---

## A-3-1. 🔴 진영 번호 체계 (2026-09-02 확정)

`script.Faction.Team` 은 **문자열이지만 번호로 쓴다.** `FactionLogic` 이 정본이다.

| 대역 | 뜻 |
|---|---|
| `1` ~ `5` | **플레이어 캐릭터.** 캐릭터 본인만 들어간다 |
| `11` ~ `15` | **미니언.** `11` = "플레이어 1 을 치러 온 미니언" |
| `21` ~ `25` | **방어시설·자기 유닛.** `21` = "플레이어 1 의 터렛·수비 유닛" |
| `Monster` | **공통 몬스터.** 야생 몹 · 보스 · 파밍 몹 |
| `Neutral` | 그 외 전부. 공격도 버프도 안 통한다 |

**소유자 = 번호 % 10.** `1` · `11` · `21` 은 전부 플레이어 1 의 것이다.
**지정 대상은 별도 칸에 저장하지 않는다 — 팀 번호 자체가 지정 대상이다.**

### 관계 규칙

| A | B | 관계 | 왜 |
|---|---|---|---|
| `1` | `2`~`5` | **ALLY** | 🔴 플레이어 **캐릭터끼리는** 공격 불가. 경쟁은 사냥 스틸로만 |
| `1` | `21` | ALLY | 자기 시설 |
| `1` | `22`~`25` | **ENEMY** | 🔴 **남의 방어시설은 때릴 수 있다.** 보정은 시설 쪽 시스템이 갖는다 |
| `11` | `1` · `21` | ENEMY | 지정 대상의 것 |
| `11` | `2`~`5` · `22`~`25` | ALLY | 지정 대상이 아니면 건드리지 않는다 |
| `11` | `12`~`15` | ALLY | 미니언끼리는 안 싸운다 |
| `21` | `22`~`25` | ENEMY | 서로 다른 진영의 시설 |
| `Monster` | 전부 | ENEMY | 야생 몹·보스는 모두와 적대 |
| `Neutral` | 전부 | NEUTRAL | 공격·버프 전부 차단 |

같은 미니언이 상대에 따라 적/아군이 갈리므로 **팀 문자열 비교만으로는 표현이 안 된다.**
`FactionLogic.BandRelation` 특례가 일반 규칙(팀 다르면 적대)보다 먼저 적용된다.

### 진영이 없는 엔티티

`script.Faction` 을 가진 모델은 **10종뿐**(미니언 7 · 터렛 2 · 중립 1). 나머지 100종은 `GetTeam` 이 자동 판정한다:

1. `script.Faction` 이 있으면 그 팀 — `NormalizeTeam` 이 예전 이름을 흡수한다 (**`TeamA`→`21`**, `TeamB`→`11`)
2. `PlayerComponent` 가 있으면 `PlayerTeam` (기본 `1`)
3. `script.Monster` 또는 `script.FarmReward` 가 있으면 `Monster`
4. 그 외 `Neutral`

> 🔴 **3번이 없으면 `Models/Monsters` 82종(일반 몹 76 + 보스 8)이 전부 `Neutral` 로 떨어져 플레이어가 아무도 못 때린다.**
> 실제로 그 상태였다. 모델 82개에 컴포넌트를 붙이는 대신 판정 한 곳에서 해결한다.

> ⚠️ `TeamA` 는 `1` 이 아니라 **`21`** 로 간다. 그건 플레이어 캐릭터가 아니라 그 진영의 유닛·터렛이었다.
> `FactionAuraController.PlayerTeam` 만은 캐릭터 진영이라 `TeamA` 를 `1` 로 따로 받는다.

### 공격 규칙

**예외 없이 진영으로만 판정한다.** `PlayerAttack` / `FactionAttack` 모두 `_FactionLogic:IsEnemy` 하나만 본다.
"파밍 몹은 진영 무시" 같은 예외를 두면 진영 밖 엔티티가 생겨 위 사고가 재발한다.

---

## A-4. 공개 타입

GDD §5.1(2026-08-29 정정본)이 정본이다. 핵심만 재기재:

```
VillageConfig       VillageId · VillageMapName · Lane2MapName · Lane1MapName
                    RearMapName · BossMapName · ThemeId

NpcInstanceKey      OpaqueRuntimeKey  (클라에는 불투명 문자열)
                    서버 내부 해석: MatchId : VillageId : CatalogNpcId : SpawnOrdinal

PlayerStatState     BaseStat + EquipStat + EnhanceStat + BuffStat = FinalStat
                    STR · DEX · INT · LUK · ATTACK · MAGIC · DEFENSE
                    SPEED · JUMP · ACCURACY · AVOID · MAX_HP · MAX_MP

ItemInstance        InstanceId / ItemId / EnhanceLevel / Durability / AppliedGemsCsv
                    MatchPlayerState.match_inventory 또는 match_equipped[EquipSlot]

LaneState           VillageId · Stage(1~4) · SuppressorDown · TowerHp

MinionFlowState     MatchId · Phase · ElapsedSeconds · HpMul · AtkMul
                    StageByVillage (VillageId → Stage)

VillageCoreState    VillageId · OwnerUserId(nil이면 미점유) · ClaimedAtTime
                    CurrentHp · IsExposed(억제기 파괴 후)

PublicPlayerSummary UserId · DisplayName · AvatarSnapshot · EquippedGearSummary
                    MatchLevel · VillageGrowth
                    BossKillCount · MonsterKillCount
                    LineStage (1~4. 낮을수록 잘 버티는 중)
```

🔴 **비공개 필드는 `PublicPlayerSummary` 에 넣지 않는다** — 매치·계정 재화 / 창고 / 시설 레벨·HP·파괴 상태 / 수비 몬스터 종류·수·HP·훈련 레벨 / 파병 편성. 서버가 허용 필드만 복사해 반환한다.

---

# B부. 이벤트

## B-1. EventBus

**왜**: 직접 호출은 "A가 B를 호출 → A의 파일이 B를 안다 → 구독자를 추가할 때마다 발행자 파일을 고쳐야 한다"가 된다. 여러 명이 붙으면 그 발행자 파일이 충돌 지점이 된다.

> **기능 추가 = 새 파일 + 데이터 행. 기존 파일 수정 0.**

### 위치와 API

`RootDesk/MyDesk/Core/EventBus.mlua` — `@Logic`. 서버·클라 양쪽에 각각 존재.

```
_EventBus:Publish(ev)                              -- 같은 쪽 구독자에게 발행 + 디버그 로그 1줄
_EventBus:PublishToClients(name, payload)          -- @ExecSpace("Multicast")
_EventBus:PublishToUser(name, payload, forUserId)  -- @ExecSpace("Client")
```

### 발행

```lua
local ev = MonsterKilledEvent()
ev.MonsterId = 1001
ev.MapName = "Henesys_Hunt_HillNorth"
ev.KillerUserId = userId
_EventBus:Publish(ev)
```

🔴 **발행자는 구독자를 모른다.** 발행자 파일에 구독자 이름이 등장하면 규칙 위반.

### 구독 — 구독자 자기 파일에서

**정적 (권장, 정리 불필요):**

```lua
@EventSender("Logic", "EventBus")
handler HandleMonsterKilled(MonsterKilledEvent event)
    -- 몬스터 처치 시 도감 진행도를 갱신한다.
end
```

**동적 (조건부로 붙였다 뗄 때만):** `_EventBus:ConnectEvent(...)` + **`OnEndPlay` 에서 `DisconnectEvent` 필수**, 핸들은 **`property any`** 에 보관.

⚠️ 주의
- `@EventSender` → `handler`, `ConnectEvent` → `method void`. **섞으면 컴파일되고 영영 안 불린다**
- `ConnectEvent` 는 Entity/Logic/Service 에만 있다 → **버스가 Logic이라 Component 안에서도 붙일 수 있다**(버스를 두는 실용적 이점)
- 동적 구독 핸들을 지역 변수에 두면 GC된다
- `handler` 는 예약어 → `eventHandler`

### 서버 / 클라 경계

이벤트는 **같은 쪽 안에서만 전파된다고 가정한다.** `EventType` 객체는 **RPC를 넘지 못한다**(허용 타입 목록에 없다).
반대편에 알려야 하면 `PublishToClients(name, payload)` 로 **이름 + 문자열 payload** 를 넘기고 받는 쪽에서 재구성해 `Publish` 한다. 재구성 표는 `EventBus.mlua` 안에 두되 **크로스가 실제로 필요한 이벤트만** 등록한다.

> ⚠️ **미확인**: 실제 전파 범위와 `EventType` 파라미터 허용 여부는 EventBus 첫 구현 때 로그로 확인하고 **결과를 여기에 적는다.**
>
> 확인 결과: _(EventBus 구현 시 기입)_

## B-2. 작성 규약과 도입 순서

한 이벤트 = 한 파일, **발행 도메인 폴더**에.

```lua
@Event
script TowerDestroyedEvent extends EventType
    property string MatchId = ""
    property string VillageId = ""
    property string Stage = ""      -- TOWER1 / TOWER2 / SUPPRESSOR
end
```

1. payload는 **원시 타입과 문자열만.** `any`/table 금지 — RPC로 넘기면 `[LEA-3036]` 이고 **빌드에 안 잡힌다.** 목록은 문자열 직렬화
2. 열거값은 **문자열**
3. 🔴 **중복 방지 키를 포함** — `SettlementId` 등. 같은 결과를 두 번 반영하면 안 된다
4. 🔴 **payload는 append-only.** 필드 추가는 안전(기존 구독자는 모르는 필드를 무시). **이름 변경·삭제는 구독자를 깨뜨린다** → 공지 후 단독 작업

**도입 순서** — 이 문서만 고쳐서 push하면 안 된다. 정의 없이 구독하면 빌드 로그에 `Symbol not found` 가 난다.
발행자가 **3종 세트를 한 번에** push: ① 이 문서 B-3에 행 추가 ② `{도메인}/{Name}Event.mlua` ③ refresh로 생성된 **`.codeblock`**(EntryKey가 `codeblock://{소문자명}` 이라 결정론적 → 커밋해도 안전).

🔴 **정의 PR이 먼저 머지되고, 구독자 PR은 그 뒤다.**

## B-3. 이벤트 명세

> **규격은 전부 확정. `.mlua` 구현은 해당 기능을 만들 때.** "폴더" 가 곧 **소유권 배정 단위**다.
> ⚠️ = M2 예약 · **굵은 필드** = 중복 방지 키 · 🔴 = 다대다 지점(반드시 이벤트로)

### `Match/` — 매치 진행 · 맵 이동

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `MatchStartedEvent` | MatchId, PlayerIdsCsv, **DifficultyLevel**, StartServerTime | 마을 초기화 · HUD · 통계 · **난이도 배수 적용** |
| `MatchPhaseChangedEvent` | MatchId, Phase, ElapsedSeconds | 미니언 흐름 · HUD · 침범/발록 해금 |
| `MatchLevelChangedEvent` | UserId, PrevLevel, NewLevel | 🔴 **10=전직 자격 · 21=침범·발록 자격** · HUD · 통계 |
| `ExpGainedEvent` | UserId, Amount, SourceType, SourceId | 레벨 계산 · 통계 |
| `PlayerEliminatedEvent` | MatchId, UserId, Reason | 파병 취소 · 통계 · 정산 · 로비 퇴장 |
| `MatchEndedEvent` | MatchId, WinnerUserId, Reason | 정산 · 상태 폐기 · 결과 UI |
| `MapEnteredEvent` | UserId, MapName, PrevMapName | 맵별 컨트롤러 · 통계 · BGM |
| `MapLeftEvent` | UserId, MapName | 정리 |

### `Stat/` — 플레이어 성장·전투 계산

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `StatRecalculatedEvent` | UserId, FinalStatsCsv, Source | 데미지 계산 · 이동/점프 반영 · 스탯 UI |
| `ApAllocatedEvent` | UserId, AllocatedStatsCsv, RemainingAp | 레벨 원장 · 스탯 UI |
| `SpSpentEvent` | UserId, SkillId, Amount, RemainingSp | B의 스킬 트리 · HUD · 스탯 UI |

`FinalStatsCsv`와 `AllocatedStatsCsv`는 ExecSpace 경계를 넘는 table 대신 문자열로 직렬화한다. `StatService`만 Base·Equip·Enhance·Buff를 합산한다.

### `Village/` — 코어 · 소유권

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `VillageClaimedEvent` | VillageId, OwnerUserId, ClaimedAtTime | 🔴 **NPC 10종 · 포탑 · 모집/훈련 잠금 해제** · 통계 · 토스트 |
| `VillageClaimDeniedEvent` | UserId, VillageId, Reason | 거절 토스트 |
| `VillageCoreDamagedEvent` | VillageId, AttackerUserId, Amount, RemainingHp | **주인에게 즉시 알림** · HUD |
| `VillageFallenEvent` | MatchId, VillageId, OwnerUserId | 🔴 **게임 오버 처리** · 파병 취소 · 통계 · 정산 |

### `Lane/` — 미니언 상시 흐름 · 전선

> ⚠️ **전투 인스턴스가 없다.** 미니언은 그냥 계속 걸어오고 포탑·수비 몬스터·플레이어가 그때그때 막는다. "전투 시작/종료" 이벤트를 만들지 않는다.

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `MinionSpawnedEvent` | MatchId, VillageId, MonsterTypeId, InstanceId | 전투 AI · 통계 |
| `MinionKilledEvent` | MatchId, VillageId, InstanceId, KillerUserId, IsDispatched | 🔴 **경험치·메소**(파병 몬스터는 보상 없음) · 통계 |
| `TowerDamagedEvent` | VillageId, Stage, RemainingHp | 포탑 UI · HUD |
| `TowerDestroyedEvent` | MatchId, VillageId, Stage | 🔴 **전선 판정** · 재건 UI · 통계. **1회성** |
| `SuppressorDownEvent` | MatchId, VillageId | 🔴 **억제기의 역설** — 미니언 강화 배율 + 경험치 상승 적용. **1회성** |
| `LaneStageAdvancedEvent` | MatchId, VillageId, NewStage(2~4) | 전선 이동 · HUD · 공개 통계 `LineStage`. **1회성** |
| `TowerUpgradedEvent` / `TowerRepairedEvent` / `TowerRebuiltEvent` | VillageId, Stage, NewLevel | **성장 포인트** · 통계 · UI |
| `DefenderEnteredLaneEvent` / `DefenderLeftLaneEvent` | UserId, VillageId, MapName | 파밍 시간 손실 통계 · HUD |

### `Dispatch/` — 파병

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `DispatchSubmittedEvent` | **OrderId**, SenderUserId, TargetVillageId, Count | 보유 목록 차감 · 미니언 흐름 합류 · UI |
| `DispatchCancelledEvent` | **OrderId**, SenderUserId | 보유 목록 복귀 · UI |
| `DispatchJoinedEvent` | **OrderId**, TargetVillageId | 방어자 "파병 섞임" 경고(총량 등급만) · 통계 |

### `Monster/` — 몬스터 · 엘리트

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| 🔴 **`MonsterKilledEvent`** | MonsterId, MapName, KillerUserId | **경험치 · 메소 · 드랍 · 도감 · 업적 · 통계 (6)** |
| `EliteSpawnedEvent` | MapName, EliteMonsterId, BaseMonsterId | 알림 · 통계 |
| `EliteKilledEvent` | EliteMonsterId, MapName, KillerUserId | 🔴 **엘리트 재료 지급** · 도감 · 업적 · 통계 |

> `MonsterKilledEvent` 가 이 설계의 핵심 사례다. 직접 호출로 만들면 **6명이 한 파일을 경합**하고, 이벤트로 하면 각자 자기 파일에 `handler` 하나씩이다.

### `Boss/` — 보스 · 발록

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `BossDamageRecordedEvent` | BossId, UserId, Amount | 누적 피해 원장 |
| `BossKillResolvedEvent` | BossId, WinnerUserId | **최다 피해 판정** → 보상 독식 · 도감 · 업적 |
| `BossFirstClaimedEvent` | BossId, UserId, **SettlementId** | **마노 선취 1회** — 10렙제 체력 갑옷 + 메소 |
| `BalrogChallengeStartedEvent` | MatchId, UserId, ChannelKey | 메소 차감 · 개인 채널 입장 |
| 🔴 **`BalrogKilledEvent`** | MatchId, UserId, **SettlementId** | **매치 승리 판정** · 정산 · 종료 |

### `Npc/` — NPC 상호작용

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `NpcOpenedEvent` | UserId, NpcInstanceKey, CatalogNpcId, UiGroupName, UiRoute | UI 라우팅 · 통계 |
| `NpcOpenDeniedEvent` | UserId, NpcInstanceKey, Reason | 거절 토스트 |
| `NpcActionExecutedEvent` | VillageId, CatalogNpcId, ActionId, UserId | 🔴 **성장 포인트** · 통계 · 업적 |
| `ItemCraftedEvent` | UserId, RecipeId, ResultItemId | **성장 포인트** · 도감 · 업적 |
| `ItemEnhancedEvent` | UserId, ItemId, GemId, NewLevel | 🔴 **보석 소모** · 장비 상태 · 업적 |
| `ItemRepairedEvent` / `ItemPurchasedEvent` | UserId, ItemId / ShopItemId, Count, Cost | 내구도 / 인벤토리 |
| `StorageChangedEvent` | UserId, SlotIndex, ItemId, Count | 창고 UI |
| `MonsterRecruitedEvent` | UserId, VillageId, MonsterInstanceId, MonsterTypeId | **성장 포인트** · 보유 UI |
| `MonsterDeployedEvent` | VillageId, MonsterInstanceId, SlotIndex | 배치 UI |
| `MonsterDefenderDiedEvent` | VillageId, MonsterInstanceId | 보유 목록 **영구 제거** · 통계 |
| `MonsterHealedEvent` | VillageId, MonsterInstanceId, NewHp | UI |
| `TrainingStartedEvent` | VillageId, MonsterTypeId, TargetLevel | 동시 작업 배수 재계산 · UI |
| `TrainingCompletedEvent` | VillageId, MonsterTypeId, NewLevel | **성장 포인트** · 병과 보너스 · 업적 |

### `Item/` — 인벤토리 · 장비

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `EquipChangedEvent` | UserId, EquipSlot, PrevInstanceId, NewInstanceId | `StatService` · 장비/인벤토리 UI |

`ItemEnhancedEvent`는 기존 `Npc/` 표의 확정 이벤트를 그대로 사용한다. 장비 강화 후에는 `ItemEnhancedEvent` → `StatRecalculatedEvent` 순서로 발행한다.

### `Map/` — 맵 · 포탈 · 월드맵

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `MapEntryDeniedEvent` | UserId, MapName, Reason | 거절 토스트 (21레벨 미만 침범 시도 등) |
| `PortalUsedEvent` | UserId, RouteId, FromMap, ToMap | 통계 · 월드맵 갱신 |

### `Economy/` — 재화

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `ItemDroppedEvent` / `ItemPickedUpEvent` | MapName, ItemId, Count, OwnerUserId / UserId, ItemId, Count | 드랍 엔티티 · 인벤토리 · 도감 |
| `MesoGainedEvent` / `MesoSpentEvent` | UserId, Amount, SourceType·SinkType, SourceId·SinkId | 재화 원장 · HUD · 업적 |
| `GemGainedEvent` / `GemSpentEvent` | UserId, GemId, Count | 재화 원장 · **강화 UI** |
| `EliteMaterialGainedEvent` / `EliteMaterialSpentEvent` | UserId, MaterialId, Count | 재화 원장 · **포탑 강화 UI** |

> `SourceType`/`SinkType` 이 어긋나면 통계·업적이 전부 어긋난다. §0-2 목록을 벗어난 값을 쓰지 않는다.

### `Progression/` — 기록 · 보상

| 이벤트 | payload | 주요 구독 |
|---|---|---|
| `AchievementProgressedEvent` / `AchievementUnlockedEvent` | UserId, AchievementId, Current, Required | 업적 UI · 보상 |
| 🔴 **`RewardGrantedEvent`** | UserId, RewardSourceType, RewardSourceId, **SettlementId** | 재화·아이템 지급 · **영구 저장** · 토스트 |
| `CollectionUpdatedEvent` | UserId, MonsterId, Kind(`DISCOVER`/`KILL`) | 도감 UI · 업적 |
| `VillageGrowthPointAddedEvent` | UserId, VillageId, ActionKey, Points | 공개 통계 `VillageGrowth` |

> 🔴 **`RewardGrantedEvent` 가 보상의 단일 통로다.** 업적·보스·도감·발록이 각자 지급하지 않고 **전부 이 이벤트를 발행**한다. `SettlementId` 로 중복 지급을 막는다 (GDD §4.10: "같은 정산 ID를 두 번 지급하지 않는다").

### `Skill/` `Job/` — 상대 담당

⬜ **시스템 등록서 대기** (§1). 특히 아래가 최우선 협의 항목이다:

| 필요한 이벤트 | 왜 |
|---|---|
| `JobChangedEvent(UserId, PrevJobId, NewJobId)` | 🔴 **10레벨 전직 → 코어 연결 자격**. 상대 작업이 사용자 골격의 크리티컬 패스에 있다 |
| `SkillUsedEvent(UserId, SkillId, MapName)` | 쿨다운 UI · 통계 · 업적 |

---

## B-4. 도메인 ↔ 폴더 ↔ 담당

| 도메인 | 폴더 | 담당 |
|---|:---:|:---:|
| EventBus | `Core/` | A |
| 매치 진행 · 맵 이동 · 난이도 · 승패 · 계정 | `Match/` | A |
| 코어 · 소유권 | `Village/` | A |
| 미니언 흐름 · 전선 · 포탑 | `Lane/` | A |
| 파병 | `Dispatch/` | A |
| 몬스터 · 엘리트 | `Monster/` | A |
| 보스 · 발록 | `Boss/` | A |
| 맵 · 포탈 · 월드맵 | `Map/` | A |
| NPC · 상호작용 | `Npc/` | A |
| 플레이어 스탯 · 데미지 | `Stat/` | A |
| 인벤토리 · 장비 · 강화 | `Item/` | A |
| 재화 | `Economy/` | A |
| 기록 · 보상 | `Progression/` | A |
| **스킬 · 전직** | **`Skill/` `Job/`** | **B** |

기존 폴더(`Catalog/` `Spawn/` `Faction/` `Farm/` `Summon/` `Camera/` `Terrain/` `WorldMap/`)는 유지하되, 이관 계획은 §협업-규칙에 따른다.

---

## 변경 이력

| 날짜 | 변경 |
|---|---|
| **2026-09-03** | **[등록] 플레이어 스탯·장비·강화·기능 NPC·UI.** `StatId` 13종·6장비 슬롯·4아이템 종류·4NPC 섹터, 신규 CSV 6종, `MatchPlayerState` 성장 원장, UI 릴레이 및 이벤트 계약을 추가했다. `FunctionalNpcCatalog`에 `SectorId`·`SlotOrder`를 추가하고 보석 표를 A-2로 승격했다. |
| **2026-08-29 (2차)** | **현행 GDD(정합성 정정본) 기준으로 전면 재작성.** 초안이 개정 전 기획 위에 쓰여 표 이름부터 어긋나 있었다 — `WavePhaseConfig`→`MinionPhaseConfig` / `WaveComposition`→`MinionComposition` / `DefenseMapConfig`→`LaneConfig` / 대포탑 2모드→포탑 3종+코어(`TowerConfig`) / NPC 14행→12행·60인스턴스. **엘리트 · 주니어 발록 · 마노 선취 · 억제기의 역설 · 난이도 신규 반영.** 이벤트를 전투 인스턴스 전제에서 **상시 흐름** 구조로 재설계 |
| 2026-08-29 | 최초 작성 (개정 전 GDD 기준 — 폐기) |
