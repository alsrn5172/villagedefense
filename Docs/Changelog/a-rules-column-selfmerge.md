# a/rules-column-selfmerge

## 2026-09-17 — 규칙 문서 정리 (B #40 질문 → A 결정)

B 가 #40 comment 5700352071 에서 찾은 규칙 문서 간 어긋남 세 곳을 A 결정대로 문서에 반영했다.

- **열 추가는 등록 대상이 아니다** (`스키마-계약.md` §1 등록 대상 판정). §0-1 9 헤더 변경 규칙(공지 후 단독)만 따르고,
  계약서 헤더 정의와 `check-integrity` CANONICAL 은 구현 PR 안에서 같이 고친다.
  B 에게 준 영문 요약본(저장소 밖)의 "column → 등록 PR 먼저" 문구는 A 가 따로 고쳤다.
- **헤더 변경 공지는 Draft PR 로 충분하다** (`협업-규칙.md` §3-4 · `스키마-계약.md` §0-1 9). 단 PR 본문 맨 위에
  "○○ 헤더 변경 포함" 을 적는다. 이슈 공지는 따로 필요 없다.
- **self-merge 판정은 폴더 기준이 정본** (`협업-규칙.md` §3-3). 괄호를 "8번이 비어 있는 PR" → "8번에 남의 폴더 파일이 없는 PR" 로 정정.
  자기 폴더 안 기존 파일 수정도 24시간 무응답 시 self-merge 가능.
- **`Docs/` 는 공용** (`협업-규칙.md` §2-2). 자기 시스템 몫(자기 표 헤더 정의 · 자기 CANONICAL · 자기 CHANGELOG 조각)은
  남의 폴더 수정으로 치지 않는다. `스키마-계약.md` 와 CSV 헤더는 여전히 공지 후 단독.

코드 · 데이터 변경 없음.
