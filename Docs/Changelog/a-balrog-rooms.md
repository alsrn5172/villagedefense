# a/balrog-rooms — 발록 방 6개 + 선취 판정 = 승리 조건 (WO-017)

> base `main 90b0d92`. 지시서: `메월드폴더/WorkOrders/WO-017-발록방-6개-선취판정.md`

**이건 게임의 승리 조건이다.** 이 브랜치 전에는 매치가 끝나는 길이 "전원 탈락" 과 "30분 만료" 뿐이었고
**이기는 방법이 없었다.** GDD 첫 줄의 "주니어 발록을 가장 먼저 잡는 사람이 이긴다" 가 코드에 없었다.

---

## 2026-09-10 — 단계 0: 발록 방 맵 5개 복제 (`4047bb0` · `885c623`)

- `MapBuilder.fromTemplate` 으로 `Sleepywood_Boss_JrBalrog` → `…2` ~ `…6`. EntryKey · 루트 경로 · 엔티티 UUID ·
  내부 참조를 전부 새로 쓴다. 42개 맵 · id 3987개 · **충돌 0** · 옛 맵명 잔존 0건.
- `Global/SectorConfig.config` 에 `map://` 5줄 (알파벳 순 · CRLF 유지).
- `PortalRoutes.csv` 에 복귀 행 `S01B2` ~ `S01B6`.
- **BossId 는 6방이 `8130100` 하나를 공유한다.** `BossCatalog:94` 가 중복 BossId 행을 폐기하고
  `BossSkillRunner` 가 스킬을 BossId 로만 찾는다 — 방마다 나누면 `BossSkills` 7행이 방마다 복제돼 35행이 된다.
  대신 `BossSpawner.AllowClonedMap` (기본 `false` → 다른 보스 5종 동작 불변)을 복제 방 5개에만 켰다.
- Maker 가 Reimport 에서 **EntryKey 를 소문자로 정규화** (`map://sleepywood_boss_jrbalrog2`) —
  기존 맵이 전부 소문자라 Maker 가 고쳐준 것. `fromTemplate` 은 넘긴 이름을 그대로 쓴다.
- 런타임: 6방 전부 로드 · 방마다 발록 1마리 · 다른 보스 6종 정상 스폰.

## 2026-09-11 — 방 배정 · 입장권 · 90초 · 선취 판정

### 사용자 확정

| 항목 | 결정 |
|---|---|
| 방 배정 | **매치 시작 때 참가 순서대로 1회 고정.** 입장할 때마다 배정하지 않는다 — 못 잡으면 **HP 그대로 재도전**하는 게임이라 내 방 발록이 내 피해를 들고 있어야 한다 |
| 입장권 | 낙인의 영혼석(`BRAND_SOULSTONE`) · ★1~5 = **1 · 5 · 10 · 15 · 20** 개 · 입장 때 소모 · 환불 없음 |
| 진입 | **A안** — 마을 포탈 1개 → 서버가 배정된 방으로 보낸다 |
| 퇴장 | 90초 만료 · 사망 · 걸어서 나감 = 슬리피우드 마을로. **배정은 유지** |
| 레벨 제한 | 없음 (계약서 A-2-10 의 `RequiredLevel 21` 은 채택 안 함) |
| 재생성 금지 | **필요 없다** — 발록 하나가 죽는 순간 매치가 끝난다 (사용자 지적) |

### 무엇을 만들었나

| 파일 | 무엇을 |
|---|---|
| `Boss/BalrogRoomService.mlua` **(신규)** | 배정 · 입장 판정 · 입장권 소모 · 도착 확인 · 90초 · 사망/이탈/탈락 · 선취 판정 · 누적 피해 |
| `Boss/BossSpawner.mlua` | `ResetBoss()` — 보스를 풀 HP 새 엔티티로. 발록 방만 부른다 |
| `Match/MatchSessionLogic.mlua` | `StartMatch` → 방 배정 훅 · `DeclareWinner` · 결과 화면 발록 피해 열 · 탈락 → 방에서 꺼내기 |
| `PortalNetwork.mlua` | `GatedToMap` — 발록 입구 행만 바인딩 제외 · ↑키를 서버 판정으로 넘김 |

### 설계 판단

- **발록 HP 를 따로 저장하지 않는다.** 코드로 확인했다: 보스 HP 자연회복 없음(`Monster` 가 HP 를 채우는 곳은
  스폰과 `Respawn()` 뿐인데 보스는 꺼져 있다) · 플레이어가 방을 나가면 `StateChaseMonster.IsTargetValid` 가
  **맵 검사로 먼저** 대상을 푼다(`KeepTargetForever` 보다 앞) · 빈 방에선 `DistanceToTarget = 99999` 라
  스킬이 하나도 안 나간다(소환도 안 쌓인다). **발록 엔티티 자체가 장부다.**
- **누적 피해 = `MaxHp - 현재 HP`.** 1인 1방이라 자기 방 발록에 준 피해가 곧 그 사람 것이다.
  결과 화면의 `damage = 0 -- TODO(WO-017)` 두 곳을 이걸로 채웠다. 승자는 MaxHp 라 순위 맨 위다.
- **매치 시작 때 6방 발록을 새로 깐다.** 사용자는 "매치가 끝나면 맵이 폐기된다" 로 알고 있었는데
  **실제로는 안 없어진다** — 발록 방은 `IsInstanceMap: false`(정적 맵)이고 코드 전체에 보스를 리셋하는 곳이 0곳이다.
  안 깔면 다음 판이 ① 죽은 방(5분 뒤에야 재생성) ② 깎인 HP 를 물려받는다.
  리스폰을 끄는 것만으로는 **이긴 사람 방의 발록이 영영 안 나온다.**
- **발록 입구는 `LoadRoutes` 에서 거른다.** `BindRow` 에서 건너뛰면 `AttemptBindings` 가 완료로 안 세서
  `RetryTimeout`(8초) 뒤 "해석 실패" 경고를 잘못 남긴다. 클라 ↑키는 표를 직접 읽으므로 그 행을 여전히 찾는다.
- **도착 유예.** `MoveToEntityByPath` 직후엔 아직 마을에 있다. 다음 틱에 "방 밖" 으로 읽으면 들어가자마자
  쫓겨난다 → `moving → in` 두 단계로 두고 **도착해야 90초를 센다.** 8초 안에 안 나타나면 이동 실패로 푼다.
- **방 안 사망.** 엔진이 5초 뒤 **그 방의 SpawnLocation** 에 되살린다(`PlayerComponent.Respawn`:
  RespawnPosition → 맵 SpawnLocation → 입구). 사망하면 점유를 풀고, 되살아나면 마을로 꺼낸다.
- **탈락자.** `SpectateService.Enter` 는 캐릭터를 숨기고 멈추기만 하지 옮기지 않는다 → 방 안에서 탈락하면
  꺼낸다. 배정은 유지한다.
- **선취 판정 1회.** `BalrogRoomService.DeclareWinner` 가 `Winner` 로 막고, `MatchSessionLogic.Expire` 가
  한 번 더 막는다. 같은 틱에 둘 이상 죽으면 방 번호가 빠른 쪽 (결정적). 클라가 "이겼다" 를 보내는 경로는 없다.
- **리셋 실패 방은 판정에서 뺀다.** `lastHp` 를 안 채워서, "없는 발록" 이 죽은 것으로 읽혀 가짜 승자가 나오지 않게.

### 🔴 검증 과정에서 뒤집힌 전제

| 전제 | 실제 |
|---|---|
| "다른 보스도 리스폰 안 된다" | **된다.** `Monster.RespawnOn` 은 꺼져 있지만 `BossSpawner` 가 따로 `RespawnSeconds`(8종 전부 300) 뒤 새로 깐다 |
| "매치 끝나면 맵 폐기" | 정적 맵이라 **안 없어진다** → 매치 시작 리셋 |
| "탈락하면 관전으로 빠진다" | 관전 진입은 **옮기지 않는다** → 방에서 꺼내야 한다 |
| "방에서 죽으면 나간다" | 엔진이 **그 방 안에** 되살린다 → 부활 뒤 꺼낸다 |

### 🔴 Codex 2회 리뷰 — 반영 10 · 기각 2

같은 프롬프트를 **새 세션으로 두 번**(A·B) 돌리고, 인용 줄번호 21개를 전부 직접 확인했다 (이번엔 둘 다 정확).
한쪽만 낸 지적은 직접 검증해서 채택·기각을 따로 정했다.

| # | 구멍 | 조치 | 출처 |
|---|---|---|---|
| 1 | 0.25초 폴링 사이에 두 방이 죽으면 실제 선착순이 아니라 **방 번호로** 갈린다 | 발록 생사를 **매 프레임** 본다. 같은 프레임만 동률 — 서버 입장에서 동시다 | A·B |
| 2 | 시간 종료·전원 탈락 `Expire` 가 발록 폴링보다 먼저 돌면 **방금 죽은 발록이 승리로 안 잡힌다** | `Expire` 가 승리 아닌 종료 직전에 `ClaimPendingWinner` 로 한 번 더 묻는다. 안쪽 `Expire("승리")` 에선 Winner 가 차 있어 재귀하지 않는다 | A·B |
| 3 | 발록 리셋에 실패한 방에도 들어가 **입장권만 쓴다** | `IsRoomReady` — 입장권을 쓰기 전에 막는다 | A·B |
| 4 | 입장권을 이동 **전에** 빼서, 이동 실패면 못 들어가고 증발 | 요청 때 개수만 보고 **도착했을 때** 뺀다(`Arrive`) | A·B |
| 5 | 탈락이 부활 대기 표식을 지운다 · 옮기는 중 탈락하면 관전자가 방에 남는다 · 마을 이동을 걸자마자 표식을 지운다 | 내보내기 표식(`evict`) 하나로 합치고 **방 밖이 확인될 때까지** 유지. 늦게 도착하는 이동을 위해 최소 8초 | A·B |
| 6 | 90초 마감 뒤 같은 폴링 창에서 죽인 발록도 승리 | 마감을 **서버 시각 절댓값**으로. ~~무효 처치는 그 방 발록을 새로 깐다~~ → **09-13 사용자 규칙으로 교체**: 창 밖에선 발록이 아예 안 맞는다(무적) · HP 는 절대 되돌리지 않는다 (아래 09-13 절) | A·B |
| 7 | 결과 화면 피해가 **0.25초 전 캐시** | `DamageOf` 가 지금 HP 를 읽는다 (엔티티가 치워졌을 때만 캐시) | A·B |
| 8 | 매치가 끝나도 방에 남은 사람이 **다음 판 새 주인과 같은 방**에 서서 새 발록을 때린다 | `BeginMatch` 가 방에 남은 사람을 `LeaveToLobby` 순서(관전 해제 → 원장 초기화 → `ReturnToStation`)로 치운다 | A·B |
| 9 | `RequestEnter` 가 위치를 안 봐서 **변조 클라가 어디서든** 입장 | 서버가 **슬리피우드 마을에 있는지** 확인 | A 단독 · 채택 |
| 10 | 매치 중 접속을 끊었던 사람(마을이 없어 넥서스 탈락이 안 걸림)이 **재접속해 도전** | `MatchSessionLogic.leftUsers` 확인 | A 단독 · 채택 |
| ✗ | 마을 포탈에 **저장된** `PortalEntityRef` 가 남아 있으면 엔진 직행으로 우회된다 | **기각.** MapBuilder 로 읽어보니 그 포탈의 `PortalComponent` 에 ref 가 **없다** (`BoxOffset · BoxSize · IsLegacy · Enable` 뿐). 런타임에도 이제 안 건다 | A 단독 |
| ✗ | `BalrogRoomService` 가 없으면 매치를 시작하지 말라 | **기각.** `StartMatch` 는 로비가 심장을 빼고 사람을 옮긴 **뒤에** 불린다 — 거기서 막으면 더 나쁘다. 항상 등록되는 `@Logic` 이라 `log_error` 로 둔다 | B 단독 |

~~⚠️ 규칙 판단 1개 — 마감 뒤 처치를 무효로 하고 풀 HP 로 새로 깐다~~ → **09-13 사용자 확정으로 폐기.**
"도전 사이에 HP 가 회복되지 않는다" 를 어긴다. 아래 09-13 절의 무적 창으로 바꿨다.

### 인스턴스 룸 → WO-024 (별도)

사용자 질문 "매치가 여러 개인데 월드가 하나면 100명이면 월드 100개?" 에서 나왔다. 월드 100개는 필요 없지만
(MSW 가 인스턴스를 자동으로 띄운다) **지금 코드는 인스턴스 하나에 매치 하나**다. 인스턴스 룸이 해법이고
(룸마다 Logic 이 따로 돈다), 그렇게 가면 위의 **발록 리셋은 필요 없어진다.** 사용자 결정: WO-017 먼저,
룸은 Maker 시험 뒤 별도 WO. Codex 2회 영향 분석 + 쟁점 4건 직접 확인 → `WorkOrders/WO-024-인스턴스룸-동시매치.md`.

## 2026-09-13 — 사용자 규칙 반영 · 런타임 검증 · 버그 2건

### 사용자 확정 (09-13)

> 나의 발록 방은 고정이고, 내가 두 번에 나눠서 처치를 해도 됨. (첫 번째 도전 이후 발록의 체력이 다시 회복되지 않으므로
> 두 번째 도전에서 격파해도 됨. 단, 재료를 두 번 모아야 됨)

→ 여러 번에 나눠 잡아도 된다 · **도전 사이에 HP 는 절대 회복되지 않는다** · 도전마다 입장권을 낸다.

### 무엇을 바꿨나

| 파일 | 무엇을 |
|---|---|
| `Boss/BalrogRoomService.mlua` | **도전 창 밖 무적.** `SetBossVulnerable` 이 발록의 `HitComponent.Enable` 을 켜고 끈다 — 리셋 직후 끔 · 도착(`Arrive`)에서 켬 · 도전 종료(`Release`)에서 끔 · 90초 마감은 `CloseExpiredWindows` 가 **매 프레임** 끈다. 창 밖 사망(마감과 같은 프레임 · `HitComponent` 를 안 거치는 지속피해 오라)은 **주인 처치로 인정**하고 HP 를 되돌리지 않는다 |
| `Match/MatchSessionLogic.mlua` | `BeginMatch` 를 `Running = true` · `SetPhase(1)` **뒤로** (버그 1) |
| `StateChaseMonster.mlua` | 안전망 `GuardAgainstFlyAway` 를 `CanFallOffLedge` 몹에도 돌린다 (버그 2) |

- 왜 `HitComponent.Enable` 인가: `SpectateService` 가 관전자를 무적으로 만드는 스위치와 같다. 발록 모델의 피격 컴포넌트는
  네이티브 `HitComponent` 하나뿐이다(`MonsterHit` 없음). 런타임에서 `AttackFrom` 5타가 켜면 5 명중 · HP 감소, 끄면 0 명중 · HP 불변.

### 🔴 버그 1 — 매치 시작 직후 발록 방 감시가 꺼진다 (런타임에서 발견)

`StartMatch` 가 `BeginMatch` 를 `Running = true` **앞에서** 불렀다. `BalrogRoomService.OnUpdate` 는 "감시 중(active)인데
Running 이 꺼져 있다" 를 매치 종료로 읽는다 — 그 사이에 프레임이 한 번만 끼어도 `OnMatchOver` 가 돌아
**시작하자마자 `active=false`** 가 된다 (입장 · 90초 · 선취 판정 전부 멈춤). Maker `execute_script` 로 시작할 때 실제로 났다.
순서만 바꿨다. 수정 뒤 `active=true` · 가짜 "매치 종료" 로그 0.

### 🔴 버그 2 — 주니어 발록이 방 왼쪽 끝에서 떨어져 끝없이 추락 → 처치 불가 (런타임에서 발견 · 기존 버그)

- 방1 발록의 y 가 -688 → -1332 로 계속 내려갔다 (6.7 유닛/초 = 낙하 최고속도). 방2~6 은 스폰점(9.0,-1.3)에 서 있었다.
- 재현: 플레이어가 입구(-3.3, 3.45 · 윗발판)에 서 있으면 발록이 아래 발판에서 그 x 로 와 좌우로 흔들리다
  **발판 왼쪽 끝(x≈-4.35)** 을 넘는다. 그 밑은 허공이다.
- 원인: 보스는 `EnableFallOff`(발판 끝에서 떨어져 따라감 · 사용자 확정)만 켜져 있고, 맵 밖으로 빠진 걸 되돌리는 안전망
  `GuardAgainstFlyAway` 는 **`CanJumpToTarget` 일 때만** 돌았다. 2026-09-03 "보스는 점프하지 않는다" 이후 보스는 안전망이
  꺼진 상태였다. 안전망 주석엔 이미 "맵 아래로 빠진 경우도 잡는다 · IsolatedMove 를 풀면 끝없이 떨어질 수 있다" 고 적혀 있었다.
- 수정: `CanJumpToTarget or CanFallOffLedge` 일 때 돌린다. `CanFallOffLedge` 를 켜는 곳은 보스(`BossSpawner`) ·
  보스 소환수(`BossSkillRunner`) 둘뿐이라 일반 몹은 영향 없다.
- 런타임: 방2 발록을 발판 밖(-4.9)으로 밀자 up=-6.08 에서 `[FlyAway]` → 제자리(9.0,-1.3) 착지.
  방1 에서 플레이어를 입구에 세워 110초 관찰 — 1번 떨어져 up=-6.01 에서 되돌림 → 발판 끝(-4.30,-1.30) 착지 → 이후 추격 계속.
- ⚠️ 남는 모양새: 플레이어가 윗발판에 계속 서 있으면 발록이 가끔(110초에 1번) 떨어졌다 돌아온다. 아예 안 떨어지게 하려면
  발록만 `EnableFallOff` 를 끄면 되지만, "보스는 떨어져 따라간다" 는 사용자 확정 규칙의 예외라 이번엔 안 했다.

### 런타임 검증 (개인 월드 · TEST 프로필 · 1인)

| # | 확인 | 결과 |
|---|---|---|
| 1 | 부팅 | `gated S01A (서버 배정)` · `routes=82 bound=82 invalid=0` · `ready · rooms=6 costs=1,5,10,15,20 limit=90.0s` · 에러 0 |
| 2 | `StartMatch` | 배정 `1=<uid> 2~6=-` · 6방 발록 풀 HP 157500 · 전부 무적 · **active=true (버그 1 수정 확인)** |
| 3 | 입장권 0개로 입장 요청 | 거절 · 방 비어 있음 |
| 4 | 1차 입장 (영혼석 3) | `ENTER … 1개 확인 · 이동 중` → `맞기 시작` → `ARRIVE … 영혼석 -1` · 3→2 · 마감 90초 |
| 5 | 90초 자연 만료 | 그 프레임 `무적` → `EVICT (90초 만료)` → `→ 마을` · HP 불변 |
| 6 | 2차 입장 | 도착 HP **107500 그대로** · 2→1 |
| 7 | 무적 스위치 (실제 공격) | 창 안 `AttackFrom` 5타 = 5 명중 · HP 107500→107478 / 끄면 0 명중 · HP 불변 |
| 8 | 2차 90초 만료 | 무적 → EVICT → 마을 · HP 107478 유지 · `DamageOf` = 50022 (현재 HP 로 계산) |
| 9 | 3차 입장 → 실제 공격으로 처치 | 도착 HP 107478 · 1→0 · `WINNER` **1회** → `[Match] WINNER` → 결과 화면 `승리 — 밍키타` · `DamageOf` = 157500 |
| 10 | 다음 매치 | 방에 남은 플레이어 → 승강장 (`ClearStragglers`) · 6방 풀 HP 로 다시 깔림 |
| 11 | 창 밖 사망 (지속피해 경로: HP=0 + `Dead()`) + 같은 프레임 `Expire` | 경고 `창 밖에서 죽었다 — 주인 처치로 인정` · `WINNER` 1회 · 결과 1회 · 뒤따른 만료는 아무것도 안 함 |
| 12 | 발록 추락 | 버그 2 참조 |
| 13 | (Codex 반영 뒤) 방에서 걸어 나감 | 나가는 프레임에 `CloseExpiredWindows` 가 무적 → 그 뒤 0.25초 틱의 `EXIT` |
| 14 | (Codex 반영 뒤) 방 안 사망 (`ProcessDead`) | 다음 프레임 `CloseExpiredWindows` 무적 → `EVICT (사망)` → 5초 뒤 부활 → 마을 |
| 15 | (Codex 반영 뒤) 도전 중 시간 만료 | `Expire` **같은 호출 안에서** 무적 + 매치 종료 → 결과 화면. 종료 뒤 hit=false · active=false · 피해 0 |
| 16 | (Codex 반영 뒤) 마감 1초 당김 | 경계 `>=` 로 `무적` → `EVICT (90초 만료)` → 마을 |
| 17 | (Codex 반영 뒤) 승리 | `WINNER` 1회 → `Expire` 안에서 무적 + 매치 종료 **1회** → 결과 `승리 — 밍키타` · 이전 판 방에 남은 사람 → 승강장 |

- 표기만 틀린 로그: 리셋 스폰 때 `[BossSkillRunner] contact ready … dmg=1.0` 이 섞여 찍힌다. 스프라이트가 캐시돼
  `PreloadAsync` 가 동기로 불리면서 공격력 주입 **전** 값을 찍는 것 — 실제 피해는 `CalcDamage` 가 매 타 `AttackPower` 를
  새로 읽는다. 6방 전부 `atk=210` 확인. 공용 파일이라 안 건드린다.

### 🔴 Codex 2회 리뷰 × 2라운드 (09-13 변경분) — 반영 5 · 기각 2

1라운드는 변경 3건(무적 창 · 시작 순서 · 추락 안전망), 2라운드는 1라운드 반영분을 새 세션 두 번(A·B)씩 돌렸다.
인용 줄번호는 전부 직접 확인했다.

| # | 구멍 | 조치 | 출처 |
|---|---|---|---|
| 1 | 도전 중에 매치가 끝나면 `OnMatchOver` 가 무적을 안 걸어 **결과 화면 동안에도 발록이 맞는다** | `OnMatchOver` 가 모든 방 무적 | 1R A·B |
| 2 | 이전 판 감시(`active=true`)가 남은 채 `Running=true` 가 먼저 보이면 **이전 판 원장으로** 판정이 돌아 이전 판에 죽은 발록이 새 판 승리가 될 수 있다 | `Running` 전에 `ClearLedger()` 로 감시부터 끈다. 배정·리셋은 그 뒤 `BeginMatch` | 1R A·B |
| 3 | 사망 · 걸어서 나감은 0.25초 틱에서야 `Release` → 그 사이 남은 투사체·소환수가 발록을 깎는다 | `CloseExpiredWindows` 가 매 프레임 **사망 · 맵 이탈 · 접속 끊김**까지 보고 즉시 무적 | 1R B 단독 · 채택 |
| 4 | `Running=false` 뒤 무적은 다음 프레임 `OnUpdate` 에서야 → 결과 화면 뒤 1프레임 피격 | `Expire` 가 같은 호출 안에서 `OnMatchOver` | 2R A·B |
| 5 | 마감 경계가 `>`(창 닫기) 와 `>=`(내보내기) 로 섞였다 | 세 곳 모두 "now >= 마감 이면 닫힘" | 2R A 단독 · 채택 |
| ✗ | 창 밖 사망(`HitComponent` 를 우회하는 지속피해 오라)을 **승리로 인정하지 말라** | **기각.** ① 그 오라(`FactionAuraController`)가 붙은 맵이 저장소에 **0개** — 지금은 발록 방에 닿지 않는다 ② 인정 안 하면 죽은 발록이 방치되거나 스포너가 5분 뒤 **풀 HP 로 되살려** "HP 회복 없음" 규칙을 어긴다 ③ 1인 1방 고정 배정이라 그 방에서 피해를 줄 수 있는 사람은 주인뿐이다 | 1R·2R A·B |
| ✗ | 죽은 주인의 남은 피해로 발록이 죽으면 승자가 된다 | **기각.** 위와 같은 이유로 주인 처치다. 게다가 #3 으로 사망 프레임에 무적이 걸려 그 틈 자체가 사라졌다 | 1R B 단독 |

## 남은 것

| 항목 | 상태 |
|---|---|
| Codex 2회 코드 리뷰 | ✅ 09-11 (반영 10 · 기각 2) · 09-13 2라운드 (반영 5 · 기각 2) |
| **런타임 검증** | ✅ 09-13 (위 표) |
| `BalrogRoomService.codeblock` | ✅ Maker refresh 생성 |
| `Docs/스키마-계약.md` A-2-10 갱신 | 계약서는 `BalrogChallenge`(메소·레벨 21) + `BalrogRoom`(빈 방 동적 배정) 로 적혀 있어 결정과 다르다. **공지 후 단독** |
| 입장권 개수 표 이동 | `DifficultyConfig`(PR #50) 머지 뒤 `TicketCosts` 속성을 그 표 열로 옮긴다 |
| GDD §7 Phase 3 `발록 방 6개` · `선취 판정` → ✅ | 런타임 검증 끝. GDD 는 PR #49(WO-015)가 잡고 있어 **#49 머지 뒤** 별도로 |
| 발록 추락 모양새 | 윗발판에 서 있으면 110초에 1번꼴로 떨어졌다 돌아온다. 아예 막으려면 발록만 `EnableFallOff` 예외 — **사용자 판단** |
