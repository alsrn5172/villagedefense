# a/balrog-heart-economy — 발록의 심장 경제 · 소모 계층 (WO-022 S1)

> base `main 90b0d92`. 지시서: `메월드폴더/WorkOrders/WO-022-발록의심장-경제-난이도.md`
> 확정 값 근거: `Docs/추가기획2/미정-값.md` **G 절** (같은 브랜치 `0d006f5`)

## 2026-09-10 — 난이도별 심장 소모 + ★1 무료 스택 (S1)

### 왜

로비는 이미 난이도를 골라 매치를 출발시키는데, **심장은 표시만 하고 실제로 빠지지 않았다.**
`MatchLobbyGateway.HeartCost` 가 `★N = N개 · 혼자면 -1` 을 **코드에 박아** 계산해서
숫자를 보여 주기만 했고, `StartMatch` 에는 `-- TODO(WO-022)` 주석이 남아 있었다.

계정 영구 저장 계층(WO-018 S1)이 머지되어 `account_balrogHeart` 를 실제로 쓸 수 있게 됐으므로
**소모 쪽만** 먼저 붙였다.

### 범위 — 소모만. 지급은 없다

**🔴 순위별 지급(보상)은 이 조각에 없다.** 순위 판정 1순위가 "발록 누적 피해" 인데
그걸 재는 코드가 아직 없다(WO-017). 지급이 붙기 전에 `Grant*` 를 만들면 검증할 수 없는 코드가 남는다.

**🔴 인공심장(★4·★5 사용 불가)은 S2 다.** 표에 `RankerChallenge` 열만 미리 뚫어 뒀다.

### 확정된 규칙 (사용자 · G 절)

| # | 규칙 |
|---|---|
| G-1 | ★N 매치는 심장 **N개**. 참가 **1명이면 각 난이도에서 1개 감면** (★1 솔로 = 0 = 무료) |
| G-2 | 참가자 중 **한 명이라도** 부족하면 **매치 시작을 거절**한다 |
| G-3 | 차감은 **매치가 실제로 출발하는 순간** 한 번. **환불 없다** — 포기·탈락·탈주 모두 |
| G-4 | ★1 **전용** 무료 스택. 최대 2개 · 1시간에 1개 |
| G-5 | 2개가 꽉 차면 **타이머가 멈춘다.** 1개를 쓰면 그 즉시 새 1시간 사이클 |
| G-5' | 단 **이미 타이머가 돌고 있던 중이면 그대로 이어간다** — 59분에 쓴 사람이 손해 보면 안 된다 |
| G-7 | 신규 계정 = 무료 2 + 심장 2 |

### 무엇을 만들었나

| 파일 | 성격 |
|---|---|
| `RootDesk/MyDesk/Progression/BalrogHeartService.mlua` | **신규.** 소모 계층 전부 |
| `RootDesk/MyDesk/DifficultyConfig.csv` + `.userdataset` | **신규.** 난이도별 값의 **정본** |
| `RootDesk/MyDesk/Progression/AccountProfile.mlua` | 필드 2개 추가 · `SchemaVersion` 1 → 2 |
| `RootDesk/MyDesk/Progression/AccountStorageLogic.mlua` | `FreeHeartMax` · `FreeHeartRefillSec` 상수 |
| `RootDesk/MyDesk/Progression/AccountData.mlua` | 원장에 무료 스택 · getter/setter 5개 |
| `RootDesk/MyDesk/Match/MatchLobbyGateway.mlua` | 검증·차감 배선. TODO 해소 |

`BalrogHeartService` 가 답하는 것 셋:
`CostOf(난이도, 인원)` / `MissingReport(참가자, 난이도)` / `Charge(참가자, 난이도)`.

**🔴 값은 코드에 박지 않는다.** `HeartCost` 는 서비스에 위임하고 서비스는 CSV 를 읽는다.
밸런스 숫자가 코드와 표 두 곳에 생기면 반드시 갈라진다.

**🔴 저장은 `_PlayerDBManager` 창구만 거친다.** 서비스는 `_AccountData` 원장에만 쓰고
`_DataStorageService` 를 직접 부르지 않는다 (WO-018 의 단일 창구 규칙 · Credit 절약).

### `SchemaVersion` 을 왜 올렸나

`Deserialize` 가 이미 `t["키"] or 기본값` 이라 **필드만 추가하는 변경은 그 자체로 전방 호환**이다.
그래도 1 → 2 로 올린 이유는 **모양이 바뀐 사실을 기록**하기 위해서다 — 나중에
"이 세이브에 무료 스택 필드가 있나" 를 판단할 근거가 버전 말고는 없다.

옛 세이브(필드 없음)는 `freeHeartStack = 2` · `freeAtMs = 0` 으로 읽는다.
**그때는 무료를 쓸 수단이 아예 없었으니 꽉 찬 상태로 시작하는 게 맞다** — 0 으로 주면
"기능이 생기기 전부터 있던 계정만 손해" 가 된다. Codex 2회차가 이걸 버그로 지목했지만
의도된 마이그레이션이라 **채택하지 않았다.**

### 시각은 `DateTime.UtcNow.Elapsed` 다

무료 스택은 **오프라인 동안에도** 차야 하므로 절대 시각을 저장한다.

**🔴 `_UtilLogic.ServerElapsedSeconds` 는 쓸 수 없다.** "월드가 뜬 뒤 경과 시간" 이라
**재시작할 때마다 0으로 돌아간다** — 오프라인 충전이 통째로 깨진다.

이 저장소에 `DateTime.UtcNow` 사용처가 **0건**이었으므로 런타임에서 직접 확인했다:

| 확인 | 값 |
|---|---|
| `DateTime.UtcNow.Elapsed` | `63924642366622` — **서기 1년 기준 절대 ms** |
| `− epoch(1970)` | `1789045568618` = 유닉스 ms. 맞다 |
| 2초 뒤 차이 | `1996` ms. 실제로 흐른다 |

`6.4e13` 은 double 정확 표현 범위(`2^53 ≈ 9.0e15`) 안이라 JSON 왕복에도 정밀도 손실이 없다.

### 🔴 Codex 2회 교차검증에서 잡은 재화 구멍 6개

같은 프롬프트를 **새 세션으로 두 번** 돌려(A·B) 대조하고, 줄번호 8개를 전부 직접 확인했다.
**두 답이 갈린 2건은 내가 직접 재검증해서 각각 판정**했다.

| # | 구멍 | 어떻게 됐나 | 출처 |
|---|---|---|---|
| 1 | `_BalrogHeartService == nil` 이면 검증·차감을 **둘 다 건너뛰고 공짜 출발** | 없으면 **거절**로 반전 | A·B 공통 |
| 2 | 차감 뒤 `_MatchSessionLogic == nil` 이면 **심장만 사라지고** 매치는 없다 | **차감 전으로** 검사를 옮김 | A·B 공통 |
| 3 | `_PlayerDBManager == nil` 이어도 차감 성공 처리 → 다음 접속에 **심장이 되살아난다** | 창구 없으면 `false` | **B 단독** |
| 4 | 시계 역행 시 `at` 을 `now` 로 되감아, 복구될 때 **되감은 폭만큼 무료 스택이 공짜로** 들어온다 | `at` 유지 · 한 주기 초과만 재기준 | **A 단독** |
| 5 | 로드 전 기본값(0)을 실제 잔액처럼 심사 | 전원 `IsLoaded` 전엔 "불러오는 중" 으로 보류 | A·B 공통 |
| 6 | 원장 쓰기가 `CanWrite` 에 막혀도 `Charge` 가 **`true` 를 돌려준다** | 되읽어 **실제로 빠졌는지 확인** | 내가 찾음 |

#### 4번이 왜 재화 생성인가 (A 만 잡았고 B 는 `C. OK` 라고 했다)

`at` = 사이클 시작, 마감 = `at + 주기`.

- `at = 1000` · 주기 `100` → 마감 `1100`
- 시계가 `now = 500` 으로 되감김
- **옛 코드**: `at = 500` → 마감 `600`. 시계가 실제 `1000` 으로 복구되면
  `now - at = 500` → `gained = 5` → **무료 스택이 즉시 꽉 찬다**
- **고친 코드**: `at = 1000` 유지 → 마감 `1100`. 시계가 따라잡을 때까지 충전이 멈춘다

되돌아간 폭이 **한 주기를 넘으면** 일시 보정이 아니라 진짜 시각 리셋이라 보고 `now` 로 다시 잡는다.
이 방향은 마감이 **뒤로** 밀리므로 절대 공짜로 주지 않는다.

#### 6번 — `Charge` 가 자기 쓰기를 확인하지 않았다

`AddBalrogHeart` · `SpendFreeHeart` 는 둘 다 `CanWrite` 를 지나는데, 로드 전이면
경고만 찍고 **조용히 버린다**(반환값 없음). `Charge` 는 그걸 안 보고 `true` 를 돌려줬다.
결과: **아무도 안 냈는데 매치가 출발한다.**

이제 유저마다 차감 전후 잔액을 되읽어 `기대 == 실제` 를 확인하고, 어긋나면 `log_error` + `false`.
무료 스택이 버려져도 그만큼이 심장으로 넘어오므로 **심장 확인 하나로 둘 다 잡힌다.**

**⚠️ 일부만 빠진 채 `false` 를 돌려주는 경우가 있다.** 환불 수단이 없으므로
**뺀 것은 반드시 저장까지 보낸다** — 안 보내면 원장과 디스크가 갈라진다.

### 기타 방어

- `FreeHeartRefillSec = 0` 이면 나눗셈이 `inf` 가 되므로 `periodMs <= 0` 조기 반환
- `Charge` 는 **전원 검증을 먼저 끝낸 뒤** 한 명씩 뺀다 (앞사람만 빠지고 안 출발하는 상태 방지)
- 꽉 찬 상태에서는 타이머를 `0` 으로 멈춘다. 안 멈추면 다음 소모 때 **밀린 충전이 한꺼번에** 들어온다
- `math.min(maxStack, ...)` 클램프 덕에 시계가 앞으로 크게 튀어도 상한을 넘지 않는다

### 🔴 런타임 검증에서 잡은 버그 — DataSet 행은 1-based 다

**Codex 2회 교차검증이 둘 다 놓쳤고, 실제로 돌려보고서야 나왔다.**

`LoadConfig` 가 `for i = 0, n - 1` 로 돌았다. 그런데 MSW DataSet 행은 **1-based** 다:

```
GetRowCount() = 5
GetCell(0, "Difficulty") = nil     ← 0행은 없다
GetCell(1..5, "Difficulty") = 1..5
GetCell(6, ...) = nil
```

그래서 `i = 0..4` 로 돌면 0행(nil)을 버리고 **1~4 만 읽어 ★5 행을 통째로 빼먹는다.**
`table_` 에 키가 1,2,3,4 뿐이었다 (`[Heart] DifficultyConfig 4행 로드`).

**값으로는 안 드러났다.** ★5 가 없으면 `CostOf` 가 폴백 `d × FallbackHeartCost`(=5)를 쓰고
`IsRanker` 도 폴백 `d >= 4`(=true)를 쓴다 — 지금 표의 값과 **우연히 똑같다.** 그래서
`CostOf(5, 1) = 4` 처럼 정답이 나와 정상으로 보였다. 드러나는 건 표를 고친 뒤다:
★5 의 `HeartCost`·`RankerChallenge`·`Enabled` 를 CSV 에서 바꿔도 **조용히 무시됐을 것이다.**

저장소 관례도 1-based 다 (`PortalNetwork:81` · `:173` · `:322`). 내 것만 0-based 였다.
고친 뒤 `[Heart] DifficultyConfig 5행 로드` · `table_` 키 1~5.

> 교훈: **폴백이 정본과 같은 숫자를 내면 로드 실패가 값으로 안 보인다.**
> 그래서 `LoadConfig` 는 값이 맞나가 아니라 **행 수**를 봐야 한다.

### 런타임 검증 결과 (2026-09-11 · 개인 월드 · 워크트리 직결)

빌드 로그 **에러 0** (기존 Info 90건 + 알려진 `ParseStatCsv` 경고 1건, 새 파일은 무관).

| # | 확인 | 결과 |
|---|---|---|
| 1 | 표 로드 | `5행 로드` · `table_` 키 1~5 · ★5 가 폴백 아닌 **표에서** (`cost=5 ranker=true`) |
| 2 | `CostOf` 전 난이도 | d1 solo `0` / 2인 `1` · d2 `1`/`2` · d3 `2`/`3` · d4 `3`/`4` · d5 `4`/`5` |
| 3 | `IsRanker` | d1~3 `false` · d4·d5 `true` |
| 4 | `UsesFreeStack` | d1 만 `true` |
| 5 | 신규 계정 기본값 | `heart=2 free=2 freeAt=0` (G-7) |
| 6 | `PayableOf` | d1 `4`(무료2+심장2) · d3 `2`(심장만) |
| 7 | ★1 **무료부터** 쓴다 | `free -1 -> 1` → `free -1 -> 0` · **심장 2 그대로** |
| 8 | **타이머 이어가기 (G-5')** | 첫 소모 `(타이머 시작)` · 두 번째 `(타이머 유지)` — 사이클 재시작 안 함 |
| 9 | ★1 솔로 = 0개 | `차감 없음` · 심장·무료 변화 **0** |
| 10 | ★3 솔로 = 2개 | `heart -2 -> 0` · `(무료 0 + 심장 2)` |
| 11 | **부족하면 거절** (G-2) | ★5 솔로(4개) vs 보유 2 → `false` · **심장 변화 0** (부분 차감 없음) |
| 12 | **음수 금지** | 잔액 0 에서 ★2 → `false` · `heart=0` |
| 13 | **오프라인 충전** | `freeAt` 을 2.5시간 전으로 → `무료 충전 0 -> 2 (경과 9000초)` · 상한 2 클램프 · `freeAt=0`(정지) |
| 14 | **시계 역행 · 한 주기 이내** | 30분 미래로 → `free=0` (**안 준다**) · `at` **유지** — Codex A 지적 수정 확인 |
| 15 | **시계 역행 · 한 주기 초과** | 2시간 미래로 → `at` 재기준 · **마감이 뒤로** 밀림 (절대 공짜 없음) |
| 16 | **저장·복원** | `heart=7 free=1 freeAt=63924657971660` 심고 flush → Play 재시작 → **그대로 복원** |
| 17 | **ms 정밀도** | 14자리 절대 ms 가 JSON 왕복에서 **한 자리도 안 깨졌다** |
| 18 | **오프라인 시간이 실제로 흐른다** | 재시작 전 `SecondsToNextFree=2999` → 후 `2970`. 그 사이 실제 경과분만큼 줄었다 — `ServerElapsedSeconds` 를 쓰면 0으로 돌아가 불가능한 동작 |

검증 뒤 원장은 `heart=2 free=2 freeAt=0` 으로 되돌려 놨다.

**검증하지 못한 것**: 구멍 6번(원장 쓰기 거부 감지)은 로드 실패를 인위로 만들어야 해서
런타임으로 못 짚었다. 코드 경로는 `기대 == 실제` 대조 + `log_error` 로 남아 있다.

### 남은 것

| 항목 | 왜 안 했나 |
|---|---|
| `Docs/스키마-계약.md` 등록 | `AccountProfile` 신규 2필드 + `DifficultyConfig` — **공지 후 단독** 규칙 (이슈 #40 공지 완료) |
| 순위별 **지급** | 발록 누적 피해를 재는 코드가 없다 → WO-017 |
| 인공심장 (★4·★5 차단) | S2 |
| G-1 ★4 총합 16 vs 17 · G-6 탈락자 지급 · G-8 매치★ = 발록던전★ | **미정 3건** (`미정-값.md` G 절) |
