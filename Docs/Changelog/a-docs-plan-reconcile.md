# a/docs-plan-reconcile — WO-015 M1 계획 문서 개정

> 지시서: `메월드폴더/WorkOrders/WO-015-계획문서-개정.md`. base `main 2e9aa58`. PR #44.
> 🔴 **코드·CSV 변경 0줄.** 문서만.

## 2026-09-09 — 폐기 규칙 본문 교체 + §7 상태 재판정

### 왜

`VillageDefense-M1-GDD.md` 가 2026-08-28 이후 멈춘 사이 **추가기획1(09-04)·추가기획2(09-08)** 가 핵심 규칙을 갈아엎었고, 구현은 WorkOrder(WO-001~014) 트랙으로 앞서갔다. 그래서 두 가지가 동시에 틀어져 있었다:

1. **규칙이 틀렸다** — 본문이 4단 방어선·21레벨 침범 게이트·20~25분·2~6인·4페이즈를 *정본으로* 서술했다. **문서를 믿고 구현하면 폐기된 사양을 만드는** 상태.
2. **상태가 틀렸다** — §7 84개 항목이 거의 전부 `⬜` 인데 Phase 1·2 대부분은 이미 구현돼 있었다.

PR #42 가 헤더 메타 + §4 매치 구성까지 맞췄고, 이 PR 이 본문과 체크 상태를 마저 정리했다.

### 교체한 규칙 (GDD §1~§9 전반)

| 폐기 | 교체 | 출처 |
|---|---|---|
| 4단 방어선 (1차포탑·2차포탑·억제기·코어) | **3단** 포탑·억제기·**넥서스** · 맵당 시설 1개 · `TowerStage`/`LaneStage` 3값 | 추가기획1 |
| 21레벨 침범 게이트 | **`PHASE3` 페이즈 게이트** · 그 전 시설 피해 **5%** · 레벨 기준 아님 | 추가기획2 |
| 미니언 4페이즈(1/2/2.5/3) | **5단** `PHASE0-1`·`PHASE0-2`(전직 유예 30초)·`PHASE1`·`PHASE2`·`PHASE3` · 2분 간격 | 추가기획2 |
| 20~25분 · "별도 타이머 없음" | **30분 명시 만료** (`MatchConfig.MatchDurationSeconds`) | 추가기획2 |
| 마을 6개 점유 · 2~6인 | 점유 **5개** · **2~5인** · **리스항구 = 공용 시작 지역**(`Enabled=false`) | 추가기획1 |
| 매치 자원 4종 | **7계층** — 카운터 3종(메소·빅토리아 주화·**발록의 심장**) + 인벤토리 `MATERIAL` 35종 | 추가기획1 |
| 엘리트 = 변종 5종 + 전용 재료 | **자이언트**(베이스 스케일업) · `EliteMaterialInfo` 폐기 | 추가기획1 |
| 기능 NPC 10종 · 60 인스턴스 | **마을 12 × 5 + 3**(로비 안내원 · 차원 관문 · 리스항구 초보자 상점) | 추가기획1 + 사용자 |
| 훈련 45초 · 과부하 배수 `1+0.4(n-1)` | **종류 단위 Lv1~5** · 꿈의 조각 3/5/10/15 **즉시 소모** · 배율 1.0~5.0 | 추가기획1 |
| 보스 도보 입장 | **입장권**(지역재화 25 · 발록 낙인의 영혼석) + **디멘션 게이트**(에너지 코어 1) | 추가기획1 |
| 막타 보상 | **누적 피해 최다**(동률=먼저 때린 쪽 · 남에겐 안 보임 · 피해 0이면 미지급) + 자석 + 물약 쿨타임 | 추가기획2 |
| 결과 화면 M2 · 관전 M3 | **둘 다 M1 승격** | 추가기획1 |

**위치 확정 (2026-09-09 사용자)**: 넥서스 = **마을 하단 미니언 길 위** · 파병 담당관 = **각 마을**(`OWNER_ONLY`) · 매치 안내원 = **로비**.

### §7 상태 재판정 (84개 항목)

판정 기준 — **실물(파일·행)이 있으면 최소 `🟡`, 런타임 확인된 것만 `✅`, 실물이 없는 것만 `⬜`.** 규칙이 바뀐 항목은 문장을 먼저 새 규칙으로 고친 뒤 상태를 매겼다(옛 문장에 `✅` 를 달면 폐기 규칙이 완료로 박제된다).

| Phase | 결과 | 남은 것 |
|---|---|---|
| 0 통합 게이트 | **9/11 ✅** | `Global/WorldConfig.config` CoreVersion **`26.5.0.0` 미승격**(실측) · 데이터 패키지 감사 |
| 1 세로 조각 | **14/19 ✅** | 로비 맵 · `LevelTable` 실값 · 나머지 4마을 지형 · 파병관 마을 이전 |
| 2 경제·NPC | **14/18 ✅** | `LevelTable` 실값 · 엘리트 자이언트(0행) · 미니언 구성/배율 |
| 3 파병·발록 | **6/15 ✅ · 3 🟡** | 발록 방 **6개 복제**(현재 1개) · 도전/선취 판정. 파병은 구현됐으나 **회귀로 막힘**(아래) |
| 4 기록·영구 성장 | **0/10 ⬜** | 🔴 **DataStorage 계층 자체가 없다.** 발록의 심장·플레이 기록·순위판이 전부 여기 |
| 5 5마을 확장 | **2/11 ✅** | 레인 지형 **9/15맵**(헤네시스·커닝·페리온 완료 · 엘리니아·노틸러스 남음) |

주요 실측 근거(2026-09-09 최종): `Global/WorldConfig.config` = **`26.5.0.0`** / `Environment/config` = `26.7.0.0` · `LaneConfig` **9행(3마을)** · `BossInfo` 8행(주니어 발록·마노 포함) · 슬리피우드 맵 2개 · **발록 방 1개뿐**(`BalrogChallenge`/`BalrogRoom` 표 없음) · `EliteMonsterInfo`·`EliteSpawnTable` **0행** · `LevelTable` 전 행 자리표시자 · `Dispatch/` **있음**(`DispatchRule` 4행) · **`Progression/` 없음** · 맵 35개.

> ⚠️ 1차 조사 때 `LaneConfig` 1행 · `Dispatch/` 없음으로 기록했으나 **낡은 스냅샷이었다.** 사용자 지시로 현재 main 기준 재실측해 §7·§4.9·§9 와 로드맵을 다시 맞췄다.

### Roadmap

- 헤더 — `M1 ⬜ 미착수` → **`🔨 active`** · 통합 대상 → **`강화하고살아남기`** · 20~25분 → 30분
- **제품 비전·전체 출시 기준**도 새 규칙으로(사용자 지시) — 2~5명 · 3단 방어선 · 30분 · `PHASE3` 게이트 · 5단 곡선 · 드랍 소유권
- 기능 지도 A~H 전면 갱신 (포탑 2개 행 → 포탑·억제기 / 결과화면·관전 M1 승격 / 드랍 소유권·자석·물약 쿨·디멘션 게이트 신규 행)
- 미정 항목 — **#2·#4·#5·#9 해소 표시**, #7·#8·#12 문구 갱신. 결정 이력 3행 추가(추가기획1·2·WO-015)
- M2 를 "승리 루프 확장"으로 재정의(난이도 ★·발록의 심장 곡선·인공심장·패스권) · M3 에 마을별 시설 고유 효과 5종

### Phase1 문서

7개 브랜치 전부 `✅` 로 갱신하고 맨 위에 **"과거 기록 · 이후 작업은 `WorkOrders/WO-*.md` 가 관리"** 를 명시했다. 삭제하지 않았다 — M1 자체가 진행 중이라 초기 구현 순서의 기록으로 남긴다.

### 🔴 발견한 회귀 — 파병이 전 페이즈에서 막혀 있다

`Dispatch/DispatchService.CurrentRule()` 은 `_MatchSessionLogic.CurrentPhaseName` 으로 `DispatchRule` 을 조회한다.
그런데 **WO-014 가 페이즈를 5단으로 개명한 뒤 `DispatchRule.csv`(4행)와 `DispatchService.mlua` 폴백 상수가 옛 이름(`PHASE1_PIONEER`·`PHASE2_VILLAGE`·`PHASE25_PRESSURE`·`PHASE3_FINALE`)에 그대로 남았다.**

```
RuleOf(phaseName) → 모르는 페이즈면 { enabled = false, villageCap = 0 }   ← "안전한 쪽"
CurrentPhaseName  → PHASE0-1 / PHASE0-2 / PHASE1 / PHASE2 / PHASE3
DispatchRule.csv  → PHASE1_PIONEER / PHASE2_VILLAGE / PHASE25_PRESSURE / PHASE3_FINALE
```

→ **모든 페이즈에서 파병 접수가 거절된다.** 에러도 경고도 없다(폴백이 조용히 막는다).
WO-014(페이즈 개명)와 파병 브랜치가 병행 개발돼 머지 시점에 어긋난 것이다. `LaneFacility` 의 같은 문제는 WO-014 에서 고쳤지만 `DispatchRule` 은 그때 존재하지 않아 놓쳤다.

**고치는 법**: `DispatchRule.csv` 4행 + `DispatchService.mlua:34-37` 폴백 4줄을 새 이름으로. `PHASE0-1`/`PHASE0-2` 는 미니언이 없으니 `enabled=false`.
**이 PR 에서는 고치지 않았다** — 문서 전용 PR 이라 CSV·코드를 건드리지 않는다. 별도 WO.

교훈은 §9 위험 표에 추가했다: **페이즈 값을 바꿀 때 `CurrentPhaseName` 을 읽는 모든 구독자와 `Phase` 열을 쓰는 모든 표를 함께 검색한다.**

### 발견해서 기록만 한 것 (2)

🔴 **수비대 1묶음 마릿수 — 기획 10 / 코드 8.** 모집 **비용**(재화 8개)을 **마릿수**로 잘못 쓴 버그다(`LaneStateService.mlua:633`). 몬스터별 묶음 크기 표(`MonsterRecruit`)가 없어 지금 고쳐도 임시 상수라, GDD §4.8 각주 + [`추가기획2/미정-값.md`](../추가기획2/미정-값.md) B-3 에 올리고 **코드는 건드리지 않았다.**

### 검증

- `node Docs/tools/check-integrity.cjs` — 전부 통과, 경고 3건(기준선 유지)
- 폐기 용어 잔존 검색 — 본문 0건 (변경·결정 이력 행은 폐기 기록이라 의도적으로 남김)
- 줄바꿈 — GDD·Phase1 **LF**, `Roadmap.md` 는 원래 **CRLF** 라 그대로 유지(`.gitattributes` 가 `* -text` 라 바꾸면 전체 파일 충돌)
- `git diff` 에 코드·CSV 0줄

---

## 2026-09-10 (3차) — WO-018 S1 · WO-019 · WO-023 머지 반영

> base `main 90b0d92`. PR #49. 🔴 **코드·CSV 변경 0줄.**

### 왜

1차(PR #42 · 헤더)와 2차(PR #44 · 위 절)로 규칙 교체는 끝났다.
그런데 그 뒤 **WO-018 S1 · WO-019 · WO-023 이 머지되면서 문서의 🔴 경고 3개가 정반대가 됐다.**

| 문서가 하던 말 | 실제 |
|---|---|
| *"로비 맵이 아직 없다 — `LeaveToLobby` 가 토스트만 띄운다"* | 승강장·배 2맵 + 매치 브라우저 완성 |
| *"DataStorage 계층이 아직 없다"* | `Progression/` 4파일 · 복원 런타임 검증 완료 |
| Phase 4 *"전부 미착수다. `PlayerDBManager` 도, DataStorage 계층 자체도 없다"* | S1 완료 → 2 ✅ · 2 🟡 |
| Phase 3 *"🔴🔴 회귀 — 파병이 전 페이즈에서 막혀 있다"* | **이미 고쳐져 있다.** `DispatchRule.csv` 5행 · `DispatchService.mlua:37-41` 폴백 둘 다 새 이름 |

마지막 것은 **문서만 보고 별도 WO 를 팠으면 헛일이 될 상황**이었다.

### 뒤집힌 계약 — 준비 확인 폐기

§7 Phase 1 과 Roadmap 이 *"로비 큐·**준비 확인**"* 이라고 적고 있었다. **폐기됐다.**
준비 토글 대신 **대기 중인 매치 목록에서 골라 참여하고 매치 주인이 시작**한다 (WO-023 · 사용자 확정).
하한도 **2명 → 1명**(솔플 허용).

### 실측으로 정정한 숫자

Codex 2회 교차검증 뒤 **직접 다시 세어** 확정했다. 6개 다 두 회차와 일치했다.

| 항목 | 문서에 있던 값 | 실측 |
|---|---|---|
| `map/` 의 맵 | 35개 | **37개** (로비 2 추가) |
| 기능 NPC 런타임 | "= 60 인스턴스" | **63** (12 × 5마을 + 공용 3). 카탈로그는 15행 |
| `LaneConfig` 마을 | (Phase 5 미기재) | **3마을** — `HENESYS`·`KERNING`·`PERION` 9행. 엘리니아·노틸러스 없음 |
| §7 총 항목 | 84개 | **85개** |
| `WorldConfig` CoreVersion | `26.5.0.0` | ✅ 그대로 (**미승격**) |
| Elite 2표 | 0행 | ✅ 그대로 |

### 상태 재판정

| Phase | 전 | 후 | 근거 |
|---|---|---|---|
| 0 | 9/11 | **10/11** | 패키지 감사 ⬜→✅ — WO-018 S1 에서 실제로 감사했다(공식 `player-data-package` 의 `Core/` 는 밴/킥+GM 툴 전용이라 계정 저장 골격이 아니었고, 설치 패키지 0개라 키 충돌도 없다) |
| 1 | 14/19 | **17/20 ✅ · 3 🟡** | 헤더 숫자 자체가 실제 bullet 수와 안 맞았다. `MatchLobbyGateway` 🟡→✅ |
| 4 | 0/10 | **2/10 ✅ · 2 🟡** | 저장키 감사·`PlayerDBManager` ✅ / 정산 보존·순위 판정 🟡 |
| 5 | 2/11 | **2/11 ✅ · 3 🟡** | 🟡 표기가 빠져 있었다 |

### 같이 넣은 것

`Docs/MAP-NAMING-RULES.md` 에 **`Lobby` Role 추가** — WO-019 가 만든 로비 맵 2개가 규칙에 없는 Role 을 쓰고 있었다.
*"`Lobby` 맵은 `LaneConfig`·`MapMonsters`·`VillageNpcSector` 에 등록하지 않는다"* 도 같이 박았다 (매치 원장 밖이다).

### 검증

- `check-integrity.cjs` 전부 통과 · 경고 4건(기준선 유지)
- 폐기 용어 **본문 0곳** — 남은 것은 §10 이력 행 하나가 "해소" 로 인용한 것뿐이다
- 🔴 **줄바꿈** — GDD·Phase1·MAP-NAMING **LF**, `Roadmap.md` 는 **CRLF 298** 로 원래대로.
  (삽입한 이력 행이 LF 로 들어가 한 줄 섞였던 것을 잡았다. `.gitattributes` 가 `* -text` 라 섞이면 파일 전체가 충돌한다)
- 🔴 **이 조각을 덮어쓰지 않았다** — 2차(93줄) 기록 아래에 이어 붙였다. 한 번 덮어썼다가 `git checkout` 으로 되돌렸다
- `git diff --stat` 이 `Docs/` 5개만 — 코드·CSV 0줄
