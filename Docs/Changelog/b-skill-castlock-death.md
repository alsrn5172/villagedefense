# b/skill-castlock-death

## 2026-09-25 — 시전 락 중 사망이 사라지는 버그 수정

### 증상 (#83 4차 Play 에서 발견 · 사용자 결정으로 별도 PR)

- 지상에서 스킬을 쓰면 시전 락(`SkillCaster.LockPresentation`)이 클라 `StateComponent` 를 끈다(걷기 모션 방지).
- 그동안 치명타를 맞으면 엔진의 사망 전이(DEAD)가 무시되고, 락이 풀린 뒤에도 다시 오지 않는다.
  - 실측: 락 중 치명타 → HP −700 · 서버 · 클라 모두 `IsDead=false`(락 해제 뒤에도). 클라 `ProcessDead()` 도 락 중엔 무시(state IDLE).
  - 락 밖에서는 같은 타로 바로 DEAD(서버 · 클라).
- 결과: 음수 HP 로 계속 살아 있고, 휘두르기 · 늦게 들어가는 스킬 타격 · 스킬 연출이 그대로 이어졌다. 모든 시전 락 스킬 공통.

### 수정

- `Skill/SkillCaster.mlua`
  - `LockPresentation`: 락 동안 0.05s 마다 HP 감시. 0 이하가 되면 곧바로 `ReleaseCastLock(castId, "dead")`(상태기 복구) → 아직 안 죽었으면 `ProcessDead()` 로 사라진 사망을 다시 건다 → 서버에 `ReportCastLockDeath`. 이 시전의 락이 끝나면 감시도 스스로 멈춘다.
    - 2026-09-25 보강(1차 Play 결과): **시전을 HP 0 인 채 시작하면 감시하지 않는다** — HP 가 이 시전 동안 0 으로 *떨어질 때만* 사망을 다시 건다. A 의 부활 팝업 선택은 HP 0 인 채 살려 두는데(아래 · #40), 그 상태의 첫 지상 시전이 곧바로 사망 · 페널티를 한 번 더 불렀다(재현: 사망 #6). 로그 `death watch off`.
  - 신규 `ReportCastLockDeath(castId)`(Server · 관전자 차단): 서버에서도 HP 0 이하(또는 사망)일 때만 · 그 시전자의 스킬 연출 엔티티 `SkillFx_<uid>_*`(휘두르기 잔상 등)를 지우고 1.5s 동안 0.1s 마다 다시 쓸어낸다(늦게 스폰되는 연출까지).
    - 그 1.5s 안에 부활하면(부활 팝업에서 곧바로 고르기 등) 쓸기를 곧바로 멈춘다 — 부활 직후 새로 시전한 스킬의 연출을 지우지 않게. 로그 `sweep stopped (revived)`.
    - 2026-09-25 보강(1차 Play 결과): 부활 판정 = **사망(`IsDead`)을 한 번 본 뒤 다시 살아남** · HP 는 보지 않는다. 처음엔 `Hp > 0` 도 요구했는데 A 의 팝업 부활은 HP 0 이라 쓸기가 끝까지 돌았다. 알림 순간 서버는 아직 `IsDead=false` 일 수 있어(HP 0 이 먼저 옴 · 실측) "사망을 본 뒤" 조건이 필요하다.
- `Skill/SkillAttack.mlua` `IsAttackTarget`: 시전자(플레이어)가 HP 0 이하거나 죽었으면 false — 시전 뒤 늦게 들어가는 스킬 타격(파워 스트라이크 접촉 등)이 안 들어간다.
  - 2026-09-25 보강(1차 Play 결과): **아이언 바디 반사(`FlatTag`)는 뺀다.** 반사는 맞은 그 틱에 들어가는 피해라 늦은 타격이 아닌데, 막으면 치명타에는 반사가 사라졌다(시전과 무관 · #95 이전엔 들어갔다).

### 건드리지 않은 것

- 사망 · 부활 흐름 자체(A 의 `PlayerRespawnService` · 엔진) — 사라진 사망을 다시 걸기만 한다.
- `PlayerHit`(A) · 시전 락 길이 · 공중 시전(상태기를 끄지 않음).
- `EffectService` 로 튼 연출(엔티티가 아님)은 지우지 못한다 — 예: main 의 파워 스트라이크 찌르기 불꽃(#83 에서 없어짐).

### 1차 Play (2026-09-25 · 결과 → 위 보강 3건)

테스트 빌드 = 이 브랜치 `fef8460` + #83 `6a66379` 을 합친 로컬 전용 브랜치(푸시 안 함 · 끝나고 지움) — 0.45s 대기는 #83 의 파워 스트라이크 타이밍이다(main 은 0.2s · 0.75s 2단).
진짜 매치 = A 의 `_MatchSessionLogic:StartMatch({uid}, 1)`(AutoStartOnPlay 와 같은 개발 경로 · LIVE 프로필) — A 의 `PlayerRespawnService` 가 돈다. 로비 맵(정적 룸)에서: 실제 인스턴스 룸은 로비가 계정 데이터(심장)를 써야 만들어져 쓰지 않았다.
세팅(메모리만): Lv30 전사 3차 · 전사의 강철 검(T20) · 파워 스트라이크 쿨 0 · 달팽이 2마리(HP 1억 · 고정 · 접촉 끔). 치명타 = 시전 0.2s 뒤 HP 1 + 달팽이 `MonsterAttack:AttackFast`(진짜 접촉 경로).

**빌드 경고: 1 → 1**(`LWA-1111` 기존 · 에러 0). 실행 경고 9건 = 전부 기존(`BossCatalog` 4 · `LWA-3047` 3 · 첫 시전 `LWA-3048` 2). 에러 6건 = A `[BalrogRoom] 방N 스포너 없음`(정적 룸에서 매치를 시작해서 · 이 PR 무관).

| 항목 | 기대 | 결과 |
|---|---|---|
| 시전 0.45s 대기 중 치명타 | 곧바로 DEAD(서버 · 클라) · 락 풀림 · 스킬 타격 안 들어감 · 연출 엔티티 지워짐 | **PASS** — +0.21s 치명타 → 클라 `cast lock OFF (dead)` · IDLE→DEAD 1번 · 서버 DEAD 1번 · 0.45s 타격 없음(달팽이 HP 그대로) · 숨긴 잔상 1개 지워짐 · 드러나지 않음 |
| 시전 밖 사망 | 예전처럼 DEAD | **PASS** — 엔진 사망 그대로 · 이 PR 코드 안 돎 |
| 시전 중 일반 피격 | 예전처럼(휘두르기 · 타격 그대로) | **PASS** — 사망 없음 · 0.45s 타격 −225 · 잔상 +0.45s 표시 · 락은 타이머로 풀림 |
| 사망은 한 번만 | 사망 1건 = `ProcessDead` 1번 · 부활 대기 시간 그대로 · A `[Death]` +1 · 페널티 1번 | **PASS** — 사망 7건 모두 클라 DEAD 1번 · 서버 사망 1번 · A `[Death]` 1번(사망 횟수 1→7 · 건너뜀 · 중복 없음) · 시간 부활 15.08 / 14.99 / 15.08s(= RespawnDuration 15) · 2페이즈 페널티 1번씩(메소 −5000 · 10100→5100) |
| 부활 직후 시전 | 1.5s 쓸기 안에 부활해 시전해도 그 연출이 남는다 · 스킬 정상 | **A 팝업 부활로는 FAIL** — HP 0 인 채 부활 → 쓸기가 끝까지 돎 · 그 상태의 첫 지상 시전이 사망 #6(페널티 한 번 더). **HP 를 채운 부활(A 수정 흉내)로는 PASS** — 치명타 0.43s 뒤 부활 → `sweep stopped (revived)` → 0.67s 에 재시전 · 잔상 남아 +0.45s 표시 · 타격 −225 |

1차 Play 에서 찾은 것:

- **아이언 바디 반사가 치명타에 사라짐(이 PR 탓)** — 반사가 `IsAttackTarget` 을 거치는데 HP 0 이라 막혔다(`flat 50362` 로그는 찍히는데 달팽이 HP 그대로 · 시전 밖 사망도 같음) → 위 보강.
- **A 의 부활 팝업 선택은 HP 0 인 채 살린다(A 기존 · 이 PR 무관)** — 대조 실험: 시전 없이 죽고 팝업 선택 → 살아 있음 · HP 0/201450(서버 · 클라 · HUD). 시간 부활은 HP 를 채운다. #40 에 요청(높은 우선순위).
- 참고(A): 만렙에서는 경험치 페널티 팝업이 −1250 을 보이지만 실제 차감은 0(`ExpCutOf` 는 표시용 `ExpProgress` · 차감은 레벨 시작 경험치 기준). #40 FYI.

### 2차 Play (2026-09-25 · 보강 3건 확인 · 전부 PASS)

테스트 빌드 = 이 브랜치 `2cf974f` + #83 `6a66379`(로컬 전용 · 끝나고 지움). 1차와 같은 진짜 매치 · 세팅 · 치명타 방법.

**빌드 경고: 1 → 1**(`LWA-1111` 기존 · 에러 0). 실행 경고 9건 = 전부 기존(1차와 같음) · 에러 6건 = A `[BalrogRoom] 스포너 없음`(정적 룸 매치 · 이 PR 무관) · 새 경고 · 에러 없음.

| 항목 | 기대 | 결과 |
|---|---|---|
| 시전 0.45s 대기 중 치명타 (+반사) | DEAD 1번 · 늦은 타격 없음 · 잔상 지워짐 · **반사 들어감** | **PASS** — 달팽이 100000000 → 99949638(반사 −50362) · 0.45s 타격 없음 · 서버 · 클라 DEAD 1번 · A `[Death]` 1번 · 잔상 지워짐 · 부활 15.07s |
| 시전 밖 치명타 (+반사) | 엔진 사망 그대로 · **반사 들어감** | **PASS** — 반사 −50362 · 이 PR 코드 안 돎 · `[Death]` 1번 · 부활 15.03s |
| 시전 중 일반 피격 | 휘두르기 · 타격 그대로 | **PASS** — 사망 없음 · 0.45s 타격 −225 · 잔상 +0.45s 표시 · 락은 타이머로 풀림 |
| 팝업 부활(HP 0 · A 미수정) 뒤 쓸기 | `sweep stopped (revived)` | **PASS** — 부활하자마자 멈춤 |
| HP 0 인 채 시작한 시전 | 재사망 없음 | **PASS** — `cast lock #4 started at HP 0 — death watch off` · 사망 횟수 그대로(3) · 잔상 남아 +0.45s 표시 · 락 타이머로 풀림 · 메소 −5000 1번 |
| 사망은 한 번만 | 사망마다 DEAD 1번 · `[Death]` 1번 | **PASS** — 사망 3건: 클라 DEAD 3번 · 서버 사망 3번 · A 사망 횟수 1→3 |

- HP 0 인 채 부활한 상태의 시전은 피해가 안 들어간다(`IsAttackTarget` 의 HP 0 검사) — 의도한 동작이다. A 가 팝업 부활 HP 를 채우면(#40 `5830769756`) 사라지는 상태.
