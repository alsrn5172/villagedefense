# M1 정리 후보 (WO-031 묶음 7 · 2026-09-26)

> GDD §7 Phase 5 "통합 월드의 모든 맵을 열거하고 샘플·임시 엔티티 후보를 목록화 → 사용자가 확인한 뒤 승인된 항목만 제거".
> ✅ **사용자 결정 2026-09-26**: 4번(모델 없는 도감 행 2개)만 삭제 · 1~3번은 남김. 지우기 전 전체 참조 검색(협업-규칙 §7) — 문서·히트박스 캐시에만 이름이 남아 있었고 런타임 참조 없음.

## 조사 방법

- 맵 43개 전부 `MapBuilder.read` 로 읽음(원본 JSON 안 봄) · 참조 = CSV(`VillageConfig` `LaneConfig` `PortalRoutes` `MapMonsters` `MapNpcs` `MapNpcs_Village` `BossInfo` `EliteSpawnTable` `WorldMapNodes`) + `SectorConfig` + `.mlua` 문자열.
- 엔티티 이름에 template/sample/test/dummy/샘플/테스트 가 든 것 · `check-integrity` C5(맵에 박제된 NPC) · C6(모델 없는 도감 행).
- UI 그룹 22개 · 개발용 스크립트.

## 결과

**맵**: 42개는 게임이 쓴다(전부 CSV 또는 스크립트가 참조 · `*Template` 이름 맵 없음). 안 쓰는 템플릿 맵 **없음**. (`Henesys_Hunt_GolemsTemple` 은 이름의 "Temple" 이 걸렸을 뿐 실제 사냥터.)

| # | 후보 | 무엇 | 지우면 | 제안 |
|---|---|---|---|---|
| 1 | `Test_Lane_Fx` 맵 + UI `LaneTestRemoteGroup` + `Lane/LaneTestDriver` · `Lane/LaneTestRemoteUI` | 시설 연출 테스트맵과 리모콘(WO-025). 이미 **Maker Play 에서만** 켜지고 그룹 월드엔 이 맵으로 가는 포탈이 없다 | 시설 공격 연출을 다시 볼 때 테스트맵이 없다 | 🟡 **남김 권장**(개발 도구 · 배포 영향 없음) |
| 2 | UI `DevStatRemoteGroup` + `Stat/DevStatRemote` | 개발용 스탯 리모컨(백쿼트 키 · 스탯 · 메소 · 보석 · AP 지급) | 스탯·재화 테스트를 execute_script 로만 | ✅ **이번에 배포 게이트를 걸었다** — Maker Play 가 아니면 서버·클라 모두 꺼짐(예전엔 배포 월드에서도 켜졌다 · 구멍). 파일은 남김 권장 |
| 3 | 맵에 박제된 NPC 3개 — `Ellinia_Hunt_GiantTree` `npc-7046` · `Orbis_Lobby_VictoriaStation` `npc-4525` · `Perion_Boss_Stumpy` `npc-2746` | `staticnpc` 모델이 맵 파일에 직접 놓임(`MapNpcs` 행 없음 · 스포너 밖) | 그 자리 장식 NPC 가 사라진다 | ❓ 사용자 판단(장식이면 남김 · 실수면 제거) |
| 4 | `MonsterInfo` 행 2개 — `9010028` 쓰로우백 리본돼지 · `9010033` 쓰로우백 아이언호그 | 모델이 없는 도감 행(C6 경고 · 어디서도 스폰 안 함) | 경고 2건 사라짐 · 영향 없음 | ✅ **삭제함**(2026-09-26 승인) |
| — | `MonsterInfo` 의 스폰 안 되는 종 56행 · 몬스터 모델 84개 | 모델은 전부 표가 참조 중 · 도감(`MapMonsters` 24종) 밖 종은 나중 콘텐츠 자원 | — | 남김(정리 대상 아님) |

## 5레인 부하 시험 (Maker 필요)

`MatchTallyService.StartLoadProbe(초)` — 계측 동안 5초마다 `[Load] fps=… maxDt=… minions=… defenders=… HENESYS=12m/5d/0x/0q …`(m 미니언 · d 수비대 · x 파병 개체 · q 파병 대기).
레시피(TEST 프로필 · 개인 월드): `_VillageOwnership.ClaimLevel = 1` → 가짜 유저 5명으로 5마을 `Claim` → `_MatchTallyService:StartLoadProbe(300)` → 웨이브 3~4회 → 로그 판독(fps · 가장 긴 프레임 · 개체 수 상한 · 파병 상한 `DispatchRule.VillageCap`).
