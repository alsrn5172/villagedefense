@Event
script SkillStateChangedEvent extends EventType

    -- Fired by PlayerSkillState whenever sp, mp, skillLevels, or jobId changes. "what" names
    -- which one, so a listener can skip a refresh it doesn't care about: "sp" | "mp" | "skill" | "job".
    property string what = ""

end
