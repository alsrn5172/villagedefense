@Event
script SkillStateChangedEvent extends EventType

    -- B 내부 · 클라 전용. PlayerSkillState 가 sp/mp/skill/job 이 바뀔 때 발행하는 UI 갱신 신호.
    -- What = "sp" | "mp" | "skill" | "job". 타 도메인은 구독하지 않는다 (계약서 B-3).
    property string What = ""

end
