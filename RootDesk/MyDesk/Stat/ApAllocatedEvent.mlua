@Event
script ApAllocatedEvent extends EventType

	-- 레벨업 AP 가 직업 주스탯 비율로 자동 분배됐을 때 발행한다 (계약서 B-3). payload 는 문자열·정수만.
	property string UserId = ""

	-- 이번에 분배된 양: "str=3;dex=1;int=0;luk=1"
	property string AllocatedStatsCsv = ""

	-- 분배 후 남은 AP (자동 분배라 보통 0)
	property integer RemainingAp = 0

end
