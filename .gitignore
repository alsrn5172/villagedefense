# ─────────────────────────────────────────────
# 강화하고살아남기 — LocalWorkspace + git
# 규칙: Docs/협업-규칙.md
# ─────────────────────────────────────────────

# mlua LSP 런타임 + node_modules
# (지금은 4KB뿐이지만 Maker가 언제든 여기에 수천 개를 풀어놓는다)
.mswai/

# 🔴 API 토큰이 평문으로 들어간다. 절대 커밋 금지.
#    합류 시 .mcp.json.example 을 복사해 자기 토큰을 넣어 쓴다.
.mcp.json

# 개인 로컬 설정
.claude/settings.local.json

# VS Code 연동 산출물 (LocalWorkspace가 생성)
.vscode/
*.code-workspace

# 스킬 툴체인 사본 — 정본은 .claude/skills/ 하나뿐
.agents/

# 세션 도구 — 각자와 그 사람의 AI 사이 1:1 대화다.
# 영구 자산(명명 규칙·로스터 CSV)은 Docs/ 로 옮겨져 있다.
WorkOrders/
메세지.md

# OS
Thumbs.db
desktop.ini
